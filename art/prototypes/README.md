# art/prototypes — throwaway tests of where Lumi's art could go

Nothing here is cut into `public/` or served by the app. Each prototype tests a bet in `docs/art-direction.md` §8 and is kept so the next session can rebuild it rather than start again. What each one showed goes in `docs/art-direction.md` §4a → *Progress*. Generated outputs are gitignored; the scripts rebuild them from the repo.

## `lumi-pieces/` — the pieces (puppet) test, bets 1 and 2

One drawing moved by code, beside today's whole-drawing sprite. Built 2026-09-13 by a subagent from the hands-free hub; published as the artifact [Lumi pieces](https://claude.ai/code/artifact/36d8ad96-7c2c-44f1-b138-a033e27129b1).

- `split.py` — splits `art/lumi/lumi-free-rest.png`: mattes her off the ground, paints the eyes and their glow out of the face, measures the eyes, face outline, neck pivot and head-weight rows into `rig.json`, and writes `body.png` (the figure at 600px) and `sprite.png` (the comparison sprite's breath rows from `public/lumi-idle.webp`).
- `index.src.html` — the page: two stages (today's sprite, the pieces), sizes 36 · 68 · 150 · 300, paper or a dark room, auto idle, and cues (blink, glances, happy, curious, sleepy, tilt). The head moves on a triangle mesh whose weight falls to zero across the bow, so the collar bends instead of tearing; the eyes are drawn in canvas with the measured colours. `build.py` inlines `body.png`, `sprite.png` and `rig.json` into `index.html`, the file that was published.
- `poses.py` — renders ten poses (`poses.png`), the avatar-size heads (`heads.png`) and close-ups, with the page's maths, to check without a browser.

Rebuild, from the repo root: `python3 art/prototypes/lumi-pieces/split.py && python3 art/prototypes/lumi-pieces/build.py && python3 art/prototypes/lumi-pieces/poses.py`.

**Status:** Chanté, 2026-09-13: "I think lumi pieces looks great", and asked for the walk test below. Before that she was open to puppeting "but still want[s] some sway/breathing motion. Don't want totally static" — the page has a breath but no sway yet. Weak spots it names: the hood tilts as one stiff shape, the eyes slide without foreshortening, the lids are straight cuts, and its breath is a little stronger than the sprite's (2px against 1.44px at 150px).

## `lumi-walk/` — the walk test: a rig walking a painted room, bets 2, 4 and 5

Lumi walking Home's painting, rigged in code from two drawings. Built 2026-09-13 after Chanté approved the pieces; published as the artifact [Lumi walks Home](https://claude.ai/code/artifact/ca116dea-7428-46c9-b11a-8f8275a715ee).

- **The drawings:** ten, named by the way she faces on the page — `art/lumi/lumi-iso-front.png` (sw, toward the lower left), `lumi-iso-back.png` (nw, away to the upper left), `lumi-iso-s.png` (toward you), `lumi-iso-w.png` (side-on, left) and `lumi-iso-n.png` (straight away), the four turning in-betweens (`-ssw`, `-wsw`, `-wnw`, `-nnw`), and `lumi-iso-wbw.png`, the seam drawing at 101.25° — side-on turned a touch away, the bow a sliver, no eye — generated from the hub and the drawings before them (`art/prompts/lumi-iso-*.md`). Every one strictly between facing you and facing away is mirrored: eighteen ring positions from ten drawings.
- **The ring is angles, not even steps.** `facings.py` writes each drawing's angle into `rig-walk.json` (`deg`); the page builds the ring from those (sorted, mirrors at 360° − deg), holds her facing as degrees, interpolates standing and rounding turns in degrees, and morphs between whichever two entries her facing lies between — so across the seam the pairs are w ↔ wbw (edges only: a face against a sun) and wbw ↔ wnw (on the sun). She still walks only the eight multiples of 45°; a turn's pace is still counted in 22.5° steps, so the extra drawing doesn't slow a turn; the hem's swing per drawing crossed scales with the angle crossed.
- `split.py` — cuts each into a body (hood, face, cloak, the coat's slit, hands) and two boots, finds the eyes for blinks and writes `rig-walk.json`. The legs are not pieces: each is a hip under the cloak, an ankle at its boot's cuff, a width and a colour, drawn by the page from one to the other every frame. The hem is bridged across the coat's slit by a closing (a running maximum pulled one panel's hem over the far boot). `keep` boxes hold a hand that touches a leg on the body. `debug-split.png` shows each facing whole, tinted by piece (legs green), and pulled apart with the legs following.
- **The room is Home's layers** (`art/scenery/home/`, from `home-layers/`): `layers.json` (the walkable floor, each layer's footprint, extent and proposed anchors, the depth rule's constants), `plate.png`, and each `layers/<id>.png` with its `-shadow`. The page draws the plate with every shadow, her contact shadow, then the layers and her back to front by the home-layers depth rule, ported verbatim (`depth.py` stays the reference; `halfWidth` and `reach` scale with her height, so at 120 the rule is exactly depth.py's). Her feet may stand inside the floor and outside every footprint and blocker grown by her clearance: `walkable.minus` as written at 120, grown by a port of `depth.py`'s `grow` at other heights.
- `tour-home.json` — the tour: stops named by a proposed anchor where one fits, or painting px, each with a rest and a reason. `build.py` takes each anchor's point from `layers.json` and fails if a stop is off the walkable floor. The loop (~83 s) rests at `armchair.stand`, `desk.stand` and `table.reach`, and rounds stops in front of the chair, at `balustrade.lean`, in front of both cushions, right of the green one, and close behind the table's back-right edge, back corner and back-left edge (8–10px outside the grown extent). Replaced the hand-traced `room-home.json` (cut-outs and a bounding-box test), 2026-09-13.
- `index.src.html` — the page: the rig warps the body row by row in software (bob, squash, sway, lean, the hood's and hem's lag; seamless, and the same in headless Chrome), steps the feet with each boot planted while it carries her and draws the legs to them, and finds paths on a grid of isometric ground that moves only in the eight directions she has drawings for (with a cost per turn, so it turns rarely). At a corner of an eighth of a turn she changes drawing on the move; at a wider one she stops and turns through the drawings between, 0.09 s each, each change carried by a small squash. Click the floor to send her; pace, height, a follow camera and the floor plan for judging (the walkable floor, the grown polygons, footprints, extents, blockers, anchors, her feet columns, and the red face and white walking arrows). `build.py` inlines Home's plate, layers and shadows, `layers.json`, the tour, the pieces and JSON into `index.html`, plus each layer's floor-coloured pixels from `home-layers/fringe.py` for the occlusion count. The plate and shadows are composited once, and every layer scaled once per device scale; a close follow camera scales a window of the floor round the view instead of the whole painting. **Her facing is a float position on the ring (`S.p`), not a drawing:** a turn slides it, `S.dirIdx` is only the drawing nearest to it (which is what the paths, the audit, the floor plan and the readout still read), and between two drawings the page morphs — each drawing warped row by row through `morph.py`'s knots onto the lerp of the two, its legs, boots and ground contacts put on the lerp too, then the nearer of them drawn solid with the other over it at its share. **Turn: Morph** (the default) or **Swap** (the old hard change, for comparison).
- `capture.py` — renders frames headless (`?grid=` steps the tour at a fixed 1/60 s and lays follow-camera cells out in one screenshot) into `out/<name>-grid.png`, a GIF and a strip; `--page` screenshots the whole page; `--audit` runs the page's `?audit=1` mode (the tour and 60 seeded random clicks) and prints how far she walked facing more than 25° off her movement, the worst angle and where, turns by kind, and how far corners were jumped. Motion checked without a browser tab, and by numbers where eyes can't tell. `--trace start,step,count` prints her position, drawing, face and heading at each sample, for when a frame grid is ambiguous. `--occlusion` runs `?occlusion=1`: the audit's walk (the tour's legs all checked for a path, the tour, the 60 clicks), and every fourth frame (second in a morph) her drawing rendered alone and the layers drawn after her rendered into another buffer at room scale, counting `behindPx` (her pixels covered by a layer the rule puts behind her, must be 0), `fringePx` (floor-coloured layer pixels over her, must be 0), `outOfReachPx` (overlap with a layer beyond her reach, whose order is arbitrary), `columnMissPx`/`columnOverPx` (per page column, a layer whose ground there is wholly in front of her drawn under her, or wholly behind her drawn over her), with the worst frame of each, plus `growCheck` and `renderMs`.
- `facings.py` — marks where she faces in each drawing (the face; for a back view the turn is judged by eye and recorded in `JUDGED`, wbw as 78.75° from facing away), prints the turn against what its angle on the ring needs, writes each drawing's ring angle (`deg`) and its turn into `rig-walk.json` for the page, and draws `out/facings.png`: the eight directions in a ring, each with its walking arrow and its face marked. `--candidates <dir>` measures generated candidates before one is chosen (gate 38–52° for a diagonal). Run after `split.py`, before `build.py`.
- `morph.py` — the correspondence a turn morphs through, so the page can hold *the same thing* in two neighbouring drawings. For every row of each drawn facing, from the hood top to the feet line, three x knots: the silhouette's left edge, one feature inside her, the right edge. The feature is the dark void of the face over the hood rows (a view with a face) or the big sun embroidered on it (a view from behind), and the bow and medallions below the hood where a drawing shows her front; rows without one interpolate from the rows that have one and then relax to the silhouette's midpoint, and each column is smoothed vertically so no row disagrees with the row above it. Each facing is marked `front` or `back`, and the page refuses to pair a face with a sun — between side-on and the first view from behind the morph falls back to the two edges alone. Written to `morph.json`; `out/morph-knots.png` draws the three curves over each drawing. Mirrored facings need no data (the page negates x and swaps left and right). Run after `split.py`, before `build.py`.

- **The idle** (folded onto the rig from `lumi-pieces/`): the eyes are drawn in code over the painted ones on every drawing with a face (s, ssw, sw, wsw, w and their mirrors). `split.py` measures what that needs — the face's outline to clip to, its width at the eye rows, the painted eye's colour at the pieces' radii (they agree with the pieces' constants to a point or two; the halo's falloff is the pieces') — and the page covers the painted eyes and halo with the face's own black (feathered, clipped to the face), then draws each eye back: blinks and sleepy under a lid whose edge bows as it closes (the pieces' lid was a straight cut), glances shift the eyes a tenth of the face's width and carry the hood half a pixel, curious widens them, happy is the pieces' ^ ^. A tilt shears the hood from nothing at the neck to ~2 px at the hood top through the head-weight rows, so the collar bends; sleepy and curious lower and lift the hood; standing she sways ±0.6 px at the hood over 4.2 s. In a morph an eye both drawings have is interpolated like the legs; the far eye as she turns away, and the last one going past side-on, fades with its own drawing. Views from behind: tilt and sway only. Scheduler (motion-and-interaction §12–14, on the sim clock so grids see it): blinks every 2.5–6.5 s (one in five a double), a glance or tilt every 8–20 s (glances six in seven), curious or sleepy every 40–90 s, one pass and a rest after; glances while walking, the rest only standing (one under way when she sets off is let go). Page controls: a cue row (Blink · Glance · Tilt · Happy · Curious · Sleepy) and **Auto idle**. Reduced motion: a cue jumps to its pose and back, nothing is scheduled. **Not on the rig:** the pieces' hands-together loop — it is a sheet drawn at eye level, not at the room's angle, so it needs its own drawing first.
- **Testing params:** `?cue=name@seconds,…` fires idle cues at sim times, `?idle=0` schedules none, `?tour=0` keeps her standing, `?audit=1&around=t0,t1` adds every frame in a window to the audit (for a worst case among the random clicks, which `?trace=` cannot reach); `capture.py --query '…'` passes them through, and `--trace` rows now include the next path points.

Rebuild, from the repo root: `python3 art/prototypes/lumi-walk/split.py && python3 art/prototypes/lumi-walk/facings.py && python3 art/prototypes/lumi-walk/morph.py && python3 art/prototypes/lumi-walk/elevation.py && python3 art/prototypes/lumi-walk/build.py`, then `python3 art/prototypes/lumi-walk/capture.py --plan --zoom 1.6 --cell 480x320 --step 2.5 --count 20 --cols 5 --start 0 --name tour`, `capture.py --audit` and `capture.py --occlusion`. `build.py` reads `art/scenery/home/` and imports `home-layers/depth.py` and `fringe.py`; `facings.py` and `elevation.py` read the iso slope from `layers.json`. `elevation.py` is the camera-angle gate (room 34.8°, s 29.1°, n 33.3°); it only prints, and belongs in the chain so every rebuild re-checks it. `capture.py --turn swap` renders the same frames with the old behaviour, for comparison.

**Status:** landed and pushed (2026-09-13, `5433a9f`) after eight rounds of Chanté's review, the last with the rig on Home's layers: "This is looking like a really good start." The eighth look below is the current state; the limits in this paragraph are from the first build. Next: Lumi picks up the lantern and sets it down on the low table (`docs/animation-pipeline.md` → Next animation). At the first publish, what the renders showed: boots step and hold, the tabletop, lantern and plant cover her behind the table and she covers the cushions in front; a blink first left the painted eyes' rims as orange rings (the lid now takes the rim and glow). Known limits: a turn is a dip and a swap, not an in-between; one room, one tour; nothing in the app plays it.

**Chanté's first look (2026-09-13):** "This is looking great! She's clipping the table, so we'll need to work on our mapping. But she looks awesome!" The rig passed; the map didn't. Two mistakes, both fixed in `room-home.json` and the page: the table's footprint was traced from its legs' feet, but the top overhangs them by ~15px (it is now the tabletop's outline dropped by its 65px height); and the clearance kept her *feet* 11px off a footprint while her cloak reaches ~25px either side (now 24px). Still true after the fix: close behind a low table the tabletop covers most of her, because at this painting's angle a 65px table hides a lot of floor — hand-traced polygons and a bounding-box depth test are the weak part, not the drawings.

**Chanté's second look (2026-09-13):** "When she turns, she's walking sideways like a crab · The turning is abrupt, it's a 'flash! now she's facing this way' · As she's walking her legs sometimes disappear, so her feet are walking nicely, but they appear to be floating beneath her. Same with her body beneath the slit of her coat." Three causes, three fixes: (1) she walked at any angle with four diagonal drawings, and at a corner she arrived still moving at 14% pace, so the stop-then-turn check never fired and she walked the next leg in the old drawing — now paths move only in eight drawn directions and she stops fully before any wider turn; (2) a turn was one swap — now three more drawings (s, w, n) give every turn drawings to pass through; (3) each foot piece carried the painted leg above its boot and the dark of the coat's slit, so a step pulled them away — now the feet are boots alone, the slit stays on the body, and the legs are drawn. Also found on the way: `build.py` inlined only the old two facings, so the first grid was black.

**Chanté's third look (2026-09-13):** "She's still not always walking forward · When she does a walking turn it's good. But when she's standing still she flips instantly · Her feet are a little better, but still clip a bit" (with a screenshot: facing the viewer, one boot gone under the cloak). Causes and fixes: (1) the short stretches joining her real start and end to the path grid ran at any angle — now joined by moves in the eight directions, and any stretch under 6px is dropped; (2) a turn from standing was one or two swaps 0.09s apart with nothing moving — now each eighth of a turn is a small step on the spot, 0.2s, the drawing changing as the boot lifts, her weight shifting; (3) pace and stride were set in page pixels, so up and down the page (where the ground is foreshortened) she over-strode and the back boot slid under the hem — now both are set on the ground, and a boot may tuck up toward the hem but never under it. **Checked by numbers:** `capture.py --audit` walks the tour for two minutes and 60 random clicks and measures facing against movement every frame — before the last fix 36px of 30,600 walked sideways (worst 180°, a 5px stretch walked backwards) and 678px of corner jumps; after, 0px sideways (worst 24.6°), no turn from standing without a step (173 stepped turns, 105 on the move), 38px of jumps in all.

**Chanté's fourth look (2026-09-13):** "The biggest issue is she's still not always walking forward. I feel like it should be a relatively easy task of marking where her face is and always making sure it's facing in the direction she's walking." She was right, and the audit had missed it: it proved she moved along the direction each drawing was *assigned*, never that the drawing faced that way. `facings.py` marks the face in each drawing: the two diagonal drawings were turned only ~29° (front) and well under 45° (back) where the room's diagonals need 45°, and those diagonals are the room's main walking lines. Both were redrawn (prompt v2: "exactly halfway between" the attached facing-you, or facing-away, and side-on drawings). The front's four candidates all measured 41–46° (v1's three: 26–28°); the back has no face, and neither the back's sun nor the line between the boots could be trusted, so it was chosen by eye in a strip between facing away and side-on, and its 45° is recorded as judged. The page now reads each drawing's measured turn: the audit judges her against where the face really points (per-direction error 0–5°), and the floor plan draws a red face arrow and a white walking arrow at her feet.

**Chanté's fifth look (2026-09-13):** "That's looking better. Turning could still be smoother though." Every change of drawing was a 45° jump. Now **sixteen facings, 22.5° apart**: four in-between drawings (`lumi-iso-ssw`, `-wsw`, `-wnw`, `-nnw`) join the five, and with mirroring she has a drawing every sixteenth of a turn; she still walks only the eight even ones. In code: corners of a quarter turn or less are rounded (a short curve she walks, changing drawing on the move); sharper turns stop, the hood leans toward the new way first, and she steps round with the first and last changes eased longer; the hem swings out behind each change on a spring; and a **Turn: Swap / Blend** toggle fades the new drawing in over 70 ms for comparison. Tour stops with no rest join the next leg so she rounds them. The in-betweens: "exactly halfway between" overshot for the 22.5° drawing (eight candidates at 32–39°), so v3 asked for an edit of the facing-you drawing and landed 12–17°; the 67.5° front one passed at 62.6°; the two from behind were judged by eye. Audit after: 0px sideways (worst 17.7°), no turns from standing without a step, every facing within 5° of its measured face.

**Chanté's sixth look (2026-09-13):** "Swap appears slightly better, but it's still pretty choppy." Sixteen drawings had made the steps smaller without making them steps at all: neighbouring drawings differ in more than angle — the hood is a different width, the cloak falls differently, the face sits somewhere else — so every change was still a jump, and the crossfade showed both at once. Now a turn is **a morph, not a change of drawing**. `morph.py` measures, per row, what in one drawing is the same thing as what in the next — the two silhouette edges and one feature between them (the face; from behind, the sun on her hood) — and the page's warp gained a piecewise-linear x-remap through those three knots, on top of the vertical remap, sway, breath, hood lean and hem swing it already did. Her facing became a float on the ring: a standing turn slides it the whole way on one ease at the old pace (the sixteenths still cost `turnStep` each, the first and last longer), a corner is rounded by sliding it over ~0.15 s, and the squash that used to hide each swap is gone because there is no longer a pop to hide. Two things the morph forced, both improvements: a turn that takes time has to *start before the corner*, so a rounded corner now begins half a turn-step early and she walks it as an arc halfway between her facing and the path (before, the facing snapped exactly when the heading did, which is why the old audit read 0 px sideways); and the stepping feet are now indexed by which foot is on the *screen* left rather than which is further away, because four of the sixteen pairs of neighbours order their two legs the other way round and a morph would have pulled leg onto leg. **Checked in the frames** (`capture.py --start 3.0 --step 0.033 --count 32 --cols 8 --zoom 8 --cell 300x300`, and `--turn swap` for the same window): the hood rotates, the face narrows and slides off to the side, the medallions turn — one figure rotating, where Swap holds a pose and then jumps. What is still not perfect: where the two drawings genuinely hold different things the crossfade still shows both for ~0.1 s — the bow and medallions ghost through as she passes side-on into the first view from behind, which is also the one pair whose feature knot has to be dropped (a face and a sun are opposite sides of her) — and mid-morph the two silhouettes miss each other by a pixel or two at the outline, a faint doubled contour at 14× zoom that is invisible at her real height. **By numbers:** 0 px of 30,748 walked sideways (worst 22.9°), no turn from standing without a step, 350 stepped turns and 249 on the move.

**Chanté's seventh look (2026-09-13):** she ranked what the walk leaves open — the drawings' angle, then the side-on ↔ first-back-view seam ("Do the extra drawing"), then the pieces' idle on the rig ("fold them in sounds right"). The angle was measured, not redrawn: `elevation.py` reads the camera's height from the hem's squash (room 35°, drawings 29–33°), close enough to keep, and is now a gate in the rebuild chain. The seam: `lumi-iso-wbw` at 101.25°, one run of four asked as an edit of side-on (the Prompt lab finding), all four showing the bow as a sliver; chosen by eye in a strip between w and wnw for the one whose hood still shows a crescent of the black inside, narrower than side-on and wider than wnw. Placing it forced the ring onto angles (above). The idle: the pieces' eyes, glances, tilt and expressions, drawn on five drawings and their mirrors (above). And a fix on the way: at ~8.5 s in the tour she turned nw → nnw and straight back — not the curve itself but a 3 px jog where two tour legs joined, rounded into a turn out and back. Jogs within 4 px of the line past them are now straightened before corners are rounded; a round under way is never reversed; a facing turned early for a corner is held until she reaches it; and on an arc she passes a corner's points rather than homing in on them (homing in, the last half pixel swung 39° off her face). **Checked in the frames:** the seam grid (`--start 3.0 --step 0.033 --count 32 --zoom 8`) shows the bow whole on side-on, a sliver as she passes wbw, gone on wnw — a shrink, not a dissolve, though at the standing turn's pace it is done in two or three frames; the corner grid at 8–9 s is one straight stretch with face and walking arrows together; idle grids with forced cues (`--query 'cue=…&idle=0&tour=0' --zoom 9`) show the drawn eyes matching the painted ones at rest (no rings), a blink's lid, curious, happy's ^ ^, sleepy's lowered lids, a tilt with the collar whole. **By numbers:** 0 px of 30,738 walked sideways (worst 21.9°), no standing flips, 384 stepped turns and 244 on the move, 91 px of corner snaps. **Still off:** sleepy's lid turns the narrow near eye of the diagonal into a small wedge; a glance is a pixel at her real height and may be too subtle to read; the idle while walking was not caught in a grid with eyes showing; sway is by construction, not seen in a still.

**Chanté's eighth look (2026-09-13):** after the layers spike she approved the next step, the walk rig on Home's layers, robust occlusion around furniture being what she cares about. What changed: the map is the layers (above); the hand-traced cut-outs, `OCC` and the bounding-box test are gone; the paths' `standable` is the layers' walkable floor, with the eight-direction grid, turn cost, octile joins, rounded corners and jog removal kept; the tour moved to `tour-home.json`. **Reach, found by the count:** the first `--occlusion` run had `behindPx` 0 and `fringePx` 0 but `outOfReachPx` 170 in 8 frames (worst 54px, the stool beside her side-on hood at 911, 549, in the random clicks): the rig's side-on drawings reach 43.4px from her feet at rest, and `reach` was 37, set for the stand-in's 36.5. It is 50 now in `layers.json` and `depth.py` (home-layers' `--test`: 2,872 depth cases, 0 disagreements); `halfWidth` 17 stays, since no column disagreed. **By numbers:** audit 0 px of 30,710 walked sideways (worst 24.1°), no standing flips, 368 stepped turns and 245 on the move, 90.2px of corner snaps, 0 unreachable (11 tour legs, 60 clicks); occlusion over 16,335 sampled frames `behindPx` 0, `fringePx` 0, `outOfReachPx` 0, `columnMissPx` 0, `columnOverPx` 0; the grow port matches `walkable.minus` exactly (20 polygons). `render()` in headless Chrome's software canvas at a 2332px stage: 3.9ms a frame for the whole room, 10.8ms following her (63ms before the floor window). **Checked in the frames:** the tour with the plan at 1.8×; close-follow grids at 7× behind the table's back-right edge, back corner and back-left edge, down its left side, by the desk and chair, along the balustrade, in front of both cushions and by the armchair; 0.1s grids at 9× where her feet cross the green cushion's and the table's depth lines. Behind the table the top covers her lower half and never her hood; in front of the cushions, the chair and the armchair nothing of them covers her; at the floor's edge the balustrade's post and rail cross her lower body; no pop where a layer changes side; her shadow stays under every piece; no path through furniture. **Still off:** at 7×, single white pixels of her hem show at the tabletop's back edge and between the table plant's leaves as she passes behind (home-layers known issues 1 and 2 — her own pixels through the layer's edge, not floor, so the fringe count cannot see them); the morph's ~0.1s doubled contour, unchanged; `fringePx` 0 uses the same classifier the trim used, so it checks the composite, not the matte; the tour never puts the chair or the stool over her (the floor there is narrow); the count is at room scale, not device pixels.

## `home-layers/` — Home as an empty plate plus furniture layers, bet 5

Home's painting taken apart instead of mapped by hand. Chanté, asked whether to refine the map or rebuild the rooms from layered items, chose to **cut the furniture out of the existing painting and inpaint the floor behind it**. Built 2026-09-13 on `ux/home-layers`; not published, nothing served. **It now feeds the walk page** (`lumi-walk/`, 2026-09-13): `art/scenery/home/layers.json`, the plate and the layers are that page's room, and its JS depth rule is ported from this viewer, so `depth.py` stays the reference for both. Moving the rig on widened `reach` from 37 to 50 (below).

- `room.src.json` — the hand-traced source, in painting px: the **inventory** (every floor-standing thing on or beside the floor, kept or left in the plate, with the reason), and per layer its `body` polygons (hugging the silhouette ~2px outside; several for thin things like the chair and balustrade), `foliage` polygons (leaves, fringes), the `mask` sent to the API, `footprint` (where it meets the floor), `extent` (the widest part dropped to the floor), `height` and proposed `anchors`; the `blockers` left in the plate; the `floor`; the chosen inpaint candidate per cluster. Traced on grid zooms from `zoom.py`, checked on `masks.py`'s overlay (`out/masks.png`, `--zoom x0,y0,x1,y1,k`).
- `inpaint.py <A|B|C|D>` — one cluster (the table and cushions; armchair and stool; desk and chair; balustrade) through OpenAI's `/v1/images/edits` with a mask, `gpt-image-2.5-sunburst`, 1536×1024, high; keeps the hole's neighbourhood of each result in `out/inpaint/` and logs tokens and cost. The rug needed a second prompt: the first invented a central medallion.
- `plate.py` — writes `art/scenery/home/plate.png`, `layers/<id>.png` and `<id>-shadow.png`, and `layers.json`, and prints the recompose error. The inpaint is used only under what a layer covers, colour-corrected to its surroundings. The floor an item only shades keeps the painting's own pixels, relit (the shadow is the luminance ratio against the inpaint, smoothed); that darkening becomes the black `-shadow` layer. A layer is solid inside its polygons; its edge is a colour-line matte between the item's own colour and the floor just outside. Leaves are a difference matte against the inpaint. Colour is de-matted against the plate. The docstring has the details.
- `depth.py` — the depth rule, clearance and the walkable floor, with self-tests. In one page column two ground points differ only in u + v, so **an item draws over Lumi when its ground polygon has a point in her feet columns (x ± 17) lower on the page than her feet**. An item with ground only within her drawing's reach (x ± 50; 37 until the walk rig, whose side-on drawings reach 43.4px at rest, came onto the layers) is tested at its column nearest her. "Lower" means by more than a 4px margin. Layers and Lumi are ordered by a topological sort. Walkable = `floor` minus every extent grown by her clearance as a ground circle (an ellipse 24 × 13.7 px).
- `cut_lumi.py` — a stand-in Lumi, `art/lumi/lumi-iso-s.png` matted with `scripts/lumi_cut.py`, at 2× her 120px.
- `index.src.html` + `build.py` → `index.html` — the viewer, in the walk page's style. It shows the plate, shadows and layers with Lumi depth-sorted; click or drag to place her. Views: Composed · Plate only · Layers tinted · Original painting (a cut you slide between the painting and the rebuilt room), plus Footprints and anchors. `build.py` makes the test points: behind, in front of, left and right of each layer, a row close behind the low table, and behind the balustrade posts. It also prints any layer whose pixels reach past its ground polygon.
- `capture.py` — headless Chrome: the default `?grid=` mode puts Lumi at every test point in one screenshot (`out/grid.png`; `--view tinted`), `--test` checks the JS rule against `depth.py`, `--page` screenshots the page.

Rebuild, from the repo root, from the committed plate and layers: `python3 art/prototypes/home-layers/cut_lumi.py && python3 art/prototypes/home-layers/build.py`, then `python3 art/prototypes/home-layers/capture.py --test`, `capture.py`, `capture.py --view tinted --name grid-tinted`, `capture.py --page`. To rebuild the plate itself, first generate candidates with `inpaint.py <cluster> --n 2` (about $0.11 a run; `.env.local`'s key), name the chosen ones in `room.src.json` → `plate_candidates`, then run `plate.py` before the above. The candidates are gitignored. To check fringes: `sweep.py --tag after`, then `build.py` and `capture.py --set <piece> --cols 10 --cell 240x220 --zoom 1.8 --name sweep-<piece>`. `sweep.py --ref <commit> --margin 0` counts an older commit's layers under the old rule.

**Status:** approved and pushed (2026-09-13). Chanté on the viewer: "It looks good to me", with the fringes that could clip Lumi trimmed. Since `5433a9f` it feeds the walk page (`lumi-walk/`, eighth look), which composites these layers around the walking rig. Next, a first object as its own layer: the lantern is painted into the `table` layer today, together with the books, plant and mug, so picking it up means cutting it out and filling in the tabletop under it. **Recompose** (plate × shadows, then layers, from the quantised files, against the painting; per channel, 0–255), after the fringe cleanup:
- whole painting: mean 0.031, max 117 (before the cleanup: 0.030, 121);
- inside the holes: mean 0.13, with 0.51% of pixels off by more than 8 (before: 0.52%);
- where the trim exposed the plate (32,979 px): mean 0.22, max 70, 56 px off by more than 8 — thin one-pixel lines on the chair's right post, the window sill and a balustrade rail, invisible composed at 3× (`out/exposed-spots.png`);
- per mask (mean / max / share off by more than 8): table 0.10 / 109 / 0.31%, cream cushion 0.09 / 70 / 0.22%, green cushion 0.06 / 29 / 0.11%, armchair 0.05 / 36 / 0.06%, stool 0.08 / 20 / 0.06%, desk 0.09 / 78 / 0.22%, chair 0.15 / 70 / 0.80%, balustrade 0.19 / 117 / 0.92%.

These numbers are small largely by construction (de-matting is exact wherever a layer has alpha); they catch overlaps, clipping and lost pixels, not ugly seams. The JS rule agrees with `depth.py` on 2,728 depth cases and 722 walkable points (2,872 depth cases and 722 walkable points, 0 disagreements, after `reach` became 50; the sweeps still count 0 fringe px over her on every piece).

What the grids show:
- **Order is right.** In front of every layer nothing covers her. Behind the table the top covers her lower half, including close behind its back edge where the hand-traced map failed. Behind the balustrade posts the post tops cross her feet. The desk no longer cuts her once its top's overhang has an extent.

**Fringe cleanup (2026-09-13, after Chanté: "I can see the fringes in the plate only mode and I wonder if those areas would clip her").** Plate-only seams can't clip her (the plate is always under her); floor a *layer* carries can, because layers draw over her. Four new scripts:
- `fringe.py` measures that floor. A layer pixel is floor-coloured when it matches a nearby floor pixel of the painting (within 16 RGB, and nearer than to the piece's own colours), or is a nearby floor colour in shade. `--show` draws it in cyan, and its attribution says which step of the matte kept each pixel.
- `sweep.py` walks her in 8px steps behind and beside every piece and counts her pixels covered by floor-coloured layer pixels.
- `look.py` cuts 4–5× close-ups of the painting, the room composed with her, and the layers alone.
- `capture.py --set <piece>` renders a sweep as a grid.

*The cause:* the matte kept floor where the inpaint disagreed with the painting near an edge (62–97% of the floor-coloured pixels differed from the inpaint by more than 40). The colour-line edge kept most of them on solid pieces (55–79%), and the leaf matte kept most on the desk's and balustrade's ivy (55–59%). The rest sat inside body polygons traced too generously (9–23%): the chair's right post took in floor, and the first rail polygon cut across floor.

*The fix* (`plate.py` → the trim):
- A layer loses its pixels beyond its own polygons, and anything below its ground outline except solid parts and leaves.
- It loses floor-coloured pixels joined to empty space or within 4px of it, floor between leaves, and leftover bits under 10px.
- The polygons were re-traced for the chair's right post and the balustrade's left rail and spindles.
- Two trim passes run, then the edges are hardened (one-pixel holes filled and partial alpha raised inside the polygons, so her cloak doesn't speckle through), then a last trim.
- Where the trim exposed the plate, the plate becomes the painting's own floor relit by the shadow beside it, so the room still matches the painting.
- The depth rule gained a **margin**: a ground point counts as in front of her only when it is more than 4px lower than her feet, so a footprint corner level with her feet no longer covers her hem.

*Fringe px over her across the sweeps* (committed layers with the old rule → now):

| Piece | Before | Now |
|---|---|---|
| table (98 frames) | 11,806 | 0 |
| desk (36) | 2,291 | 0 |
| balustrade (80) | 1,719 | 0 |
| green cushion (21) | 1,488 | 0 |
| armchair (29) | 1,124 | 0 |
| cream cushion (10) | 0 | 0 |
| stool (19) | 0 | 0 |
| chair (17) | 0 | 0 |

The zeros are not independent evidence: the trim uses the same classifier the count does. The sweep grids (`out/sweep-<piece>.png`) were read by eye, and no rug or plank patch is left over her.

**Known issues:**
- (1) Where she stands behind the table's plant, the desk's plant or the ivy, a few single pixels of her white cloak show between leaf edges. That is her own figure through gaps in the leaves, not floor, but it reads as speckle at 4×. It is barely visible at room scale.
- (2) The tabletop's back edge still lets one-pixel specks of her cloak through in some sweep frames (table 3–8, 29–31, 51–53).
- (3) The chair sweep never puts the chair over her: the floor by the desk is narrow. The chair's cleanup was judged in the tinted layer and close-ups, not by the count.
- (4) **Plate only** has seams hugging every silhouette: the rug's flowers break along the table's outline, and dark ghosts of its front legs remain. Plank lines and the window's light patches don't line up under the chair and by the armchair. Under the balustrade the plate is a patchwork of floor and stair. None of this shows with the layers on.
- (5) The stair, the right-hand bookcase and the log basket stay in the plate as blockers.
- (6) The floor's front edge by the balustrade is a guess (the rails run down a stair, so floor level there is ambiguous).
- (7) Anchors are proposals.
- (8) API: 10 images logged at about $0.54, plus two requests that hung and were killed (possibly billed, about $0.22 more). The cleanup used no API calls.

## `shadow-mock.py` — Lumi's shadows on the room paintings

The before and after behind *Lumi's shadow is drawn by the page* (`docs/decisions.md`, 2026-09-13): the companion's CSS contact shadow, cast shadow and room light reproduced in numpy on the Today and Library paintings at her spots, beside the baked cream shadow they replaced. Reads the current `public/lumi-free.webp`; the *before* side needs a sheet with the baked shadow in `out/lumi-free-before.webp` (`git show 129c4fe:public/lumi-free.webp > art/prototypes/out/lumi-free-before.webp`). Writes `out/shadow-<room>.png`. Its numbers copy the room variables in `globals.css` by hand — change both.
