# Development hygiene — an evolving strategy

*Claude's working doc, started 2026-09-13. Chanté's ask: catch problems like a stale `.next` or packages missing from `node_modules` earlier, in a doc that evolves to keep development as tidy as possible, developed by Claude's judgement and taken to her only when it has to be. Claude owns it: read it at session start, update it at session end. Branch rules, ports and the docs audit live in [`branching.md`](branching.md); this doc covers what surrounds the code — installs, generated files, env, servers, worktrees, hooks — and how its traps get caught.*

## The strategy
1. **Fail in the first second, with the fix in the message.** A trap that surfaces three steps later as a confusing error (tsc's *Cannot find module*, a server that dies at startup, a 500 at the first request) costs a turn to diagnose. A guard that runs first and says `npm ci` costs nothing. Guards live in `scripts/preflight.mjs` and `.githooks/`.
2. **Every snag becomes a row; a cheap catch becomes a guard now.** Anything that cost a turn goes in *Traps* in the same session: symptom, cause, how it's caught, fix. If the catch is a few lines of script, write it then rather than waiting for the second time.
3. **Generated state is disposable; nothing else is.** `.next/`, `node_modules/` and `tsconfig.tsbuildinfo` can be deleted and rebuilt without asking, once no running server uses them. Anything tracked, anything hand-made but untracked (`.env.local`, drafts in `art/`), and anything another session owns (branches, stashes, worktrees, servers) is never cleaned up on a guess.
4. **A merge brings environment, not just code.** Merging or pulling `main` can add a package, rename a route or add an env key. After any merge, run the preflight (`npm run check` does) before believing an error.
5. **Leave every checkout as you found it, or tidier.** Servers you started stopped, merged branches deleted, your worktree committed or clean, nothing stashed, nothing parked on `main`.

## Session start (≤ 1 minute)
1. `git branch --show-current` — is it yours? (`branching.md` → Parallel sessions.) Empty in the shared checkout means it's parked: `git switch --detach main`, then branch. In a worktree, rename a `worktree-*` or `claude/*` branch to `type/slug` before the first commit.
2. `npm run preflight` — or let `npm run check` / `npm run dev` run it. Do what it says before anything else. In a fresh worktree it also sets the checkout up: a cloned `node_modules` and a copy of `.env.local`, in about a second.
3. Merged `origin/main` or pulled mid-session? Preflight again.
4. Skim *Traps* for the area you're about to touch.

## Session end (≤ 3 minutes, before the last commit)
1. Stop every server you started, and only those. Any left running for review: say which port serves which branch, as a clickable link — in every reply while it runs, not only the one that started it.
2. Merged branches deleted; nothing of yours in `git stash list`; no checkout of yours on `main`; your worktree removed once its branch landed and it's safe, without asking (standing in it: detach, delete the branch with `git merge-base --is-ancestor <branch> main && git branch -D <branch>`, then ExitWorktree `remove` if EnterWorktree made it; one the desktop app made stays detached and clean for the census); if it isn't safe, name it and why in the summary (`branching.md` → Parallel sessions 9).
3. `npm run worktrees -- --prune` — clears every worktree that's safe to remove, whoever made it; anything kept for a reason only its owner can settle (uncommitted work, unmerged commits, a differing `.env.local`) is named to Chanté.
4. Anything that cost a turn → a *Traps* row. Land one *Backlog* item, or re-rank it if something moved.
5. Fix what this doc got wrong; add a *Change log* line.

## Guards in place
| Guard | Catches | Runs | Where |
|---|---|---|---|
| Worktree census | worktrees piling up: each one listed with why it stays (the shared checkout, this checkout, another tool's, locked, in use, active in the last hour, uncommitted changes, commits not on `main`, ignored files worth keeping) or that it can go; `--prune` removes only those, and deletes a branch only when its tip is on `main` (checked against `main`, then `-D`; `-d` judged against the checkout's own HEAD) | at session end; by hand as `npm run worktrees` | `scripts/worktrees.mjs`, `branching.md` → Parallel sessions 9–10 |
| Preflight | `node_modules` missing (clones the install of another checkout with a byte-identical `package-lock.json` whose install passes the same drift check — copy-on-write, ~1 s, no extra disk — or says `npm ci` when none matches), a symlink, or behind `package-lock.json`: direct dependencies from their installed `package.json`, every nested and transitive entry from npm's hidden lockfile (`node_modules/.package-lock.json`) — a version mismatch or a missing entry the lockfile doesn't mark optional (another platform's binary may be absent), naming five and counting the rest · generated route types pointing at routes that no longer exist (removes them; Next writes them again) · `.env.local` missing (copies the shared checkout's), or missing a key `.env.example` requires (key names only, never values) · a local Node major other than `.nvmrc`'s (a note) | before `npm run check` and `npm run dev` (npm's `precheck` / `predev`); by hand as `npm run preflight`. Env problems fail `dev` and are a note on `check`; skipped in CI | `scripts/preflight.mjs` |
| `core.hooksPath` guard | `core.hooksPath` absolute again (every worktree would run the named checkout's hooks, not its own branch's) — fails naming the fix for the scope the value came from (`git config --worktree core.hooksPath .githooks` when it is a worktree's own `config.worktree`, which overrides the shared value); unset is a note (the commit guards don't run) | inside the preflight; skipped in CI. It reads the value the checkout it runs in actually uses, shared or its own | `scripts/preflight.mjs` §4 |
| Server env check | a deploy or server missing a required key (Clerk's two, `DATABASE_URL`, Lumi's model key per `LUMI_MODEL`) or with a malformed `LUMI_MODEL` — names keys, never values | once per server start (`src/instrumentation.ts` → `register()`, Node.js runtime): throws with `NODE_ENV=production`, a `[env]` warning in dev (where the preflight has already failed `dev` on a missing key); not in `next build`, vitest or typegen | `src/lib/env.ts`, `src/lib/env.test.ts` |
| Shared script helpers | a script that finds the repo by `new URL(…).pathname` (a space in the path becomes `%20` and every path under it silently misses) or wraps `execFileSync` its own way | by convention: `ROOT`, `exec`, `attempt`, `QUIET` come from one module | `scripts/lib.mjs` (used by `preflight`, `land`, `worktrees`, `check-route-auth`, `check-css-prefixes`) |
| `npm run check` | types, lint incl. the `src/core` import guard, tests, the route-auth audit, the CSS prefix audit, Lumi's brief check | before every landing; CI on every push to `main` and every PR | `package.json`, `.github/workflows/ci.yml` |
| Route-auth audit | an exported HTTP method handler in any `src/app/**/route.{ts,tsx,js,mjs}` without `requireUser()` / `requireVisit()` / the cron secret in its own body (comments and string contents ignored, so a mention doesn't count, and one gated handler no longer covers the file) · a handler it can't see (`export { x as GET }`, `export *`) · a top-level `"use server"` file under `src/` · a missing `src/app` (fails; it used to pass as "no API routes") | inside `npm run check`; by hand as `npm run check:routes`, or `node scripts/check-route-auth.mjs <root>` against a fixture | `scripts/check-route-auth.mjs`, its parser tests in `scripts/check-route-auth.test.mjs` |
| Pool wire test | a pool option or driver upgrade that breaks the app's own client — transactions, nesting — which the PGlite test DB can't see (it has its own driver) | inside `npm run check` (vitest) | `src/db/client.db.test.ts`, `openTestServer` in `src/db/test-db.ts` |
| CSS prefix audit | a rule in the compiled stylesheet that kept `-webkit-<prop>` but lost `<prop>` (lightningcss drops the unprefixed one when the prefixed line follows it) | inside `npm run check`; by hand as `npm run check:css` | `scripts/check-css-prefixes.mjs` |
| Lumi's brief check | `src/core/ai/brief.ts` edited by hand, or not regenerated after its doc changed · a canon source of the brief changed since its last review (names each, and says how to review) · a review older than 30 days (a note only) | inside `npm run check`; by hand as `npm run check:brief` | `scripts/lumi-brief.mjs`, `docs/philosophy/lumi-brief.md` → Reviewing it |
| `npm run land` | a merge that would need `main` checked out; landing a branch that doesn't contain the latest `main` (conflicts on `main`); two sessions landing at once (compare-and-swap on the ref) | every landing, instead of `git checkout main && git merge` | `scripts/land.mjs`, `branching.md` → Landing on main |
| pre-commit hook | `.claude/` bookkeeping staged; direct commits to `main`; commits on the shared checkout's parked detached HEAD | every commit | `.githooks/pre-commit` |
| Port etiquette | killing or reusing someone else's server; two servers in one checkout | before starting any server (by hand) | `CLAUDE.md`, `branching.md` → Dev servers |

## Traps — symptom → cause → catch → fix
Newest first. *By hand* in the catch column marks a backlog candidate.

| First seen | Symptom | Cause | Caught by | Fix |
|---|---|---|---|---|
| 2026-09-13 | In Mobile Safari on the iOS Simulator a CSS fix looks like it failed: after hot reload, and after reopening the same address with `simctl openurl`, the page still misbehaves (Today's 2px sideways drag "survived" its correct fix twice and cost several turns) | Safari reused its cached stylesheet for the same address, so the computed style still held the old value | by hand: read the computed value in the page (the probe in the next row prints it) before concluding a fix didn't work | after a CSS change, reopen at a new address (`/today?v=2`, then `?v=3`…) |
| 2026-09-13 | A layout bug that only shows on a phone can't be measured from the session: the iOS Simulator tool's `inspect` is unavailable, Safari's Web Inspector is a GUI, Chrome's window won't narrow, and a 402px same-origin iframe in Chrome is refused | no DOM access from a Claude session to Safari in the Simulator; the security headers (`X-Frame-Options: DENY`, `frame-ancestors 'none'`) block every frame | by hand | measure in the page and read it in the dev server's log: Next forwards browser `console` output as `[browser]` lines, so a temporary client component mounted in the root layout that `console.warn`s on each navigation reports from real Safari (the mobile pass's probe logged each element with `scrollWidth > clientWidth`, the child whose `display: none` ends the overflow, and every tap target under 44pt). Keep it out of commits: delete the file and restore `layout.tsx` before `npm run check` |
| 2026-09-13 | On a phone under `npm run dev`, a tap on the bottom bar's Home tab opens Next's dev-tools menu instead | Next's dev indicator (the round N, bottom left) sits over the bar's first tab below 768px | by hand | open Home by address (`xcrun simctl openurl <udid> http://localhost:<port>/`); moving the indicator is `ux-review-log.md` → Review 1, finding 5 |
| 2026-09-13 | Safari on the iOS Simulator lands on sign-in, and Claude can't sign in (no creating accounts, no typing passwords or codes), so the signed-in pages can't be driven | a Clerk session is required, and the Simulator's Safari has none | the redirect to `/sign-in` | `COHERENCE_DEV_USER=1` in the worktree's `.env.local` under `npm run dev`: every request is the local test user (`src/lib/dev-user.ts`; its rows are real rows in the production database) |
| 2026-09-13 | In a Claude-in-Chrome click-through, `computer type` and `key Enter` right after a `navigate` do nothing: the composer's textarea keeps focus but its value stays empty and Enter doesn't send. It read as a Home send bug and cost several turns | the automation tab is hidden (`document.visibilityState === "hidden"`), and typed keys and Enter don't reach a hidden page, though focus does (the same tab as the hidden-tab rows below) | by hand: check `document.visibilityState`, and look in the dev server's log for the `POST /api/chat` before suspecting the app — no request means the input never arrived | set the value with the native setter and an `input` event (`Object.getOwnPropertyDescriptor(HTMLTextAreaElement.prototype, "value").set.call(el, text); el.dispatchEvent(new Event("input", { bubbles: true }))`, so React sees it), then click the send button from `javascript_tool` (`button.click()`); confirm with the server log |
| 2026-09-13 | Re-running a DB test file for a worktree with `npm exec --prefix <worktree> -- vitest run <file> --root <worktree>` fails every test in it with *column "consolidating_until" does not exist* (`library.db.test.ts`, 16 of 17), where `npm run check` in the same worktree had failed only a timeout | `npm exec` keeps the caller's cwd, and `openTestDb` finds its migrations from `process.cwd()` (`src/db/test-db.ts`), so the tests ran against the shared checkout's migrations, parked on an older commit, while `--root` pointed vitest at the worktree's code | by hand: the missing column is one `main`'s migrations add | run one file as `npm run test --prefix <worktree> -- <file>` (npm runs a script in the package's directory) or from inside the worktree; never `npm exec --prefix`/`--root` from another checkout for a DB test |
| 2026-09-13 | `npm run check` fails on one PGlite test — *Test timed out in 5000ms* (`library.db.test.ts` › *claims the watermark last…*, 5.98s once, 10.8s another time) — though the file passes alone (17/17 in 3s) | two sessions saw it the same day. That test opens and migrates its own PGlite inside the test body (`openTestDb`), on vitest's default 5s budget, while the files' shared databases open in `beforeAll` with 60s; machine load (a load average of ~24: many sessions' servers, builds and checks at once) or the full suite running 41 files at once pushes the migration past 5s | by hand: `uptime`, then the file alone (`npx vitest run <file>`) | fixed on `fix/empty-lumi-turn`: that test has the file's 60_000 budget. Any test that calls `openTestDb` itself gets the same. Before reading a DB-test timeout as a regression, check `uptime` and re-run the file alone; a failure alone, or an assertion error rather than a timeout, is real. **Recurred 2026-09-13 (`feat/chat-files`):** `client.db.test.ts` › *is refused with UNSAFE_TRANSACTION* timed out at its 30s budget in the full suite, and passed alone (5/5 in 9.6s) — the pool wire test opens a pglite-socket server per test, so it feels the same load |
| 2026-09-13 | ExitWorktree `remove` answers *No-op: there is no active EnterWorktree session to exit* for a session standing in its own landed, detached, clean worktree (`sharp-galileo-9f1ee9`) | the desktop app made the worktree and started the session in it; ExitWorktree only removes worktrees made by EnterWorktree in the same session | the tool's own no-op message | leave it detached at `main`, clean, branch deleted, and say so; the census removes it after the session ends and it has been idle an hour. Don't `git worktree remove` the session's own cwd (see the review-server row below for what a deleted cwd does) (`branching.md` → Parallel sessions 9) |
| 2026-09-13 | Signed out (an incognito window), the site offers no way in: `/` redirects to `/sign-in`, which shows *Welcome back* and the top bar's Sign in / Sign up but no form; the URL keeps growing `?redirect_url=…/sign-in…`. Signed-in browsers never see it | the Lists sheet's `app/@sheet/[...catchAll]` puts `catchAll: ["sign-in"]` in `useParams()` on `/sign-in`; `@clerk/nextjs` infers `<SignIn>`'s path by dropping every catch-all param's segments from the pathname (`usePathnameWithoutCatchAll`), so it got `/`, took `/sign-in` for an unknown step, and its fallback route called `redirectToSignIn()` — a client-side loop with no error, every request 200. Curl, Clerk's headers and the env all looked healthy | by hand: a fresh-profile headless Chrome over CDP (the `SignIn` root box has 0 children; wrap `Clerk.redirectToSignIn` and `history.pushState` in an init script to see who navigates) | `<SignIn routing="path" path="/sign-in" />` and the same for `<SignUp>`. Any new catch-all route or parallel slot: check the sign-in page signed out, in a fresh profile, before landing |
| 2026-09-13 | `npm run worktrees -- --prune` removes a worktree but prints *branch … kept*; by hand, `git branch -d chore/worktree-clone-install` (and `fix/consolidate-transaction`) says *not fully merged* — though both had landed and `git merge-base --is-ancestor <branch> main` passed | `git branch -d` judges "fully merged" against the branch's upstream or the HEAD of the checkout it runs in, never `main`. The shared checkout parks detached at the `main` of its last landing (928250d while `main` was 957b8de), so from there every branch landed since looks unmerged. The census checked ancestry against `main`, then deleted with `-d` anyway | the census since `fix/worktrees-branch-delete`: the ancestry check of the branch's tip against `main`, then `git branch -D`; `land`'s closing hint and `branching.md` → Parallel sessions 9 give the same pair. Reproduced: a branch at `main`, a checkout detached at 928250d, `-d` refused, the check passed, `-D` deleted | `git merge-base --is-ancestor <branch> main && git branch -D <branch>`, from any checkout. Never read `-d`'s refusal as unmerged work without that check |
| 2026-09-13 | A review server left running after landing (`fix/consolidate-transaction`, `npm run dev -- -p 3007` in `.claude/worktrees/relaxed-feistel-9378cf`) exits on its own with code 0; restarted, it prints *Module not found: Can't resolve '../../app-render/work-async-storage.external'* and the like for Next's own files, then Turbopack's *FATAL: An unexpected Turbopack error … Expected file content for file*, and the shell reports its cwd deleted | the worktree was removed underneath the server once its branch had landed, was clean and was merged, and the branch was deleted with it. Not by the census's `--prune`: it keeps anything whose `logs/HEAD` changed within the hour, and the last commit there was minutes old. Remover not identified — another session by hand, or the desktop app cleaning up a session worktree once its branch landed. The app locks the worktrees EnterWorktree makes (*locked claude session &lt;name&gt; (pid …)* in `git worktree list --porcelain`) but not the ones it makes itself, so nothing refuses the removal of an app-made worktree like this one. Nothing was lost; everything had landed | by hand: `git worktree list` (the worktree is gone) | take a fresh worktree from `main` for the review (`git worktree add -b <branch> .claude/worktrees/<name> main`), run the preflight (`npm ci` if no checkout has a matching install — here the lockfile had just gained `@electric-sql/pglite-socket`) and start the server there. A session whose app-made worktree is gone can't write into another worktree or move its directory (the app keeps it bound to the deleted one), so hand doc follow-ups to a new session. Until the remover is known, don't count on a landed, clean worktree surviving for a review |
| 2026-09-13 | The dev server logs `[consolidate] failed Error: UNSAFE_TRANSACTION: Only use sql.begin, sql.reserved or max: 1` on opening Home (and after chat turns); episodes and Library notes stop appearing, with no error on the page | a pool option broke a driver path nothing in the tests used: `max_pipeline: 0` (set to stop pooler hangs) short-circuits the `onexecute` callback in which `postgres`'s `sql.begin` reserves its connection, so drizzle's `db.transaction` ran BEGIN unreserved. The PGlite test DB uses its own driver, so every DB test passed; the failure ran inside `after()`, so only the log showed it | `src/db/client.db.test.ts` in `npm run check`: the app's pool (`connect`) with production's options over a real Postgres wire (pglite-socket) — transactions, nesting, a consolidation run, and the driver's refusal pinned | fixed: `connect` replaces `begin` with one on `sql.reserve()`. When changing a pool option or upgrading `postgres`/`drizzle-orm`, run that file first; a test on `openTestDb` alone proves nothing about the driver |
| 2026-09-13 | ExitWorktree `remove` refuses: *Worktree has 108 commits on worktree-&lt;name&gt; … Removing will discard this work permanently*, though the work had landed and been pushed | the tool measures against the branch it created (`worktree-<name>`) and where it started; after `git branch -m` to a real name, landing and `git switch --detach main`, every commit `main` gained since the worktree was made counts as work to discard | by hand: `git merge-base --is-ancestor HEAD origin/main` and `git branch --no-merged origin/main` (nothing listed) | when both pass, the count is `main`'s history, not yours: re-run with `discard_changes: true` (`branching.md` → Parallel sessions 9 is the say-so). If either fails, stop — that is real work |
| 2026-09-13 | `npm run dev -- -p 3006` dies with *EADDRINUSE* seconds after `lsof` showed 3006 free | another session took the same "next free port" between the check and the start: the check isn't atomic, and every session walks up from 3005 the same way | by hand: read the dev server's log after starting it, before handing out the link | take the next free port and read the log again; backlog #1 would bind instead of probing |
| 2026-09-13 | In a worktree session, `"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless --screenshot=…` is refused: *a command whose name is computed at runtime … cannot be shown not to be git* | the worktree Bash guard can't verify a quoted executable path isn't git, so it refuses the whole command | by hand | drive headless Chrome from a Python script (`subprocess` with the path as a list item): `art/prototypes/lumi-walk/capture.py` (`--page` for a whole-page screenshot, `--start/--step/--count` for a frame grid) |
| 2026-09-13 | *(Resolved 2026-09-13: `0003`–`0005` are recorded in `__drizzle_migrations` and `db:migrate` is a no-op again; Claude now applies migrations and reads that table first — CLAUDE.md → Apply and print migrations.)* `npm run db:migrate` fails with *column "source_message_id" … already exists* or *relation "threads" already exists* | `0003_memory_source_message` and `0004_library` (and `feat/plan-together`'s `0003_priorities`) were applied to the database by hand; `drizzle.__drizzle_migrations` records `0000`–`0002` only, so the migrator runs them again | by hand: `select id, created_at from drizzle.__drizzle_migrations` against the journal's `when` values (`domain.md` → Migrations Reference says which migrations are journaled) | before the next `db:migrate`, record the hand-applied migrations in `drizzle.__drizzle_migrations` (hash + `created_at` = the journal's `when`), or keep applying by hand; say in `domain.md` whether a migration is journaled |
| 2026-09-13 | A Claude-in-Chrome screenshot shows a page missing something (Today as a flat brown room, no painting) while `javascript_tool` says it's there (loaded, shown, opacity 1), and a second screenshot after changing a style is pixel-identical | the automation tab is hidden and paints no new frames; a capture returns the last frame it painted, from before the image decoded, until something forces a repaint | by hand: two identical captures across a DOM change; then make a visible change (a red outline) and capture again | before calling something "not rendering" from a capture, force a repaint (a style change) and capture again; trust computed state over a capture of a hidden tab |
| 2026-09-13 | Fifteen worktrees under `.claude/worktrees` by the afternoon — detached HEADs, merged branches, each with its own `node_modules` — and no telling which were safe to delete | sessions made a worktree per task and nothing removed it when the branch landed; a session can't remove the worktree it's standing in, and nobody else knew whether one was still wanted | the worktree census (`npm run worktrees`) | remove your worktree when its branch lands; `npm run worktrees -- --prune` at session end (`branching.md` → Parallel sessions 9–10) |
| 2026-09-13 | `curl localhost:<port>/today` answers **404** on a fresh review server, though the page compiled with no error | signed out, Clerk's proxy answers a request that doesn't ask for HTML with 404, not the sign-in redirect a browser gets | by hand: `-H "Accept: text/html"` on the protected page answers 307 | smoke-test a server with `curl -H "Accept: text/html"` (expect 307 when signed out); a bare 404 there is not a missing route. **`/sign-in` answers 307 too, not 200** (2026-09-13): curl has no Clerk dev-browser cookie, so a development instance sends every request to its handshake first (`x-clerk-auth-reason: dev-browser-missing`, `location: …clerk.accounts.dev/v1/client/handshake`); read the headers with `curl -s -D - -o /dev/null` before calling a 307 a problem |
| 2026-09-13 | *fatal: 'main' is already checked out at '/Users/chante/Projects/lumen'* — a verified, approved branch can't merge; it happened again and again through the day | the merge recipe was `git checkout main && git merge --no-ff`, and git lets a branch be checked out in one worktree only; whichever checkout merged last kept `main` (the shared checkout, usually, while another session was still working there), and ~19 merges a day made it near-constant | `npm run land` (refuses and names the holder; never checks `main` out itself) and the pre-commit guard on the parked detached HEAD | land with `npm run land -- <branch>`; park the shared checkout with `git switch --detach main` |
| 2026-09-13 | A `javascript_tool` script in the Claude-in-Chrome tab hangs until *CDP … timed out after 45000ms*; dispatched clicks seem to do nothing, or land a step late | the automation tab is hidden (`document.visibilityState === "hidden"`): `requestAnimationFrame` never fires, timers are throttled to ~1s, React commits an untrusted event's update later, and CSS transitions freeze (a fade reads as still visible) | by hand: check `document.visibilityState` first | never await `requestAnimationFrame` there; wait with `setTimeout` of 300ms or more before reading state; read computed custom properties and attributes rather than a transitioned value. **Recurred 2026-09-13 (`feat/list-dates`):** extension clicks (by coordinate and by ref) into the hidden tab, with captures showing the row unchanged, read as a Lists date field that wouldn't open, three turns running; a `javascript_tool` click showed it open and focused at once. Check `document.visibilityState` before the first click of any click-through, and verify interaction with `javascript_tool` state when it's hidden. **Recurred 2026-09-13 (`fix/home-opens-on-greeting`):** a rAF-per-frame probe of Home's first paint hung the tab to the 45s timeout. To check paint order in a hidden tab, test the microtask instead: make the DOM change and read state after `await Promise.resolve()` — a mutation observer's work there lands before any paint |
| 2026-09-13 | A Claude-in-Chrome click on an element lands on whatever is next to it (a click on the nav's parchment hit the top bar, so it looked like the click handler was broken) | the extension's screenshot coordinate frame (1448px wide here) is not the page's CSS pixels (`innerWidth` 1728), so a coordinate read off a screenshot is off by the ratio — ~19% at the right edge of a 280px panel | by hand: `document.elementFromPoint(x, y)` at the coordinate, or compare `innerWidth` with the frame the screenshot reports | click by element (`ref` from `find`/`read_page`, or `el.click()` in `javascript_tool`), or scale coordinates by `innerWidth / frame width`; `resize_window` didn't change `innerWidth` either — test a phone width in a same-origin `<iframe>` of the page instead. **Blocked since `chore/hygiene` (2026-09-13):** every response carries `X-Frame-Options: DENY` and `frame-ancestors 'none'` (`next.config.ts`), so the page refuses any frame, same-origin included; use headless Chrome's `--window-size` (`art/prototypes/lumi-walk/capture.py`) or the browser's device emulation instead. **Recurred 2026-09-13 (`fix/home-opens-on-greeting`):** an `<iframe src="/">` load probe cost two turns (a hang, then `contentDocument` null) before this row was read — read the traps before building a browser probe |
| 2026-09-13 | No glass surface blurs on Home, Today or the Library; in Chrome `getComputedStyle(sidebar).backdropFilter` is `none`, and the served CSS has only `-webkit-backdrop-filter` in those rules | the source wrote `backdrop-filter` then `-webkit-backdrop-filter` in each rule; lightningcss (Turbopack's CSS pass in dev, Tailwind's optimize in a build) folds the pair into one property and the later prefixed line wins, so the unprefixed one is dropped. Order-dependent, not value-dependent (prefixed-first keeps both). Chrome ignores the prefix, so nothing errors | CSS prefix audit (`npm run check:css`) | write the unprefixed property only; the pipeline adds `-webkit-` for Safari from its targets |
| 2026-09-13 | A page 404s on a long-running dev server (the app shell renders, *This page could not be found*) though its `page.tsx` exists | the server, orphaned from an earlier session and up since the day before, had lived through branch switches, a route rename and a merge-conflict reverse in its checkout, and its route table lost the page | by hand: the same route on a fresh server works; `ps -o lstart -p $PID` shows the old one's age | review from your own fresh server in your own worktree; stop the stale one only with Chanté's say (it isn't yours) |
| 2026-09-13 | Lumi fails in a worktree's dev server after the OpenAI switch | `.env.local` was copied into the worktree before `main` started needing `OPENAI_API_KEY` | preflight (keys against `.env.example`) | copy the key from the main checkout's `.env.local` |
| 2026-09-13 | `tsc`: *Cannot find module '@ai-sdk/openai'* right after merging `main` | the merge added a package; the worktree's `node_modules` predates it | preflight (installed versions against the lockfile) | `npm ci` |
| 2026-09-13 | `tsc`: *Cannot find module '../../../src/app/lists/page.js'* in `.next/dev/types/validator.ts` | Lists was renamed the Library; `next typegen` rewrites `.next/types` but not `.next/dev/types`, and `tsconfig.json` includes both | preflight (removes the stale types) | automatic; by hand, `rm -rf .next/dev/types` once no server of this checkout is running |
| 2026-09-13 | A worktree runs a different version of a hook than its own branch has | `core.hooksPath` was the shared checkout's absolute path (`/Users/chante/Projects/lumen/.githooks`), so every worktree ran the hooks of whatever commit the shared checkout had out — and a parked checkout trails `main` until someone catches it up; a hook changed on a branch was live nowhere until the shared checkout had it. **Regressed the same day** after this row called it fixed: the code review (`code-review-2026-09-13.md` D1) found it absolute again (one way it comes back, found later the same day on `fix/empty-lumi-turn`: a worktree's own `config.worktree` holds the absolute path, git reads it over the shared value, and so `git config core.hooksPath .githooks` — what the preflight then said — fixed the shared value and left that worktree failing; `git config --show-origin --show-scope --get-all core.hooksPath` shows both), and it was reset to `.githooks` | preflight since `chore/hygiene` (the `core.hooksPath` guard: fails when the value is absolute, skipped in CI); `GIT_TRACE=1 git hook run pre-commit` shows which copy runs | `git config core.hooksPath .githooks`, with `--worktree` when the value is the worktree's own — the preflight names the scope. Never set it with a full path. **What keeps writing it (found 2026-09-13, code review E4): Claude Code itself, outside the repo.** When it creates a worktree (EnterWorktree, `claude --worktree`, the desktop app's session worktrees), its setup step — right after copying `settings.local.json` — reads the main checkout's `core.hooksPath`, and if the value is relative resolves it against the main checkout and runs `git config core.hooksPath /Users/chante/Projects/lumen/.githooks` in the new worktree, logging *Configured worktree to use hooks from main repository: …* (it does the same for a `.husky` directory). This repo has `extensions.worktreeConfig = true`, and a plain `git config` without `--worktree` writes the shared `.git/config`, so every new Claude worktree turns the value absolute for every checkout. It is in the Homebrew CLI (`/opt/homebrew/Caskroom/claude-code/2.1.187/claude`) and in the desktop app's bundled CLI (`~/Library/Application Support/Claude/claude-code/2.1.266`); nothing in the repo, `~/.gitconfig`, `~/.claude/settings.json` or its hooks sets it. The one worktree-scoped absolute value (`sharp-galileo-9f1ee9`'s `config.worktree`) was set by hand earlier. The repo can't stop it (an absolute value is the only one the CLI leaves alone, and that is the state this row got rid of), so the preflight's guard is the catch: expect it after any session that made a worktree, and reset with the fix above. Not changed: the CLI's behaviour is Anthropic's to change |
| 2026-09-13 | `grep --include=*.ts …` → *zsh: no matches found* | zsh expands the unquoted glob before grep sees it | by hand | quote globs (`--include='*.ts'`) |
| 2026-09-13 | `git merge -F -` with a heredoc: *could not read file '-'* in a worktree session | the Bash guard in worktree sessions | by hand | repeated `-m` paragraphs |
| 2026-09-12 | A worktree's review server dies at startup: *Symlink [project]/node_modules is invalid* — though `npm run check` passed | Turbopack refuses a `node_modules` symlink pointing outside the project; tsc and vitest don't mind | preflight (symlink check) | `rm node_modules && npm run preflight` (clones a matching install, or says `npm ci`) |
| 2026-09-12 | A fresh worktree's server fails at the first request (Clerk keys) | `.env.local` is untracked, so a worktree has none | preflight (copies the shared checkout's since 2026-09-13; fails `dev` only when that checkout has none) | automatic; by hand, `cp <main checkout>/.env.local .env.local` |
| 2026-09-12 | Every session's cwd went stale after `rali` → `lumen` | the directory was renamed under running sessions | by hand | coordinated stop (`branching.md` → Parallel sessions 8) |
| — | The browser shows a different branch than the one under review | a dev server serves its checkout's live working tree, and someone switched branches there | by hand (`lsof -a -p $PID -d cwd`, then `git -C <cwd> branch --show-current`) | serve a review from your own worktree; say which port serves which branch |
| — | Pages half-build, `.next` errors | two `next dev` in one checkout share `.next` | by hand (port etiquette) | one server per checkout |

## Backlog — ranked; land or re-rank one per session
1. **Review-server helper.** One command that finds the first free port from 3005, never touches 3000–3004, checks what anything already listening serves, runs the preflight, starts `next dev` in the background and prints the clickable link. **Moved up 2026-09-13:** a port race cost a turn (the EADDRINUSE trap), so it should take a port by trying to listen on it, not by asking `lsof` first, and print the link only once the server says *Ready*. **And lock its worktree, 2026-09-13:** a landed, clean worktree was removed under its review server (that trap) — the helper should `git worktree lock --reason "review server :<port>"` while the server runs and unlock when it stops, so `git worktree remove` and the census refuse it; whether the desktop app's cleanup respects a lock is untested.
2. **Post-merge nudge.** A `post-merge` / `post-checkout` hook that says *package-lock.json changed — run npm ci* the moment it happens, not at the next check. Unblocked 2026-09-13: hooks now run from each checkout's own `.githooks`, so a new hook is live in a worktree as soon as its branch has it.

### Landed
- 2026-09-13 — One Node major everywhere (was #3; code review E8). Chanté: "Yes to pinning to node 22." `"engines": { "node": "22.x" }` in `package.json` (and the lockfile's root entry) overrides the Vercel project's *Node.js Version* (24.x), so production now runs the major that `.nvmrc`, CI, `@types/node` and local dev run. The mismatch is resolved; moving to 24 later is one deliberate change to all four.
- 2026-09-13 — Pin Node (was #3), partly: `.nvmrc` at 22 (what CI runs) and a preflight note when the local major differs. `engines` held back: Vercel runs 24.x (the new #3).
- 2026-09-13 — From the code review (`code-review-2026-09-13.md` D1, D6, D7): the preflight fails on an absolute `core.hooksPath` (skipped in CI); its drift check reads npm's hidden lockfile for every nested entry, so a nested-only change fails the check and a stale install is no longer cloned into a new worktree (verified by bumping one nested version and deleting one entry in a worktree's hidden lockfile: both named, exit 1); and `scripts/lib.mjs` holds `ROOT` (via `fileURLToPath`) and the exec wrappers for the five scripts. `land.mjs` and `worktrees.mjs` keep the same options on every call — `main`'s census and the new one, run back to back, printed identical output.
- 2026-09-13 — Fast worktree setup, Chanté's ask (a new worktree per task was slow): the preflight clones `node_modules` from another checkout with a byte-identical `package-lock.json` whose install matches it — one `clonefile(2)` of the whole tree via `python3` (1.1–1.2 s measured), falling back to `cp -cR` (9.5 s), into a temp name renamed into place — and copies the shared checkout's `.env.local`. `npm ci` measured 11.7 s with a warm cache and writes a fresh ~1 GB; a clone shares blocks until either copy changes. No matching install (a branch that changes packages) → `npm ci`, as before. Not done: cloning `.next` for a warm first compile, and a pool of spare worktrees — try the clone first.
- 2026-09-13 — Worktree census, `scripts/worktrees.mjs` as `npm run worktrees` (report) and `npm run worktrees -- --prune` (remove the safe ones): keeps the shared checkout, the current one and other tools' worktrees, anything locked, in use (a process's cwd inside), active in the last hour, dirty, holding commits not on `main`, or holding ignored files worth keeping; removes the rest with plain `git worktree remove` and deletes merged branches. Its first prune took fifteen worktrees to eleven (four gone, the rest named with their reason).
- 2026-09-13 — Hooks from each checkout's own branch: `core.hooksPath` is the relative `.githooks` (was the shared checkout's absolute path), so every worktree runs its own copy — verified with `GIT_TRACE=1 git hook run pre-commit` (*run_command: .githooks/pre-commit*, from the worktree). The detached-HEAD guard is live wherever a checkout has it, instead of only once the parked shared checkout caught up.
- 2026-09-13 — `scripts/land.mjs` as `npm run land`: lands a branch on `main` without checking `main` out (merge-tree, commit-tree, compare-and-swap update-ref), refusing while any checkout holds `main` or the branch lacks the latest `main`; and the pre-commit guard against commits on the shared checkout's parked detached HEAD. `branching.md` → Landing on main.
- 2026-09-13 — `scripts/preflight.mjs` wired as `precheck` / `predev` / `preflight`: installs against the lockfile, symlinked `node_modules`, stale generated route types, `.env.local` and its required keys. `.env.example` now lists required keys uncommented and optional ones commented, so the preflight can read it.

## What goes to Chanté
Only these; everything else Claude decides, does and records here.
- Anything touching her accounts, keys or money (Vercel, Clerk, Supabase, OpenAI, Anthropic, Google).
- Removing or rewriting anything another session or person owns: their worktrees, branches, stashes, running servers, or files in the shared checkout — including releasing `main` from a checkout a session may be working in.
- Anything on ports 3000–3004 or in another project.
- A change to how *she* works: what she runs, reviews or applies herself (migrations, the review checklist).

## Change log
- 2026-09-13 — The mobile pass (`fix/greeting-mobile-drag`, `chore/dev-test-user`, `fix/mobile-pass`): four trap rows from driving Safari on the iOS Simulator — its cached stylesheet made a correct CSS fix look like a failure, the page can't be inspected from a session (measure with a temporary `console.warn` probe read from the dev log instead), Next's dev indicator covers the Home tab on a phone, and the signed-in pages need the local test user. `core.hooksPath` turned absolute again on creating the worktree, as its row predicts; the preflight caught it. Backlog unchanged.
- 2026-09-13 — Code review E8 (`chore/strictness`): backlog #3 landed — Node pinned to 22 with `engines`, so Vercel's 24.x no longer applies. The backlog is two items now.
- 2026-09-13 — Code review E4, E5 (`chore/strictness`): found what keeps turning `core.hooksPath` absolute — Claude Code's own worktree setup, which resolves a relative value against the main checkout and writes it to the shared config (the `core.hooksPath` trap row has the detail); outside the repo, so nothing changed there, and the preflight guard stays the catch. New trap row: typed keys and Enter don't reach the hidden Claude-in-Chrome tab after a `navigate`. Backlog unchanged.
- 2026-09-13 — `feat/lumi-environment`: after merging `main`, the preflight failed on `core.hooksPath` absolute again, this time from the shared (local) config, not a worktree's own — so something outside the worktrees keeps writing the full path back. The guard caught it and its fix (`git config core.hooksPath .githooks`) worked first time; it cost one run of `npm run check`. Not yet known what re-sets it; if it recurs, `git config --show-origin` right after each session start would narrow it down. (Same finding as `feat/chat-files`, below, the same afternoon: three sightings now, all in the shared `local` scope.)
- 2026-09-13 — `feat/chat-files`: `core.hooksPath` was found absolute twice in one session, in the shared `.git/config` (`local` scope) both times, with no script in the repo that sets it; another session or a tool outside the repo is writing it. The preflight caught it both times and its fix (`git config core.hooksPath .githooks`) worked, so the guard holds; the source is still unknown. Backlog unchanged.
- 2026-09-13 — `fix/sign-in-path`: trap row for the empty sign-in page signed out (the `@sheet` catch-all hid Clerk's inferred path). No guard yet — a signed-out browser check is the catch; a cheap one would be a headless fresh-profile probe of `/sign-in` counting inputs. Backlog unchanged.
- 2026-09-13 — `fix/empty-lumi-turn`: the preflight's `core.hooksPath` fix named the shared scope while the absolute value was this worktree's own (`config.worktree`), so following it changed nothing and cost a turn; the guard now reads the scope and names `--worktree` when that is where the value lives (Guards and Traps rows).
- 2026-09-13 — Code review section D (`chore/hygiene`): the `core.hooksPath` trap row regressed after being called fixed, so the preflight now guards it (Guards row); the preflight's drift check covers nested packages; `scripts/lib.mjs` is shared by the scripts (Guards row); new Guards rows for the route-auth audit (now per handler, over every `src/app` route file, failing on a missing directory) and the server env check at startup; the same-origin iframe tip in the Claude-in-Chrome trap row is blocked by the new frame headers; `.nvmrc` at 22 landed from backlog #3, and #3 is now the Node mismatch with Vercel (24.x), which goes to Chanté. After merging this, other checkouts need `npm ci` (zod 4.6.4); the preflight says so.
- 2026-09-13 — Guard: Lumi's brief check (`scripts/lumi-brief.mjs --check`, in `npm run check`). Chanté asked that the philosophy Lumi is sent be reviewed periodically. A check that fails when a canon source moves makes the branch that moved it do the review, rather than relying on anyone remembering.
- 2026-09-13 — Rules caught up with `branching.md` → Parallel sessions 9: *Session end* 2 and `CLAUDE.md`'s worktree rule delete a landed branch by ancestry + `-D` and use ExitWorktree only for a worktree EnterWorktree made. The review-server trap row adds that app-made worktrees carry no lock; the census's comment no longer says every session locks its worktree. Backlog unchanged.
- 2026-09-13 — Trap row: ExitWorktree is a no-op in a worktree the desktop app made; leave it detached and clean for the census. `branching.md` → Parallel sessions 9 says so. Backlog unchanged.
- 2026-09-13 — Trap and guard fix: the census deleted a landed worktree's branch with `git branch -d`, which judges "merged" against the HEAD of the checkout it runs from, so from the parked shared checkout (behind `main`) it kept `chore/worktree-clone-install` and `fix/consolidate-transaction`. `scripts/worktrees.mjs` now checks the branch tip's ancestry against `main` and deletes with `-D`; `land`'s closing hint, `branching.md` → Parallel sessions 9–10 and the cheat sheet give the same pair. Backlog unchanged.
- 2026-09-13 — Trap row: a landed worktree removed under its review server (not by the census); review from a fresh worktree. Backlog #1, the review-server helper, now also locks its worktree while the server runs.
- 2026-09-13 — Worktree setup in about a second: the preflight clones a matching `node_modules` and copies `.env.local` (*Landed*). Guards row, session start and the symlink / missing-`.env.local` trap fixes updated; `branching.md` → Parallel sessions 2 says to run `npm run preflight` straight after creating a worktree. Backlog unchanged.
- 2026-09-13 — Trap and guard: `max_pipeline: 0` had broken every `db.transaction` (`UNSAFE_TRANSACTION`), so consolidation failed silently in `after()` while every DB test passed on PGlite's own driver. The pool wire test (`client.db.test.ts`, the app's pool over pglite-socket) is in `npm run check` now. Backlog unchanged.
- 2026-09-13 — Two trap rows from the isometric Library session: a port taken between the `lsof` check and `next dev` (EADDRINUSE), and ExitWorktree counting `main`'s history as commits to discard after the worktree's branch was renamed and landed. Backlog re-ranked: the review-server helper is #1, to bind rather than probe; the post-merge nudge is #2. The session-end prune removed `lists-sheet`; ten worktrees kept, each with its reason.
- 2026-09-13 — Trap row: the worktree Bash guard refuses a quoted executable path (headless Chrome); run it from Python (`art/prototypes/lumi-walk/capture.py`). No guard — the refusal is itself the catch.
- 2026-09-13 — Trap row: a signed-out `curl` to a protected page answers 404, not a redirect; smoke-test with `Accept: text/html` or `/sign-in`. No guard — it only misleads a hand check.
- 2026-09-13 — Started, from the stale `.next/dev/types` and the missing `@ai-sdk/openai` hit while merging `docs/today-spec-as-built`. Preflight landed; review links are always clickable (`CLAUDE.md` → Review server).
- 2026-09-13 — Trap and guard: lightningcss dropped `backdrop-filter` from every rule that also wrote `-webkit-backdrop-filter` after it, so no glass blurred in Chrome. The source now writes the unprefixed property alone, and `scripts/check-css-prefixes.mjs` (in `npm run check`) fails on any compiled rule that kept a prefix without its twin.
- 2026-09-13 — Trap: a stale dev server 404s a route that exists (the Library spatial-map session, which also merged `main` mid-branch and needed `npm ci` from the preflight, as the doc predicted).
- 2026-09-13 — Trap and guard, Chanté's ask after the umpteenth blocked merge: `main` held by whichever checkout merged last. `npm run land` replaces `git checkout main && git merge`; nobody parks on `main`; the shared checkout parks detached and the pre-commit hook guards that. Backlog: relative `hooksPath` moved to #1, since a parked checkout's hooks trail `main`.
- 2026-09-13 — Landed backlog #1: `core.hooksPath` is now the relative `.githooks`, so every checkout runs its own copy of the hooks and the detached-HEAD guard is live without catching the shared checkout up. The post-merge nudge (now #1) is unblocked.
- 2026-09-13 — Trap and guard, Chanté's ask to clear the worktrees safely and keep them from piling up: `scripts/worktrees.mjs` (`npm run worktrees`, `--prune`) landed from the backlog; `branching.md` → Parallel sessions 9–10 make removing your worktree at landing, and the census at session end, standing rules. First prune: art-direction (and its merged branch), project-canon, silly-jang-c798f9 and today-quieter removed; eleven kept, each with its reason.
