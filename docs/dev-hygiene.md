# Development hygiene — an evolving strategy

*Claude's working doc, started 2026-09-13. Chanté's ask: catch problems like a stale `.next` or packages missing from `node_modules` earlier, in a doc that evolves to keep development as tidy as possible, developed by Claude's judgement and taken to her only when it has to be. Claude owns it: read it at session start, update it at session end. Branch rules, ports and the docs audit live in [`branching.md`](branching.md); this doc covers what surrounds the code — installs, generated files, env, servers, worktrees, hooks — and how its traps get caught.*

## The strategy
1. **Fail in the first second, with the fix in the message.** A trap that surfaces three steps later as a confusing error (tsc's *Cannot find module*, a server that dies at startup, a 500 at the first request) costs a turn to diagnose. A guard that runs first and says `npm ci` costs nothing. Guards live in `scripts/preflight.mjs` and `.githooks/`.
2. **Every snag becomes a row; a cheap catch becomes a guard now.** Anything that cost a turn goes in *Traps* in the same session: symptom, cause, how it's caught, fix. If the catch is a few lines of script, write it then rather than waiting for the second time.
3. **Generated state is disposable; nothing else is.** `.next/`, `node_modules/` and `tsconfig.tsbuildinfo` can be deleted and rebuilt without asking, once no running server uses them. Anything tracked, anything hand-made but untracked (`.env.local`, drafts in `art/`), and anything another session owns (branches, stashes, worktrees, servers) is never cleaned up on a guess.
4. **A merge brings environment, not just code.** Merging or pulling `main` can add a package, rename a route or add an env key. After any merge, run the preflight (`npm run check` does) before believing an error.
5. **Leave every checkout as you found it, or tidier.** Servers you started stopped, merged branches deleted, your worktree committed or clean, nothing stashed, nothing parked on `main`.

## Session start (≤ 1 minute)
1. `git branch --show-current` — is it yours? (`branching.md` → Parallel sessions.) Empty in the shared checkout means it's parked: `git switch --detach main`, then branch. In a worktree, rename a `worktree-*` or `claude/*` branch to `type/slug` before the first commit.
2. `npm run preflight` — or let `npm run check` / `npm run dev` run it. Do what it says before anything else. In a fresh worktree it also sets the checkout up: a cloned `node_modules` and a copy of `.env.local`, in about a second.
3. Merged `origin/main` or pulled mid-session? Preflight again.
4. Skim *Traps* for the area you're about to touch.

## Session end (≤ 3 minutes, before the last commit)
1. Stop every server you started, and only those. Any left running for review: say which port serves which branch, as a clickable link — in every reply while it runs, not only the one that started it.
2. Merged branches deleted; nothing of yours in `git stash list`; no checkout of yours on `main`; your worktree removed once its branch landed and it's safe, without asking (standing in it: detach, delete the branch, ExitWorktree `remove`); if it isn't safe, name it and why in the summary (`branching.md` → Parallel sessions 9).
3. `npm run worktrees -- --prune` — clears every worktree that's safe to remove, whoever made it; anything kept for a reason only its owner can settle (uncommitted work, unmerged commits, a differing `.env.local`) is named to Chanté.
4. Anything that cost a turn → a *Traps* row. Land one *Backlog* item, or re-rank it if something moved.
5. Fix what this doc got wrong; add a *Change log* line.

## Guards in place
| Guard | Catches | Runs | Where |
|---|---|---|---|
| Worktree census | worktrees piling up: each one listed with why it stays (the shared checkout, this checkout, another tool's, locked, in use, active in the last hour, uncommitted changes, commits not on `main`, ignored files worth keeping) or that it can go; `--prune` removes only those, and deletes a branch only when merged | at session end; by hand as `npm run worktrees` | `scripts/worktrees.mjs`, `branching.md` → Parallel sessions 9–10 |
| Preflight | `node_modules` missing (clones the install of another checkout with a byte-identical `package-lock.json` — copy-on-write, ~1 s, no extra disk — or says `npm ci` when none matches), a symlink, or behind `package-lock.json` (names each package) · generated route types pointing at routes that no longer exist (removes them; Next writes them again) · `.env.local` missing (copies the shared checkout's), or missing a key `.env.example` requires (key names only, never values) | before `npm run check` and `npm run dev` (npm's `precheck` / `predev`); by hand as `npm run preflight`. Env problems fail `dev` and are a note on `check`; skipped in CI | `scripts/preflight.mjs` |
| `npm run check` | types, lint incl. the `src/core` import guard, tests, the route-auth audit, the CSS prefix audit | before every landing; CI on every push to `main` and every PR | `package.json`, `.github/workflows/ci.yml` |
| Pool wire test | a pool option or driver upgrade that breaks the app's own client — transactions, nesting — which the PGlite test DB can't see (it has its own driver) | inside `npm run check` (vitest) | `src/db/client.db.test.ts`, `openTestServer` in `src/db/test-db.ts` |
| CSS prefix audit | a rule in the compiled stylesheet that kept `-webkit-<prop>` but lost `<prop>` (lightningcss drops the unprefixed one when the prefixed line follows it) | inside `npm run check`; by hand as `npm run check:css` | `scripts/check-css-prefixes.mjs` |
| `npm run land` | a merge that would need `main` checked out; landing a branch that doesn't contain the latest `main` (conflicts on `main`); two sessions landing at once (compare-and-swap on the ref) | every landing, instead of `git checkout main && git merge` | `scripts/land.mjs`, `branching.md` → Landing on main |
| pre-commit hook | `.claude/` bookkeeping staged; direct commits to `main`; commits on the shared checkout's parked detached HEAD | every commit | `.githooks/pre-commit` |
| Port etiquette | killing or reusing someone else's server; two servers in one checkout | before starting any server (by hand) | `CLAUDE.md`, `branching.md` → Dev servers |

## Traps — symptom → cause → catch → fix
Newest first. *By hand* in the catch column marks a backlog candidate.

| First seen | Symptom | Cause | Caught by | Fix |
|---|---|---|---|---|
| 2026-09-13 | The dev server logs `[consolidate] failed Error: UNSAFE_TRANSACTION: Only use sql.begin, sql.reserved or max: 1` on opening Home (and after chat turns); episodes and Library notes stop appearing, with no error on the page | a pool option broke a driver path nothing in the tests used: `max_pipeline: 0` (set to stop pooler hangs) short-circuits the `onexecute` callback in which `postgres`'s `sql.begin` reserves its connection, so drizzle's `db.transaction` ran BEGIN unreserved. The PGlite test DB uses its own driver, so every DB test passed; the failure ran inside `after()`, so only the log showed it | `src/db/client.db.test.ts` in `npm run check`: the app's pool (`connect`) with production's options over a real Postgres wire (pglite-socket) — transactions, nesting, a consolidation run, and the driver's refusal pinned | fixed: `connect` replaces `begin` with one on `sql.reserve()`. When changing a pool option or upgrading `postgres`/`drizzle-orm`, run that file first; a test on `openTestDb` alone proves nothing about the driver |
| 2026-09-13 | ExitWorktree `remove` refuses: *Worktree has 108 commits on worktree-&lt;name&gt; … Removing will discard this work permanently*, though the work had landed and been pushed | the tool measures against the branch it created (`worktree-<name>`) and where it started; after `git branch -m` to a real name, landing and `git switch --detach main`, every commit `main` gained since the worktree was made counts as work to discard | by hand: `git merge-base --is-ancestor HEAD origin/main` and `git branch --no-merged origin/main` (nothing listed) | when both pass, the count is `main`'s history, not yours: re-run with `discard_changes: true` (`branching.md` → Parallel sessions 9 is the say-so). If either fails, stop — that is real work |
| 2026-09-13 | `npm run dev -- -p 3006` dies with *EADDRINUSE* seconds after `lsof` showed 3006 free | another session took the same "next free port" between the check and the start: the check isn't atomic, and every session walks up from 3005 the same way | by hand: read the dev server's log after starting it, before handing out the link | take the next free port and read the log again; backlog #1 would bind instead of probing |
| 2026-09-13 | In a worktree session, `"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless --screenshot=…` is refused: *a command whose name is computed at runtime … cannot be shown not to be git* | the worktree Bash guard can't verify a quoted executable path isn't git, so it refuses the whole command | by hand | drive headless Chrome from a Python script (`subprocess` with the path as a list item): `art/prototypes/lumi-walk/capture.py` (`--page` for a whole-page screenshot, `--start/--step/--count` for a frame grid) |
| 2026-09-13 | `npm run db:migrate` fails with *column "source_message_id" … already exists* or *relation "threads" already exists* | `0003_memory_source_message` and `0004_library` (and `feat/plan-together`'s `0003_priorities`) were applied to the database by hand; `drizzle.__drizzle_migrations` records `0000`–`0002` only, so the migrator runs them again | by hand: `select id, created_at from drizzle.__drizzle_migrations` against the journal's `when` values (`domain.md` → Migrations Reference says which migrations are journaled) | before the next `db:migrate`, record the hand-applied migrations in `drizzle.__drizzle_migrations` (hash + `created_at` = the journal's `when`), or keep applying by hand; say in `domain.md` whether a migration is journaled |
| 2026-09-13 | A Claude-in-Chrome screenshot shows a page missing something (Today as a flat brown room, no painting) while `javascript_tool` says it's there (loaded, shown, opacity 1), and a second screenshot after changing a style is pixel-identical | the automation tab is hidden and paints no new frames; a capture returns the last frame it painted, from before the image decoded, until something forces a repaint | by hand: two identical captures across a DOM change; then make a visible change (a red outline) and capture again | before calling something "not rendering" from a capture, force a repaint (a style change) and capture again; trust computed state over a capture of a hidden tab |
| 2026-09-13 | Fifteen worktrees under `.claude/worktrees` by the afternoon — detached HEADs, merged branches, each with its own `node_modules` — and no telling which were safe to delete | sessions made a worktree per task and nothing removed it when the branch landed; a session can't remove the worktree it's standing in, and nobody else knew whether one was still wanted | the worktree census (`npm run worktrees`) | remove your worktree when its branch lands; `npm run worktrees -- --prune` at session end (`branching.md` → Parallel sessions 9–10) |
| 2026-09-13 | `curl localhost:<port>/today` answers **404** on a fresh review server, though the page compiled with no error | signed out, Clerk's proxy answers a request that doesn't ask for HTML with 404, not the sign-in redirect a browser gets | by hand: `-H "Accept: text/html"` on the protected page answers 307 | smoke-test a server with `curl -H "Accept: text/html"` (expect 307 when signed out); a bare 404 there is not a missing route. **`/sign-in` answers 307 too, not 200** (2026-09-13): curl has no Clerk dev-browser cookie, so a development instance sends every request to its handshake first (`x-clerk-auth-reason: dev-browser-missing`, `location: …clerk.accounts.dev/v1/client/handshake`); read the headers with `curl -s -D - -o /dev/null` before calling a 307 a problem |
| 2026-09-13 | *fatal: 'main' is already checked out at '/Users/chante/Projects/lumen'* — a verified, approved branch can't merge; it happened again and again through the day | the merge recipe was `git checkout main && git merge --no-ff`, and git lets a branch be checked out in one worktree only; whichever checkout merged last kept `main` (the shared checkout, usually, while another session was still working there), and ~19 merges a day made it near-constant | `npm run land` (refuses and names the holder; never checks `main` out itself) and the pre-commit guard on the parked detached HEAD | land with `npm run land -- <branch>`; park the shared checkout with `git switch --detach main` |
| 2026-09-13 | A `javascript_tool` script in the Claude-in-Chrome tab hangs until *CDP … timed out after 45000ms*; dispatched clicks seem to do nothing, or land a step late | the automation tab is hidden (`document.visibilityState === "hidden"`): `requestAnimationFrame` never fires, timers are throttled to ~1s, React commits an untrusted event's update later, and CSS transitions freeze (a fade reads as still visible) | by hand: check `document.visibilityState` first | never await `requestAnimationFrame` there; wait with `setTimeout` of 300ms or more before reading state; read computed custom properties and attributes rather than a transitioned value. **Recurred 2026-09-13 (`feat/list-dates`):** extension clicks (by coordinate and by ref) into the hidden tab, with captures showing the row unchanged, read as a Lists date field that wouldn't open, three turns running; a `javascript_tool` click showed it open and focused at once. Check `document.visibilityState` before the first click of any click-through, and verify interaction with `javascript_tool` state when it's hidden |
| 2026-09-13 | A Claude-in-Chrome click on an element lands on whatever is next to it (a click on the nav's parchment hit the top bar, so it looked like the click handler was broken) | the extension's screenshot coordinate frame (1448px wide here) is not the page's CSS pixels (`innerWidth` 1728), so a coordinate read off a screenshot is off by the ratio — ~19% at the right edge of a 280px panel | by hand: `document.elementFromPoint(x, y)` at the coordinate, or compare `innerWidth` with the frame the screenshot reports | click by element (`ref` from `find`/`read_page`, or `el.click()` in `javascript_tool`), or scale coordinates by `innerWidth / frame width`; `resize_window` didn't change `innerWidth` either — test a phone width in a same-origin `<iframe>` of the page instead |
| 2026-09-13 | No glass surface blurs on Home, Today or the Library; in Chrome `getComputedStyle(sidebar).backdropFilter` is `none`, and the served CSS has only `-webkit-backdrop-filter` in those rules | the source wrote `backdrop-filter` then `-webkit-backdrop-filter` in each rule; lightningcss (Turbopack's CSS pass in dev, Tailwind's optimize in a build) folds the pair into one property and the later prefixed line wins, so the unprefixed one is dropped. Order-dependent, not value-dependent (prefixed-first keeps both). Chrome ignores the prefix, so nothing errors | CSS prefix audit (`npm run check:css`) | write the unprefixed property only; the pipeline adds `-webkit-` for Safari from its targets |
| 2026-09-13 | A page 404s on a long-running dev server (the app shell renders, *This page could not be found*) though its `page.tsx` exists | the server, orphaned from an earlier session and up since the day before, had lived through branch switches, a route rename and a merge-conflict reverse in its checkout, and its route table lost the page | by hand: the same route on a fresh server works; `ps -o lstart -p $PID` shows the old one's age | review from your own fresh server in your own worktree; stop the stale one only with Chanté's say (it isn't yours) |
| 2026-09-13 | Lumi fails in a worktree's dev server after the OpenAI switch | `.env.local` was copied into the worktree before `main` started needing `OPENAI_API_KEY` | preflight (keys against `.env.example`) | copy the key from the main checkout's `.env.local` |
| 2026-09-13 | `tsc`: *Cannot find module '@ai-sdk/openai'* right after merging `main` | the merge added a package; the worktree's `node_modules` predates it | preflight (installed versions against the lockfile) | `npm ci` |
| 2026-09-13 | `tsc`: *Cannot find module '../../../src/app/lists/page.js'* in `.next/dev/types/validator.ts` | Lists was renamed the Library; `next typegen` rewrites `.next/types` but not `.next/dev/types`, and `tsconfig.json` includes both | preflight (removes the stale types) | automatic; by hand, `rm -rf .next/dev/types` once no server of this checkout is running |
| 2026-09-13 | A worktree runs a different version of a hook than its own branch has | `core.hooksPath` was the shared checkout's absolute path (`/Users/chante/Projects/lumen/.githooks`), so every worktree ran the hooks of whatever commit the shared checkout had out — and a parked checkout trails `main` until someone catches it up; a hook changed on a branch was live nowhere until the shared checkout had it | fixed 2026-09-13: `git config --get core.hooksPath` reads `.githooks` (relative); `GIT_TRACE=1 git hook run pre-commit` shows which copy runs | keep it relative; if it ever reads absolute again, `git config core.hooksPath .githooks` |
| 2026-09-13 | `grep --include=*.ts …` → *zsh: no matches found* | zsh expands the unquoted glob before grep sees it | by hand | quote globs (`--include='*.ts'`) |
| 2026-09-13 | `git merge -F -` with a heredoc: *could not read file '-'* in a worktree session | the Bash guard in worktree sessions | by hand | repeated `-m` paragraphs |
| 2026-09-12 | A worktree's review server dies at startup: *Symlink [project]/node_modules is invalid* — though `npm run check` passed | Turbopack refuses a `node_modules` symlink pointing outside the project; tsc and vitest don't mind | preflight (symlink check) | `rm node_modules && npm run preflight` (clones a matching install, or says `npm ci`) |
| 2026-09-12 | A fresh worktree's server fails at the first request (Clerk keys) | `.env.local` is untracked, so a worktree has none | preflight (copies the shared checkout's since 2026-09-13; fails `dev` only when that checkout has none) | automatic; by hand, `cp <main checkout>/.env.local .env.local` |
| 2026-09-12 | Every session's cwd went stale after `rali` → `lumen` | the directory was renamed under running sessions | by hand | coordinated stop (`branching.md` → Parallel sessions 8) |
| — | The browser shows a different branch than the one under review | a dev server serves its checkout's live working tree, and someone switched branches there | by hand (`lsof -a -p $PID -d cwd`, then `git -C <cwd> branch --show-current`) | serve a review from your own worktree; say which port serves which branch |
| — | Pages half-build, `.next` errors | two `next dev` in one checkout share `.next` | by hand (port etiquette) | one server per checkout |

## Backlog — ranked; land or re-rank one per session
1. **Review-server helper.** One command that finds the first free port from 3005, never touches 3000–3004, checks what anything already listening serves, runs the preflight, starts `next dev` in the background and prints the clickable link. **Moved up 2026-09-13:** a port race cost a turn (the EADDRINUSE trap), so it should take a port by trying to listen on it, not by asking `lsof` first, and print the link only once the server says *Ready*.
2. **Post-merge nudge.** A `post-merge` / `post-checkout` hook that says *package-lock.json changed — run npm ci* the moment it happens, not at the next check. Unblocked 2026-09-13: hooks now run from each checkout's own `.githooks`, so a new hook is live in a worktree as soon as its branch has it.
3. **Pin Node.** An `.nvmrc` and `engines` at 22 (CI's version), with a preflight line when the local major differs.

### Landed
- 2026-09-13 — Fast worktree setup, Chanté's ask (a new worktree per task was slow): the preflight clones `node_modules` from another checkout with a byte-identical `package-lock.json` whose install matches it — one `clonefile(2)` of the whole tree via `python3` (1.1–1.2 s measured), falling back to `cp -cR` (9.5 s), into a temp name renamed into place — and copies the shared checkout's `.env.local`. `npm ci` measured 11.7 s with a warm cache and writes a fresh ~1 GB; a clone shares blocks until either copy changes. No matching install (a branch that changes packages) → `npm ci`, as before. Not done: cloning `.next` for a warm first compile, and a pool of spare worktrees — try the clone first.
- 2026-09-13 — Worktree census, `scripts/worktrees.mjs` as `npm run worktrees` (report) and `npm run worktrees -- --prune` (remove the safe ones): keeps the shared checkout, the current one and other tools' worktrees, anything locked, in use (a process's cwd inside), active in the last hour, dirty, holding commits not on `main`, or holding ignored files worth keeping; removes the rest with plain `git worktree remove` and deletes merged branches. Its first prune took fifteen worktrees to eleven (four gone, the rest named with their reason).
- 2026-09-13 — Hooks from each checkout's own branch: `core.hooksPath` is the relative `.githooks` (was the shared checkout's absolute path), so every worktree runs its own copy — verified with `GIT_TRACE=1 git hook run pre-commit` (*run_command: .githooks/pre-commit*, from the worktree). The detached-HEAD guard is live wherever a checkout has it, instead of only once the parked shared checkout caught up.
- 2026-09-13 — `scripts/land.mjs` as `npm run land`: lands a branch on `main` without checking `main` out (merge-tree, commit-tree, compare-and-swap update-ref), refusing while any checkout holds `main` or the branch lacks the latest `main`; and the pre-commit guard against commits on the shared checkout's parked detached HEAD. `branching.md` → Landing on main.
- 2026-09-13 — `scripts/preflight.mjs` wired as `precheck` / `predev` / `preflight`: installs against the lockfile, symlinked `node_modules`, stale generated route types, `.env.local` and its required keys. `.env.example` now lists required keys uncommented and optional ones commented, so the preflight can read it.

## What goes to Chanté
Only these; everything else Claude decides, does and records here.
- Anything touching her accounts, keys or money (Vercel, Clerk, Supabase, OpenAI, Anthropic, Google).
- Removing or rewriting anything another session or person owns: their worktrees, branches, stashes, running servers, or files in the shared checkout — including releasing `main` from a checkout a session may be working in.
- Anything on ports 3000–3004 or in another project.
- A change to how *she* works: what she runs, reviews or applies herself (migrations, the review checklist).

## Change log
- 2026-09-13 — Worktree setup in about a second: the preflight clones a matching `node_modules` and copies `.env.local` (*Landed*). Guards row, session start and the symlink / missing-`.env.local` trap fixes updated; `branching.md` → Parallel sessions 2 says to run `npm run preflight` straight after creating a worktree. Backlog unchanged.
- 2026-09-13 — Trap and guard: `max_pipeline: 0` had broken every `db.transaction` (`UNSAFE_TRANSACTION`), so consolidation failed silently in `after()` while every DB test passed on PGlite's own driver. The pool wire test (`client.db.test.ts`, the app's pool over pglite-socket) is in `npm run check` now. Backlog unchanged.
- 2026-09-13 — Two trap rows from the isometric Library session: a port taken between the `lsof` check and `next dev` (EADDRINUSE), and ExitWorktree counting `main`'s history as commits to discard after the worktree's branch was renamed and landed. Backlog re-ranked: the review-server helper is #1, to bind rather than probe; the post-merge nudge is #2. The session-end prune removed `lists-sheet`; ten worktrees kept, each with its reason.
- 2026-09-13 — Trap row: the worktree Bash guard refuses a quoted executable path (headless Chrome); run it from Python (`art/prototypes/lumi-walk/capture.py`). No guard — the refusal is itself the catch.
- 2026-09-13 — Trap row: a signed-out `curl` to a protected page answers 404, not a redirect; smoke-test with `Accept: text/html` or `/sign-in`. No guard — it only misleads a hand check.
- 2026-09-13 — Started, from the stale `.next/dev/types` and the missing `@ai-sdk/openai` hit while merging `docs/today-spec-as-built`. Preflight landed; review links are always clickable (`CLAUDE.md` → Review server).
- 2026-09-13 — Trap and guard: lightningcss dropped `backdrop-filter` from every rule that also wrote `-webkit-backdrop-filter` after it, so no glass blurred in Chrome. The source now writes the unprefixed property alone, and `scripts/check-css-prefixes.mjs` (in `npm run check`) fails on any compiled rule that kept a prefix without its twin.
- 2026-09-13 — Trap: a stale dev server 404s a route that exists (the Library spatial-map session, which also merged `main` mid-branch and needed `npm ci` from the preflight, as the doc predicted).
- 2026-09-13 — Trap and guard, Chanté's ask after the umpteenth blocked merge: `main` held by whichever checkout merged last. `npm run land` replaces `git checkout main && git merge`; nobody parks on `main`; the shared checkout parks detached and the pre-commit hook guards that. Backlog: relative `hooksPath` moved to #1, since a parked checkout's hooks trail `main`.
- 2026-09-13 — Landed backlog #1: `core.hooksPath` is now the relative `.githooks`, so every checkout runs its own copy of the hooks and the detached-HEAD guard is live without catching the shared checkout up. The post-merge nudge (now #1) is unblocked.
- 2026-09-13 — Trap and guard, Chanté's ask to clear the worktrees safely and keep them from piling up: `scripts/worktrees.mjs` (`npm run worktrees`, `--prune`) landed from the backlog; `branching.md` → Parallel sessions 9–10 make removing your worktree at landing, and the census at session end, standing rules. First prune: art-direction (and its merged branch), project-canon, silly-jang-c798f9 and today-quieter removed; eleven kept, each with its reason.
