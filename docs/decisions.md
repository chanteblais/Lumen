# Decisions log (ADR-lite) — engineering

Append-only. One entry per decision that changes `architecture.md`, `domain.md` or the plan. Format: date · decision · why · what it replaces.

**Product decisions** (what the product is and why, and the constraints they put on future work) live in [`living/decisions.md`](living/decisions.md), the canon's log. An entry here that changes how the product behaves gets a short entry there too.

- **2026-09-18 · Rhythms, and how full the day is as a shape (`feat/rhythms`, migration `0010`).** Chanté asked for her gym and meditation conversations to shape Today, with "a rhythms section" — and chose *how full the day is, as a shape* plus *the line* from the dashboard ask ([product decisions](living/decisions.md)). `rhythms` + `rhythm_practices` (`core/domain/rhythms.ts`: `holdRhythm`, `letGoRhythm`, `practiceRhythm` once per local day via a unique index, `unpracticeRhythm` app-only, `listRhythms` with practice days from last Monday, `describePractice` in words; tested on PGlite). Tools `hold_rhythm` / `practiced_rhythm` / `let_go_rhythm`; the persona tells Lumi a routine is a rhythm, not an intention, and never to keep score. Snapshot → context block (*Rhythms they're building*) and planner (words only; a rule that a rhythm is never on the path and the dayLine may name room for one). `PATCH /api/rhythms/[id]` `{ action: practiced | unpracticed }` for Today's *Did it*. `shapeDay` now keeps empty windows and reports `minutes` / `free` per window, so Today can say where a rhythm fits ("room before practicum") and draw the band. Today: `.today-shape` (the band), a *Rhythms* part (`RhythmRow`: name, seven marks, their words, *Did it*). Cached prefix moves (persona + three tool descriptions): `prefix.test.ts` re-pinned; every cached turn misses once after deploy. Voice eval run 12. No data is derived into a score anywhere; nothing else for the user to keep beyond the burden-log rows.
- **2026-09-18 · The persona's Today line describes the spine (`docs/dominance-retired`).** `persona.ts` → *The app, and where they are* still said "One thing Right now … on its card", missed when the page changed; now it describes what's there (what's done, Now, the suggested things in the windows between fixed times, Start here, a line per area; a row opens to Not this and Break it down). Cached prefix moves: `prefix.test.ts` pins the new hashes; every cached turn misses once after deploy. Voice eval run 11 (scenario 8 only). Same branch: the canon retires *one thing should dominate* and opens question 29 on dashboards ([product entry](living/decisions.md)); the brief's text is unchanged, re-stamped. No migration; nothing for the user to keep.
- **2026-09-18 · Today: the rest of the day laid along itself — no Right now card (`ux/today-at-a-glance`).** Chanté: "focus less on breaking down a single task, and more to give you today at a glance"; then, with `art/mockups/today-at-a-glance-mockup.webp`, "Let's get rid of this right now item"; then "take some creative license … someone with ADHD-like traits … the project's design philosophy". Three cuts on one branch; this is the third. `PlanSection` renders `voice` (unchanged) and `day`: one sheet with a spine. **`shapeDay`** (`core/domain/day-shape.ts`, pure, tested) lays the path (`plan.rightNow` then `plan.afterThat`) into the windows between now, each `plan.later` landmark still ahead (10 minutes' transition before each; an hour long when unestimated) and the day's end (22:00 local): first fit in Lumi's order, half an hour assumed when unestimated, leftovers as `spill`; window spans are `spanWords` (words, never numbers), said only before a landmark. **`glanceAreas`** (`core/domain/day-glance.ts`, pure, tested): one line per list with anything open, headline by precedence *matters* (a priority holding today, `holdsOn`) › *due today* › *due tomorrow* › *on today's path* › *can wait*, top four by that rank, shown in list order; no numbers. Rows are `TodayRow`s (`components/today/TodayRow.tsx`, replacing `RightNowActions.tsx`): the plan's first thing gets `startHere` (its first step rendered before any tap) and estimates ≤ 10 min get `quick`; each opens in place to its first step, *Not this* and *Break it down*. **Done today** is `doneOn` (`core/domain/intentions.ts`, tested) over the snapshot's `recentlyDone`, drawn above the Now mark. The spine is laid out from the clock on every render (`force-dynamic`); nothing about it is stored. **Not this on a row that isn't the plan's first** now just drops it from the path: `recutAfterDecline` reads the newest `declinedToday` id, and when it isn't `rightNow` saves `advancePlan(plan, id)` with `restCanWait: true` — no model call, no note, no refine (tested). `DayPlanJson` keeps `rightNow`: the planner, `clampPlan`, the re-cut triggers and `reshape_today`'s pin are unchanged; the page just stops setting it apart. Lumi's brief's places line changes with it (it said Today's changes happen "on its card"), so the cached prefix moves: `prefix.test.ts` pins the new hashes, and every cached turn misses once after deploy. Product entry in [`living/decisions.md`](living/decisions.md). **Replaces** the three layers of 2026-09-13 (words · one Right now card · a faint slip). Nothing for the user to keep; no migration.
- **2026-09-18 · The avatar is just her head, with nothing behind it; the lantern cut is retired (`ux/lumi-avatar-portrait`).** Chanté drew the hooded head in the medallion costume on transparency (`art/lumi/lumi-avatar-head.png`, 2026-09-15) and asked for the avatar to be "just her head with no background", as `art/mockups/chat-mockup.png` draws it. `scripts/cut-lumi-avatar.py` now trims that whole drawing — hood, ribbon and medallions — and fits it to 98% of a transparent 272px square, centred; `LumiAvatar` draws it with `bg-contain`, no `rounded-full`, nothing behind, so the paper or the painted room shows around her. It replaces the painted medallion of *Lumi's avatar is Chanté's painted medallion* below (`art/lumi/avatar.png`, no longer cut; the same painting is kept as `mockups/lumi-avatar.png`), and she now wears the same costume and hood as the Lumi in the corner (`art-direction.md` §7). With nothing showing the six lantern heads (cut but unshown since the medallion) or the lantern body (unplayed since the hands-free Lumi), `public/lumi-heads.webp`, `public/lumi-idle.webp` and `scripts/cut-lumi-idle.py` are deleted, with the `head` sheet, `headCell` and `LUMI_EXPRESSIONS` in `LumiSprite` — the second option of `animation-pipeline.md` backlog #2; `scripts/lumi_cut.py` is now the only set of cut helpers. The source sheets stay in `art/lumi/` (the lantern sheet is still the style reference every prompt attaches). An expression, when one is wanted, is a drawn eye shape over this face (bet 1), not a sheet. The favicon (the compass star) is unchanged. Nothing for the user to keep; no migration.
- **2026-09-15 · A Library note's date is their local day, not UTC (`fix/note-when-local-day`).** `open_thread`'s `notes` and `earlier` stamped `when` with `createdAt.toISOString().slice(0, 10)`. In Vancouver (UTC−7/−8) anything filed after 4–5pm came back to Lumi as the *next* day, so "you decided that yesterday" could be a day out — in a product whose whole frame is days. Now `localDate(n.createdAt, timezone)` (`core/time.ts`), the helper the context block already used for the same notes (`localFormat`), so the tool result and the context can no longer disagree about the same note. Covered by a regression test in `library.db.test.ts` with an evening-in-Vancouver instant that falls on the next UTC day. `due-date.ts` and `priorities.ts` keep `toISOString().slice(0, 10)`: they read back dates built with `Date.UTC` from local calendar fields, which is the correct use. Volatile context only, after the cached prefix — the persona and tool descriptions are unchanged. **Replaces** the UTC stamp introduced with the Library tools (2026-09-13). No migration; nothing for the user to keep. Same UTC pattern as the one fixed in `scripts/lumi-brief.mjs` on `docs/desk-round-2`.

- **2026-09-15 · Row level security on every table, and nothing granted to Supabase's API roles (`fix/rls-all-tables`, migration `0009`).** Supabase's security advisor flagged `leads`, `day_plans`, `design_contributions`, `design_contribution_revisions` and `design_digests` with RLS off. Reading the database showed `anon` and `authenticated` held every privilege on all 16 tables in `public`, through Supabase's default privileges for new tables. The other 11 tables had RLS switched on outside the migrations, so the gap reopened with each new table. The Data API serves `public` to anyone holding the anon key, which Supabase treats as public. Now every table declares `.enableRLS()` in `schema.ts` (no policies: the app connects as `postgres`, the owner, which RLS doesn't apply to, so nothing in the app changes), `0009` enables it and revokes the two roles' grants and default privileges on tables and sequences in `public`, and `src/db/rls.db.test.ts` fails `npm run check` if a table lacks either. Replaces "all DB access is server-side, so RLS is irrelevant" in `architecture.md`. Using `supabase-js` or the Data API later would mean writing policies and granting back deliberately. No product change; nothing for the user to keep.

- **2026-09-14 · The persona and brief say what Lumi is for: seeing what matters, and moving it forward (`docs/lumi-function`).** Chanté named Lumi's function ([product decision](living/decisions.md)). In `persona.ts`, the opening line changes, and *What you're for* gains a short paragraph and a first item, *What matters*: goals and priorities noticed in passing, readings offered as questions, never a goals list, thinking about direction left as thinking. Items 1–6 become 2–7. The brief gains one sentence in *Coherence remembers; you understand*. Both sit in the cached prefix, so the first call after deploy misses the cache once. No tool or context-block change; `prefix.test.ts` pins the new hashes. Voice eval run 9. No migration; nothing for the user to keep.
- **2026-09-14 · Lumi's design notebook: a structured store, a deterministic daily digest, gated per user (`feat/design-contributions`, migration `0008`).** Chanté asked for Lumi to contribute proactively to Coherence's design documentation, with a daily digest, without approving each note. Chosen: **database rows with a readable view, not a markdown file in the repo** — the app runs on Vercel and must never write the repository, and a store keeps explicit per-part statuses and a revision history a file would blur. **Two statuses per note** (`insight_status`, `possibility_status`) so endorsing a problem can't endorse a proposed interface. **An append-only revision table** holding the whole note after every change, whose bigserial id is the digest's watermark. **The digest is built in code, no model call**: the notes already hold Lumi's thinking; a model pass would add cost and a second place for inference to creep in. **Retry safety** from a unique `(user_id, from_revision_id)`, windows ending at the previous local day plus a 2-minute settle. **Triggered in `after()` on a design partner's first chat turn of a new day** (the reflection/consolidation pattern; no cron route, no `CRON_SECRET` to set) with `npm run design:digest -- --generate` for scheduling from outside. **Gated by `COHERENCE_DESIGN_PARTNERS`**, with the persona section and tools added only for partners, so every other user's cached prefix is unchanged. Considered and not built: a `docs/design-contributions.md` Lumi edits (repository write from the app); a model-written digest; a Vercel cron route (setup, and a public proxy path, for no gain while notes only come from chat). **Replaces** nothing.
- **2026-09-14 · A session left open teaches reflection nothing (`session/2026-09-14-routine-review`).** `deterministicSessionOps` no longer contradicts a matching strategy when a session was `abandoned` (left open past twice its length, closed by a later visit), and `worthModelStep` skips the model step for one; `REFLECTION_RULES` creates an anti_pattern only from a stopped-early session whose transcript says what got in the way. Open question 28 and the reconciliation had flagged the old rule: timeout alone is not evidence, and the routine review's *Silence is not an outcome* made the rule explicit (only learn evaluatively from what the user said or did). Dormant code — nothing starts or ends a session since 2026-09-13 — fixed now so sessions can't bring the rule back. Product entry: `living/decisions.md` → *Directions adopted from the first routine review*. **Replaces** "abandoned + a matching strategy → contradict" (M5). No migration; nothing for the user to keep.

- **2026-09-13 · Holds stay drawn poses; the full rig is parked.** Chanté asked whether rigging Lumi completely would save time, and chose to test it on one facing (`art/prototypes/lumi-rig/`, two rounds: a flat wing, then sleeves cut from the drawings). It held the hood, boots and grip exactly and matched the drawn poses at room scale. At 9× at the table, though, the arm read as a floating hand with a dark wrist, no sleeve showed at the hold, and nothing below the tabletop can be reached without bending. Each facing would cost about an hour and two sleeve shapes. Her call: park the rig. Holds stay drawings, organised by hold type, with the hand kept on one side across mirrored facings (for the lantern carry, sw and ssw are drawn in her left hand and s is mirrored). **Replaces** nothing; records why the rig didn't take over from the drawn holds. Prototype only; nothing served, no migration.

- **2026-09-13 · Lumi's lantern is a room object she carries high along Home's low table (`ux/lumi-lantern-table`).** Chanté's answers to the plan. The painted spot is about 70 px from every table edge, out of her reach. So the lantern starts by the back corner (`table.lantern`) and is set down at the left corner (`table.place`, moved), both reached from behind the back-left edge facing se. For now she carries it along the table only (holding drawings sw, ssw and s; carrying it anywhere later needs only more drawings). It is about 40 px (the painting's is 60, half her height) and always lit, its light a separate additive glow that moves with it. The lantern is one piece, the same pixels on the table and in her hand, hung from a measured grip with her fist drawn over it. A composite at the stand showed the tabletop at her chest, so she reaches up and carries it high (fist between chin and eyes), not at her side. **Replaces** the lantern painted into the `table` layer and the proposed `table.place` (760, 640). Prototype only; nothing served, no migration.

- **2026-09-13 · In a room, Lumi's size is the room's; the 150px limit is the corner's.** Chanté, after the room scale landed: "Size of the room is fine. She looks sharp." `art-direction.md` §2 now says she is 150px in a corner and the room's size standing in a room, so she can be drawn larger on a big window. Her 176×208 cells hold up, so there's no 2× cut for now. Resolves the question left open in *In a room, Lumi is the room's size, not the window's* below. Docs only; no migration; nothing for the user to keep.
- **2026-09-13 · A local-only test user for driving the signed-in app (`chore/dev-test-user`).** For the mobile pass on the iOS Simulator, where Claude can't sign in (no creating accounts, no typing passwords or codes). Chanté chose it over making a Clerk test user herself or skipping the signed-in pages. `COHERENCE_DEV_USER=1` under `npm run dev` makes every request one fixed user (`clerk_user_id` `dev_test_user`, "Test"): the proxy stands aside and `requireUser()` / `requireVisit()` answer with its row, created on first use (`src/lib/dev-user.ts`, `architecture.md` → Env). **Guarded twice:** ignored unless `NODE_ENV` is `development`, and a production server with the key set refuses to start (`validateServerEnv`), both tested. **Traded:** its rows are real rows in the production database, and with no Clerk session the header shows *Sign in · Sign up*. **Chosen over** a Clerk sign-in token minted from the Backend API (a credential in a URL, and impersonation by another name) and Clerk's `+clerk_test` emails (still a code typed to authenticate). **Replaces** nothing: until now only a real Clerk session reached a signed-in page.
- **2026-09-13 · In a room, Lumi is the room's size, not the window's (`chore/library-large-chairs`).** Chanté noticed the Library scaled with the window while Lumi didn't: at a fixed 150px she was 171 of its painting px at 1100×900 and 75 at 2560×1440, so her size against the furniture depended on the window. Of scaling her with the room, scaling within limits, or no longer scaling the room, Chanté chose the first. Her height is `--lumi-h`, a length in the room's own pixels times its fit (`--px`), set where her spot is mapped (from 1100px, landscape): the Library 134 (Chanté: "try 134", her look at 1440×900) and Today 160 (its look at 1440×900). `LumiCompanion` sizes the figure and its sprites from the variable (150px in the corner everywhere else); the sprite's picture and her shadows and light are already percentages of the box, and the feet offsets that place her (58px and 14px of the 150px box) and the dev cue strip's gap scale with it. The bubble measures her, so it follows. **Traded:** on large windows she is drawn bigger than her 176×208 cells and softens (about 267px tall at 2560×1440, over 500 device px on a 2× screen); a 2× cut from the 768×1024 drawings is the fix if it shows. `art-direction.md` §2 still says the largest Lumi is 150px, in a corner: raised with Chanté, not changed. Replaces her fixed 150px in the rooms. No migration; nothing for the user to keep.
- **2026-09-13 · The Library serves the isometric room with full-size armchairs.** Chanté's ask: her re-render `background-iso-largechairs.png` becomes `scenery/library/background.png`, and the isometric painting it replaces is archived as `art/archived/scene_mockups/library-background-iso.png`. It is a new render, not an edit: the reading circle's armchairs are as large as the alcove's, the circle is laid out a little differently, the tiled floor runs to the canvas edges with the dark ground only above the walls, and the room is framed about 6% smaller (a landmark fit put the centre and right walls within ~2px of a 0.94 scale; the left wall fits no single transform). So `public/library-room.webp` and the scene's inline placeholder are regenerated, the scene's colour follows the new ground (`#231912` → `#332218`), and every painting coordinate the page uses is measured again on it: Lumi's spot (1060, 745) → (1085, 755), still the open floor right of the circle in front of the stair bay; the three plaque faces and the table slip (`components/library/room-slots.ts`, shown only in debug mode). **And its walls reach the window's edges** (Chanté's ask, the same day): a cover fit of the whole canvas left floor showing past the walls on both sides, so the Library is now scaled to the span of its corner posts (painting x 128–1412: `max(100vw / 1284, 100vh / 1024)`), centred on them and on the painting's middle. Wide windows crop a little top and bottom; a window narrower than 1284:1024 fills its height, as before. Lumi's spot, the plaque stage and the placeholder share the fit (`--library-px`, `--scene-cx`); Today keeps its cover fit. The spatial map (v3) is not redrawn: its manifest now names the archived painting it was measured on, so its maps still draw true, and re-measuring it waits until the map is next needed. Replaces the served painting of *The Library is served from `background-iso.png`* below. No migration; nothing for the user to keep.
- **2026-09-13 · Lumi's avatar is Chanté's painted medallion (`ux/lumi-avatar-medallion`).** Chanté asked for her painting `art/lumi/avatar.png` as the chat avatar, "just the circular image", with none of the white border. `LumiAvatar` now shows `public/lumi-avatar.webp`, which `scripts/cut-lumi-avatar.py` cuts from the painting to its circle. It used to show a head from `lumi-heads.webp` in a forest circle. The `expression` prop went with the heads; no caller passed one. The heads sheet is still cut but no longer shown. The `.medallion` ring (a 2px paper gap and a brass hairline) went too: Chanté still saw white around the avatar, and she wants the painting to fill the whole space. Replaces *2026-09-11 · Lumi avatar is the hooded figure*.
- **2026-09-13 · Node 22 everywhere, pinned with `engines` (code review E8; `chore/strictness`).** Chanté: "Yes to pinning to node 22." `.nvmrc`, CI, `@types/node` and local dev were on 22 while the Vercel project ran production on 24.x, a major no check had run. `"engines": { "node": "22.x" }` in `package.json` (and the lockfile's root entry, the only lockfile change) overrides the project setting. Moving to 24 later is one deliberate step across CI, `.nvmrc`, `@types/node` and `engines`. **Replaces** the hold on `engines` in *Pin Node* (`dev-hygiene.md`, code review D12).
- **2026-09-13 · Preview deploys are not set up (code review E7; `chore/strictness`).** Chanté: "Let's skip preview." Preview holds only `OPENAI_API_KEY`, so a preview deploy fails at startup naming the missing keys, by design. Setting it up would take the Clerk development-instance keys (the live ones refuse `*.vercel.app`) and would share production's database; the steps are in `pre-prod.md` → Environment variables. **Replaces** "scope every variable to Production and Preview".
- **2026-09-13 · The canon states Lumi's window as roughly 30 to 40 messages (code review E6; `chore/strictness`).** B9 (below) made the window 30–39 messages so the history caches, while `philosophy/lumi.md` still said "the last 30". Chanté chose to update the canon to match rather than hold the code to 30. `lumi.md` §§5–6 and the *Coherence remembers* implication now say roughly 30 to 40; the brief doesn't state the window, so it was reviewed and re-stamped, its text unchanged. Product entry: `living/decisions.md` → *Lumi sees roughly the last 30 to 40 messages*. **Replaces** the note in B9's entry that the canon still said 30.
- **2026-09-13 · The context block rides last, on the newest user message; the chat window's start moves in steps of ten (code review B9; `fix/ai-and-routes`).** The chat request was `[persona, context block, …30 messages]`: the block changes every turn and the window dropped its oldest two every turn, so only persona + tools ever cached (a live turn before: `in=29948 cacheRead=14920 cacheWrite=15022`). Now `instructions: [persona]`, then the window, then the block as a marked text part after their words in the newest user message, built per request and never stored (`core/ai/prompt.ts`). The window keeps 30–39 messages, starting at a multiple of ten from the conversation's first, so its bytes hold for about five turns. Every model call has its own `promptCacheKey`. **Chosen over** a trailing system message (the Anthropic provider sends one only behind the `mid-conversation-system` beta; one layout for both providers keeps the model evaluation like for like) and a separate trailing user message (the same bytes, one more turn to explain). **Traded:** the block is now a user-role part rather than a system message; the lead line says it's the app's, and a `<context>` they type is defanged. Lumi sees at least the 30 messages she did, up to 9 more (the canon said "the last 30" until *The canon states Lumi's window as roughly 30 to 40 messages*, above). Measure with the `[chat] tokens` line; roll back by returning the block to `instructions`.
- **2026-09-13 · Consolidation makes one model call per trigger, and a short visit stays its own episode (code review B18; `fix/ai-and-routes`).** One chat turn logged three `[consolidate] {"status":"done","messages":1,…}` lines back to back: Home ran up to three passes, a chat turn one more, and every finished sitting is a stretch. `consolidateAfter` now makes at most `passes` model calls a run (default 1, Home included) and moves past stretches with nothing to keep — no user text, or under 60 characters — in the same run, which costs a few queries and no model call, logged as one line. **Not taken: a minimum stretch size** (gathering short visits until a stretch had several messages, or waiting hours before a short one went alone). It was built and dropped the same session: an episode is "a short memory of each visit" (the persona's *The Library*, `architecture.md` → Recent memory, `philosophy/lumi.md`), and merging visits would change what Lumi is told her memory is, a persona change this branch doesn't make. The cost it aimed at is bounded by the pass cap; revisit if a day of one-line asides shows up as many model calls. **Replaces** "up to three stretches per run by default".
- **2026-09-13 · "Their word" must carry what they meant, and it is a flag, never actor `user` (code review B4, B2; `fix/ai-and-routes`).** `findTheirWords` used to accept any 8+ character run of whole words from their last 8 messages, so two words lifted from a long message ("the book") unlocked `user_said`, and the Library tools then acted as `user`, which skips the forgotten check. Now a quote needs three content words of two letters or more, or at least half the content words of the (short) message it's in: "keep that for the book" and "forget that" pass whole, a fragment of a longer message doesn't. The Library tools write as `lumi` with `theirWord` (events carry `their_word: true`). A checked quote lifts the forgotten check, as actor `user` used to — asking again in their own words brings back a thread or note they had her forget — while an inferred write, a fragment or a consolidation run always meets it; `shelve_thread` still makes the placement theirs (`shelved_by = user`) only on checked words. (A first pass kept the forgotten check even on their words; the coordinator's review on the branch restored their ask, now that the quote check is strict.) `remember` already worked this way (`user_said` skips it). Belief corrections and forgetting keep actor `user` (only the user deletes, `memory.ts`) — it now rests on the stricter match. Chosen over a model-free share-only rule (a long quote from a long message is real) and over requiring the whole message (brain dumps would never match). **Replaces** the 8-character rule of *Lumi's memory: their word checked in code*.
- **2026-09-13 · Plan with Lumi scrapped; stated priorities kept (`feat/priorities`).** Chanté, after approving the parked `feat/plan-together` and then reconsidering: "Let's actually scrap plan with lumi", then "Keep priorities". Only the priorities half ships, rebuilt on current `main` from `land/plan-together` (kept as a reference branch): the `priorities` table and `core/domain/priorities.ts` (scope stored as said — `week` with that week's Monday, or `while` — and holding derived at read time, so a week's priority lapses on its own); `hold_priority` / `let_go_priority` in the main tool list (not the mail tools, which are off), each re-cutting the path with `reason: priority`; the context block's *What they said matters* section (id · this week, next week or for a while · their words · the intention it names), read from the snapshot; the persona's line for the tools and the section's name in its ids line (a one-time change to the cached prefix); and the day planner reading the priorities that hold today. **Left out**, all Plan with Lumi: `core/plan-ways.ts`, `PlanTogether.tsx`, the ways' signals in `PlanSection.tsx` and the `.today-plan` CSS, the `?plan=<way>` handoff in `Conversation.tsx`, and the chat route's `planWayNow` with the per-way briefs in `context.ts`; `core/time.ts → localWeekday` too, which only the ways used. Nothing on Today mentions planning with Lumi. **Why a table, not a belief kind:** a priority is the user's word with a temporal scope and supersession, which beliefs (confidence, evidence, inference) don't model. **The migration** is `0007_priorities.sql`, byte-identical to the file recorded while `land/plan-together` was in review: the table was already in the database from an earlier attempt and matched it exactly, so it was recorded rather than run (row 8, sha256 `029938f3…3002`, `created_at` 1789336938985), and `npm run db:migrate` is a no-op. Product entry: `living/decisions.md` → *Plan with Lumi is scrapped*. **Replaces** *Plan with Lumi stays parked* in *Today's card does its own organising* (below); the rejection of a Replan button (`ef-burden-log.md`, 2026-09-12) stands.
- **2026-09-13 · An explicit OpenAI cache breakpoint after the persona (`feat/lumi-environment`).** Chanté noticed long waits before Lumi's replies. The dev log showed `cacheRead=0` at the start of every turn (13–14k tokens in), though `promptCacheKey` was set. Probes against `gpt-6-astra` with the real tools: identical prompts hit, but turns whose context block differed (it always does: the time is in it) wrote the whole prompt (`cacheWrite≈8,400`) and read nothing — GPT-6 keeps a cache entry only at a breakpoint, and the implicit one is the end of the prompt. A 30-minute `promptCacheOptions.ttl` changed nothing. `cachedPrefixOptions.openai.promptCacheBreakpoint = { mode: "explicit" }` on the persona system message: turns 2+ read 8,281 tokens and wrote ~100. Cheaper as well as warmer (writes shrink from the whole prompt to the tail). Every call that marks the persona with `cachedPrefixOptions` gets it (chat, plan, Break it down, leads, reflection). First-word time in a small probe stayed ~1.5s; the live gain on a 13k prompt is to be read off the dev log's new `firstWord`. Replaces relying on OpenAI's automatic prefix caching ("OpenAI caches long prefixes on its own"). No migration; nothing for the user to keep.
- **2026-09-13 · Lumi knows the app, and the page each turn comes from (`feat/lumi-environment`).** Chanté: "I'd like Lumi to be aware of her environment and the app's functionality." Before, she knew neither: asked "how do I…" she could only guess, and "this one" from the bubble on Today had no page behind it. Two parts. **Where they are:** each client's transport sends `where: { path, via }` with the turn (the pathname at send time and the held chat's slot); `core/places.ts` → `parseWhere` accepts only a known place and way in, and the context block says it in one line under the time. Not stored — it describes the moment; missing or malformed is no line. **The app:** the persona gains *The app, and where they are* — the places and what can be done by hand in each, what isn't built (so she says *not yet* instead of inventing a button), how to use the place (when it changes the reply; never remarked on; the page, not the screen: `spaces.md` §31) and the rooms (lightly, never a theme: §10, §46). A one-time change to the cached prefix of ~2,050 characters (roughly 450 tokens, ~10% of it); Insights's line only while `MAIL_ON`. The brief already says she lives in the places; this is the *how*, so the brief is unchanged. It is the smallest form of *space context* (AI & IA §23): what the block selects doesn't change by space, and open question 24 stays open. No migration; nothing for the user to keep. Merged after *Today's card does its own organising* (below): the map names no focus sessions and no Start with Lumi, and says Not this and Break it down work on Today's card. Merged again after *Files can be shared with Lumi* (below): Home's line names the paperclip, the bubble is words only, and attaching files is off *not here yet*.
- **2026-09-13 · Files can be shared with Lumi in the conversation, and aren't kept.** Chanté's ask, with her calls: photos, PDFs and text files; read, not kept; Home's composer only. The file rides inline in the one chat request as a data URL on an AI SDK file part — no upload endpoint, no storage bucket, no table, no migration. `/api/chat` checks it (`sharedFilesProblem`: at most 3 files, a kind Lumi reads, inline only so no provider is sent to fetch a URL, per-kind and total byte limits under Vercel's 4.5 MB request body), gives Lumi the files on that turn (`readSharedFiles`: a text file becomes a text part, since OpenAI's Responses API accepts only images and PDFs as files), and saves the message with a `data-shared-file` note (name, kind) in each file's place (`noteSharedFiles`); on later turns `convertDataPart` reads the note to her as shared and not kept. The browser redraws a photo as a JPEG of at most 1600px on its long edge first (`components/chat/attach.ts`). `CoherenceUIMessage` gains its data-part type (`CoherenceDataParts`). Considered and rejected: Supabase Storage with a table and deletion rules (an archive to keep and explain; Chanté chose not to keep); keeping the file in the history sent on later turns until reload (megabytes resent every turn, for a memory that vanishes on reload anyway). Replaces *Add file* as a dead control (hidden 2026-09-12, its `+` removed 2026-09-13). No migration.
- **2026-09-13 · Today's card does its own organising; focus sessions are out of the product.** Chanté: every option on Today sent you to Home's chat, and she wanted the organising kept on Today; asked, she chose to "get rid of the start with lumi and focus sessions for now", and to do *Not this* and *Break it down* on the card without a conversation (Plan with Lumi stays parked — the small control was easy to miss, and the options weren't right). **Not this**: `PATCH /api/intentions/[id]` `{ action: "decline", reason }` records the decline and calls `recutTodaysPlan(…, "declined")` synchronously (`maxDuration` 60), so the card can show *Finding something that fits…* and then the new Right now; on review it took 13.4s on the card (a full model re-cut before the response), so the card now changes at once: `recutAfterDecline` picks the next Right now in code from After that by the reason (`planAfterDecline`) and saves it with a fixed line per reason (`DECLINE_NOTES` → `plan.note`, which `advancePlan` drops so it can't sit on the next Right now); `after()` re-cuts the rest with the model, pinning that Right now (`PlanInputs.keep`, with `justDeclined` for context), and saves only if the latest plan row is still the one just written, so a Done or another decline in the meantime is never overwritten. A model-written note was tried first and dropped: it could only arrive with the slow re-cut. With nothing in After that, the model chooses before the response. **Break it down**: `POST /api/intentions/[id]/steps` makes one structured call (`core/ai/breakdown.ts`, persona prefix + `BREAKDOWN_RULES`, effort low) and `clampSteps` tidies the result; a chosen step goes through `PATCH … first_step` → `next_action` plus `setFirstStepInPlan` (`withFirstStep`, `reason: first_step`, event `plan.first_step`) — no re-cut, so the rest of the path stays put. Considered and not taken: opening Lumi's speech bubble with the handoff message (still a conversation, which Chanté didn't want for these), and re-cutting on a chosen step (it would move the one thing). **Sessions**: removed the session bar, `/api/session`, `start_focus_session` / `end_focus_session`, their persona section, the *Just now* lines for check-ins, starts and declined messages, the abandoned-session greeting and sweep (`resolveSession` out of `loadSnapshot` and Home), the *Body double* quick start, the chat route's session handling and reflection hand-off, and the `.session-*` CSS. Kept, unused: `focus_sessions` and its rows, `core/domain/sessions.ts`, `core/focus.ts`, `reflectOnSession`, the ledger's session lines (old transcripts still render them) and `users.preferences.session_minutes` / `check_in_minutes`. Old tool calls to the removed tools in stored transcripts still convert (the SDK looks the tool up optionally). The persona and tool descriptions change: a one-time change to the cached prefix. Replaces the handoffs into chat (`today.md`, 2026-09-12) and M5's product surface. No migration.
- **2026-09-13 · The favicon is Chanté's compass star, not Lumi's head.** Chanté's ask: her `art/scenery/library/icon.png` as the favicon. PNG and ICO from `scripts/make-favicon.py`, not an SVG: the source is a painting, and its hairlines need thickening by hand at 16px. Replaces the Lumi SVG favicon from *Graphics pass: the book gets its engraving* (2026-09-12). No migration; nothing for the user to keep.

- **2026-09-13 · Home renders whole, like Today; the first paint is the room, not ivory.** Chanté: "a big flash of white and things loading faster than others." Home's page wrapped everything in a bare `<Suspense>` (from M3), and `LibraryDebug` inside it is async, so the whole view (the room, the palette switch, the chat) streamed into a hidden `S:0` and was revealed by `$RC` at the very end of the document, behind React's 300ms reveal throttle. Until then the shell painted on the ivory paper. The boundary is gone: nothing on Home needs to stream (the page already awaits its reads), and `next build` is fine without it. The same pass smooths what still arrives late: each room's scene carries a 24×16 blurred copy of its painting (~220 bytes, in the stylesheet), so the painting's fade lands on its own colours instead of a flat brown; the account button keeps a 28px slot (`.auth-slot`), so the clock no longer slides 60px left when Clerk loads; and the clock fades in when it is set. **Replaces** Suspense on every Home load and the flat scene colours under the fade.
- **2026-09-13 · The walk rig runs on Home's layers (`ux/lumi-walk-layers`).** Chanté approved moving the walking rig onto the layers ("robust occlusion around furniture" is what she cares about). The walk page draws the plate, every shadow, her contact shadow, then the layers and her in the home-layers depth order, ported from `depth.py`; paths use `layers.json`'s walkable floor; the tour is `art/prototypes/lumi-walk/tour-home.json`, on the proposed anchors. `room-home.json` (hand-traced cut-outs and a bounding-box test) is retired. `layers.json`'s `lumi.reach` goes from 37 to 50, with `depth.py`'s `REACH`: the rig's side-on drawings reach 43.4px from her feet at rest, more than the stand-in's 36.5px, and at 37 the page's `?occlusion=1` count found 170px of her overlapping a layer the rule could not rank. `halfWidth` stays 17 (no column disagreement). Landed and pushed 2026-09-13 (`5433a9f`); Chanté: "This is looking like a really good start." Next, her choice: Lumi picks up the lantern and sets it down on the low table. **Replaces** the hand-traced map in the walk test.
- **2026-09-13 · A conversation harness: Lumi's real tools against a throwaway database (`scripts/conversation-eval.mjs`).** Chanté's go-ahead, after review feedback that nine single replies can't show judgement over a conversation or whether she acts.
  - **How it runs:** each scenario gets a fresh PGlite (`openTestDb`, every migration applied), a test user, and seeded intentions, beliefs and a sitting. Each turn is assembled the way `/api/chat` assembles one (`loadSnapshot`, `listRecentActivity`, `selectBeliefs`, `selectLibrary`, `buildContextBlock`, `buildTools`) and runs `generateText` with `stopWhen: stepCountIs(5)`. Seed events are moved out of *Recent changes*, so she doesn't read the setup as her own doing.
  - **Checks:** code over tool calls and database state (`must`), plus signs for the grader (`flag`). Voice and Use stay manual.
  - **Comparing the brief:** `--both` runs each scenario with and without the brief at the same `--at` clock. The brief is taken out of `PERSONA` by string, and the script fails loudly if `persona.ts` stops placing it that way. It writes `blind.md` (the two shuffled as A and B, token counts left out) and `key.json`.
  - **Output** goes to the OS temp dir, never the repo.
  - **Why a script, not a vitest suite:** it spends model calls and its judgement is graded by hand, so it runs on purpose, like `voice-eval.mjs`.
  - **A known cost:** the assembly is copied from the route, not shared. If what a chat turn carries changes, the script has to follow; extracting one `assembleTurn` for both is the follow-up.
  - **Also fixed:** `context.ts` still told her "don't mention the length unless they do" on any long gap, which *Lumi may name time away* had replaced. It now says what the re-entry line says. `context.test.ts` only checks for "long gap".

  No migration; nothing for the user to keep.

- **2026-09-13 · History pointers stay without foreign keys; episodes keep `thread_ids` as jsonb.** From the code review (A22, A23), `fix/data-integrity`.
  - **No FKs** on `summary_through_message_id`, `through_message_id`, `source_message_id`, `supersedes_id`, `superseded_by_id`. They point into history, and the target may go first: forgetting a belief deletes its chain, and a message may be trimmed. An FK would block that delete or cascade one nobody asked for. Code treats a missing target as normal; `unconsolidatedMessages` falls back to the latest episode's end instead of re-reading the conversation.
  - **No `episode_threads` join table.** A GIN index (`episodes_thread_ids_idx`, `0006`) serves the `@>` filters. At V1 scale (one user, tens of episodes) the array does the job, and scrubbing a forgotten thread's id is rare. Revisit if a thread's visits become a hot read.

  **Replaces** nothing; records why the review's suggestions were not taken.

- **2026-09-13 · The pool still warms all ten connections on a cold instance.** Code review A15 asked whether a one-query route should open ten. Kept: the warm-up is what took Today's parallel reads from ~510 ms to ~90 ms (measured from Vancouver), warming six saved nothing, and an instance is reused across requests, so the next page on it gets the benefit. Ten idle connections per instance sit well inside the pooler's limit at V1 scale. The dev half of A15 is fixed: the pool lives on `globalThis`, so HMR no longer opens another pool on every re-evaluation. Revisit when instances multiply (many users) or the pooler's connection count shows pressure.
- **2026-09-13 · Lumi may name time away; the persona and the re-entry context line say so.** Chanté's decision on open question 17 ([product decision](living/decisions.md)). `persona.ts` → *What you're for* 4 and *Coming back* replace "never … say how long it's been" with: only when it helps them get their bearings, never so the time away sounds owed. Counts of what piled up stay banned. `context.ts`'s re-entry line changes the same way. Both sit in the cached prefix or the block, so the first call after deploy misses the cache once. **Left as it was, deliberately:** `plan.ts` still tells the day plan never to name the gap, because that line is page copy on Today and the decision covers conversation. No test asserted the old wording. Voice eval run 6 re-ran scenario 6 on the new persona. No migration; nothing for the user to keep.
- **2026-09-13 · A public privacy policy page, `/privacy`.** Publishing the Google consent screen needs a privacy policy link on a domain under Authorized domains. Chanté's other Clerk projects never needed one: Clerk's development instances use Clerk's shared Google client (Clerk's own policy), and Coherence has had its own client since mail. The page is static and needs no user; it joins the sign-in pages in `PUBLIC_PREFIXES`, so the proxy lets it through signed out and the nav stays off it (as on the auth pages, even for a signed-in visitor). Linked under the sign-in and sign-up cards. It names Chanté Blais and `chanteblais@gmail.com` (her choice). The copy stays in the page, not `src/core`: it's a policy, not Lumi's voice. Writing it found one stale claim: voice audio "never leaves the device" held only for the local engine. The Web Speech engine's audio goes to the browser's maker, and `architecture.md` and `features.md` now say so. **Replaces** only the auth pages being public.
- **2026-09-13 · Mail is switched off behind one constant.** Chanté asked whether turning off mail would stop the warning on account creation, then asked for the app side. `gmail.readonly` is a restricted scope: while the app asks for it, the Google consent screen can't be published without Google's review, so Google sign-up blocks anyone off the test list and warns the rest. `MAIL_ON = false` in `core/email/types.ts` leaves out the mail tools, the persona's mail line, *Their mail* in the context, the Insights nav item and page (redirects Home); the code, the `leads` table and its rows stay. Chosen over deleting the feature (it may come back, and deleting would lose the leads' design) and over an env var (a constant keeps the cached prefix fixed per deploy and needs no Vercel setting to be right). Google side: `pre-prod.md` → *Publish the Google consent screen*. Product entry: `living/decisions.md` → *Mail is switched off, for now*. **Replaces** mail on for everyone with Google connected (2026-09-12, below).
- **2026-09-13 · Rooms become an empty plate plus furniture layers cut from their paintings (a Home spike, `ux/home-layers`).** Chanté, asked whether to refine the hand-traced map or rebuild the rooms from layered items, chose to **cut the furniture out of the existing painting and inpaint the floor behind it**, over refining the map or repainting the room.
  - **Why:** the walk test's map was the weak part (close behind the low table the tabletop covered most of her, and a bounding-box depth test got corners wrong). With layers, occlusion comes from depth-sorting Lumi against each item's ground footprint, the walkable floor falls out of the footprints, and anchors belong to items.
  - **The method** (`art/prototypes/home-layers/`): eight layers (the low table with what's on it, both floor cushions, the armchair, the stool, the desk, the chair, the balustrade), traced as polygons. The stair, the right-hand bookcase and the log basket stay in the plate as blockers. The floor behind each cluster is inpainted with OpenAI's edit API and a mask (10 images, about $0.54). The inpaint is used only under what a layer covers; the floor an item only shades keeps the painting's own pixels, relit, and the darkening becomes a `-shadow` layer drawn under her. Each layer is matted against the plate and de-matted. The depth rule: an item draws over her when its ground polygon has a point in her feet columns more than 4px lower on the page than her feet. It lives in `depth.py` and the viewer's JS, which agree on 2,728 cases.
  - **The cleanup** (Chanté saw fringes in Plate only and asked whether they would clip her): the matte had kept floor where the inpaint disagreed with the painting at an edge. A trim now removes floor-coloured pixels from every layer. Sweeps in 8px steps round every piece count the floor pixels drawn over her, which went from 11,806 (table), 2,291 (desk), 1,719 (balustrade), 1,488 (green cushion) and 1,124 (armchair) to 0. The rebuilt room still matches the painting (mean error 0.031 of 255).
  - **A prototype:** `art/scenery/home/plate.png`, `layers/` and `layers.json` are not served, and the walk page (`art/prototypes/lumi-walk/`) moved onto them the same day (the entry above).

  **Replaces** the proposal of a depth map per painting (`animation-pipeline.md` → Next animation). No migration; nothing for the user to keep.

- **2026-09-13 · The Library's categories and threads show only in a hidden debug mode.** Chanté: "a debug mode button somewhere hidden that, when turned on, shows the categories and threads as parchment over the rooms". She chose debug-only over keeping the plaques, and five taps on the wordmark over a shortcut or a dev-only chip.
  - **The switch:** `Sidebar` counts taps on the wordmark within 2 s. The first still navigates Home and the rest `preventDefault`. The fifth toggles the cookie `coherence_debug` and reloads the page. A `router.refresh()` was lost behind the first tap's navigation to Home in the click-through.
  - **The overlay:** a server component, `LibraryDebug`, placed by the room pages (Home, Today, the Library), reads the cookie and the threads and renders the parchment. It isn't in the root layout, because the layout has no user without redirecting on the public pages.
  - **The Library page** reads the cookie too, and renders the bookcase names and the table slip only when it's on. `/library/[id]`, `/library/[id]/book` and `/library/table` still answer by URL.

  It works in production. **Replaces** the plaques showing to everyone on `feat/library-sections`.

- **2026-09-13 · Claude applies migrations.** Chanté: "I'd like you to apply migrations from now on. Feel free to proceed with any migrations that need to be applied." Claude applies migrations to the database in `.env.local` — the Supabase database production uses — once the branch's check passes. It reads `__drizzle_migrations` and the schema first, and applies an add-only migration during review but anything `main`'s code would trip on at landing. A destructive migration still gets her yes (Claude's guard, hers to lift). The SQL is still printed in the summary. The first run applied `0005_thread_shelves` and recorded the hand-applied `0003` and `0004` in the same transaction (hashes of `0000`–`0002` checked against the files first), so `npm run db:migrate` is a no-op again. **Replaces** "Chanté reviews and applies migrations herself" (`CLAUDE.md`) and "apply to prod at merge+deploy time" (`branching.md` 6).

- **2026-09-13 · Threads that become categories are sections of the Library: one parent pointer, sections derived (`feat/library-sections`).** Chanté: "Threads that become categories should gain their own section in the library… click on the different sections and see all the books that are there. The title of the books will be all your threads", with a mockup of a thread open as a book (Library › Coherence › Memory & Continuity › AI Continuity & Identity). `threads` gains `parent_id` (self fk, set null) and `shelved_by` (`0005_thread_shelves`); a section (top-level thread holding threads), a shelf (a thread holding threads inside a section), a book and a loose thread are derived by `buildShelves`, never stored, and ordered by `created_at` so places are stable. Three levels at most. Consolidation proposes `shelve` entries, clamped to loose threads, ≤ 4 a run, and one gathering thread a run that must gather 2; `shelve_thread` moves on the user's quoted words and marks the placement theirs, which Lumi and consolidation never undo. The Library block and `open_thread`/`search_library` say where a thread sits. *Why one pointer and not a Collection/Thread Group table:* her words make the category a thread itself, and the spatial map's question 7 already had a Thread in one shelf position; which painted slot a section occupies stays presentation (fill order in the UI, not stored), so answering the slot questions later needs no migration. **Replaces** the flat Library ("how the Library room shows threads isn't modelled yet", `0004`) and the note in `architecture.md` that the Library had no page.

- **2026-09-13 · Lumi's brief: the canon synthesised for her, inside the cached prefix.** Chanté's ask ([product decision](living/decisions.md)). `docs/philosophy/lumi-brief.md` is the source; `npm run brief` writes the text between its markers into `src/core/ai/brief.ts` (generated, committed), and `persona.ts` interpolates it after the opening paragraph, so the chat route, the day plan, mail leads and `scripts/voice-eval.mjs` all carry it. Reflection (`REFLECTION_RULES`) doesn't: it's a narrow structured step. **A generated module rather than reading the markdown at runtime**, so `src/core` needs no filesystem access and the serverless bundle needs no traced docs file; **rather than text typed into `persona.ts`**, so the brief is reviewed as prose with its sources beside it. **Review is guarded:** `npm run check:brief` (inside `check`, so CI too) fails when `brief.ts` differs from the doc, or when a source's git blob hash differs from the one stamped at the last review (`npm run brief -- --reviewed` stamps them), so the branch that moves the canon reviews the brief before it lands. A review older than 30 days is a note, not a failure. The cached prefix goes from 2,049 tokens to 3,994 on `gpt-6-astra` (voice eval run 5; the persona's "under ~2k tokens" in `architecture.md` is replaced). No migration; nothing for the user to keep.

- **2026-09-13 · Transactions run on a reserved connection (`fix/consolidate-transaction`).** Opening Home logged `[consolidate] failed Error: UNSAFE_TRANSACTION: Only use sql.begin, sql.reserved or max: 1`, and chat turns hit the same, so no visit had been folded into episodes or the Library since consolidation landed. Cause: `max_pipeline: 0` (*The database pool never pipelines*, below) short-circuits the callback in which the `postgres` driver's `sql.begin` reserves its connection, so drizzle's `db.transaction` sent BEGIN on an unreserved pooled connection and the driver refused it. Consolidation's apply step is the only transaction in the app. `connect` (`db/client.ts`) now replaces the pool's `begin` with `beginOnReserved` — BEGIN, the work and COMMIT or ROLLBACK on `sql.reserve()`, savepoints for nesting — so every `db.transaction` keeps its atomicity and `max_pipeline: 0` stays. Considered and not taken: `max_pipeline: 1` (lets one query pipeline onto a busy connection, the hang that decision fixed); a helper every call site must remember (the next `db.transaction` would fail the same way); dropping the transaction (consolidation's watermark claim and its writes must land together). Tested over a real Postgres wire: `@electric-sql/pglite-socket` (new dev dependency) serves PGlite to the app's own pool in `client.db.test.ts`. No migration; nothing for the user to keep.

- **2026-09-13 · The isometric Library is `scenery/library/background.png`; the frontal room is archived.** Chanté's ask: `background-iso.png` is renamed `background.png`, the one-name-per-place convention of `art/scenery/`, and the frontal reading room it replaced moves to `art/archived/scene_mockups/library-background-frontal.png`. Nothing changes on the page (`public/library-room.webp` is the same image). The spatial-map manifest and its maps point at the new name; `art/prototypes/shadow-mock.py` keeps mocking the frontal room from its archived path, with the spot it was measured at. No migration; nothing for the user to keep.

- **2026-09-13 · A date on a row in Lists, typed as it's said; a day alone is not an appointment.** Chanté: there's no way to add a date to a task; click the empty date column and it should format itself once sent, with no calendar dropdown, and leave times for now. The row's date is a button (`DateCell`); the field posts `{ action: "date", text }` to `PATCH /api/intentions/[id]`, and `parseDueDate` (`core/due-date.ts`, pure, tested) reads today/tomorrow, weekdays (*next fri* = the one in the coming week), *in N days/weeks/months*, month-and-day either way round with an optional year, `9/30` (a part over 12 is the day, otherwise month first), ISO, and *the 24th*. Vague spans (*next week*, *the weekend*) are refused rather than turned into a day. Read in code rather than by the model, so the date lands at once and a typo can't become a date nobody said. **A day with no time is stored as 00:00 local** (`startOfLocalDay`, DST-safe) **and `dueOn` now skips it**: before, any task dated today was a fixed-time commitment — shown under Later with a clock time and never Right now — so an essay due Friday couldn't be started on Friday. A flag column was considered and not taken: no migration while two other branches carry `0003`, and times aren't being set yet; the convention can become a column when they are (a real 00:00 appointment is the one thing it can't say). The planner flags a candidate *due today*; the context block shows a day-only date without a time and says *they changed the date on "X" in Lists*; the `due_at` tool descriptions tell Lumi the same convention (a one-time change to the cached tool prefix). No migration.

- **2026-09-13 · The Library is served from `background-iso.png`, an isometric room.** Chanté painted the reading room as an isometric cutaway on a dark ground and asked for it to be put in. `public/library-room.webp` is regenerated from it; the scene's colour becomes the painting's ground (`#231912`) so the fade-in has no seam; Lumi's spot moves to the open floor right of the reading circle (`--lumi-x: 1060; --lumi-y: 745`), where everything she covers is further back. The spatial map is redrawn as v3 on it (`art/scenery/library/spatial-map/README.md`): slots as wall faces, close-ups un-sheared from the painting, constant scale; the projection question is answered (the Library joins the isometric rooms of bet 5). Lumi keeps her usual size until Chanté decides her size in this room. The frontal `background.png` stays in `scenery/library/`. Replaces the served painting of the entries below. No migration; nothing for the user to keep.

- **2026-09-13 · Recent conversation lives on, and what mattered is filed in the Library (`feat/lumi-library`, stacked on `feat/lumi-memory`).** Chanté's direction: Lumi's memory holds recent conversation and is periodically revised, with meaningful things recorded in their rightful category — the Library — so a subject she talks about often (a book she's writing) comes back with a quick summary and the relevant parts of a growing archive; "something I would like to grow and refine as we develop". First milestone: **episodes** (a short memory per visit), **threads** (a persistent subject with aliases and a summary Lumi rewrites) and **thread notes** (idea · decision · question · progress · detail; their word or Lumi's reading; superseded, not overwritten), written by **consolidation**: one model call per finished stretch, clamped in code (a new thread needs its name in two of their messages or three notes; ≤ 2 new threads and ≤ 14 notes a run; ids must exist; quotes checked; screens and tombstones apply) and applied in one transaction that claims the conversation's watermark (`summary_through_message_id`, reused; `conversations.summary` stays unused). Each turn opens ≤ 2 threads it touches (lexical: names and aliases, then words) with ≤ 6 notes, an index of the rest, and ≤ 3 episodes from before the transcript window. Tools: `open_thread`, `search_library`, `add_to_library` (a new thread only on their quoted words), `forget_from_library` (deletes; the conversation is never scrubbed — Chanté). **Replaces** the M6 plan's rolling conversation summary. **Settles the first part of spatial-map question 7 and open question 22** — threads are life-model objects — while Collections, Thread Groups, shelves and any Library page stay open. Not a neural network in the literal sense: a layered, linked memory a model keeps consolidating; embeddings (meaning-based recall) are the next layer. Migration `0004` (three tables, additive).

- **2026-09-13 · Lumi's memory: their word checked in code, beliefs selected per turn, forgetting deletes (`feat/lumi-memory`).** Chanté's brief: continuity across visits — preferences, projects, decisions — with source and time, relevant retrieval instead of the whole store, supersession of stale or contradicted beliefs, a way to inspect, correct and delete, isolation per user, no secrets or sensitive details by default, notes never read as instructions, and chat that survives memory failing. Built on `memory_notes`; no new table or service. (1) **Their word is checked:** `user_said` needs `their_words` found in the user's recent messages, or the belief is held as a guess; guesses start at ≤ 0.6 (v1-gap-plan 2a). (2) **Screens before the insert** (`memory-rules.ts`): secrets and instruction-like text never (a sensitive-details rule — kept only on their word with an explicit ask — was built and removed the same day: Chanté, "let's not worry about privacy yet"); the same thing said again confirms the existing belief; their word for something Lumi guessed supersedes the guess. (3) **Selection per turn** (`memory-select.ts`) replaces "inject all active beliefs, ≤ 40": standing preferences (5) and strategies (3), then lexical relevance to the message, the last few turns and a running session, then the freshest projects and facts, ≤ 12; a guess below 0.5 unconfirmed for 60 days fades from the block (derived at read time); `recall_memory` searches the rest. Embeddings still wait until words stop being enough. (4) **Corrections:** `correct_belief` is the user's revision on their quoted words; Settings' Correct is the same op (v1-gap-plan 2). (5) **Forgetting deletes** — replaces the soft retire with reason `user`: the belief and its supersession chain are removed, `note` is stripped from their `memory.*` events (the one sanctioned rewrite of events: nothing a deleted belief said outlives it), and a `memory.deleted` tombstone keeps one-way content keys so an inference can't bring it back. The transcript is not touched: what the user said stays in the 30-message window until it scrolls out (open question 25). (6) **Framing:** notes are quoted, one line each, under a line saying they are data, not instructions, and never override the rules or the current request. (7) **Failure:** `loadBeliefsOrNothing` — a turn or page carries on without beliefs, and the block tells Lumi not to claim either way. Migration `0003` (`memory_notes.source_message_id`). DB tests run the real migrations on PGlite (new dev dependency, in-process).

- **2026-09-13 · Lists is a sheet from the nav, over the page you're on.** Chanté's ask, with `art/mockups/lists-mockup.png`: a nav icon for lists that brings up all your todos. Asked where the mockup met the canon, she chose a sheet over the page, no counts or Overdue, search plus Add through Lumi (no sort or filter), and the icon after Today (product entry: `living/decisions.md`); on review she moved it below the Library with a soft divider between, to separate the rooms from the tools. The rail now has two breaks, `.nav-tools` (up to 32px) before Lists and `.nav-utility` (up to 64px) before Settings, sharing one budget (`--nav-spare`, the rail's height past 638px) that goes to the tools' break first; the old 584px threshold assumed five rows. Measured on the rail: with six rows and no gaps, Settings' icon sat under the moon's button on any rail under 620px (61px under at the 560px minimum), so below 638px the rows shorten from 50px toward 38px and the icon stays 17px clear down to a ~570px rail (11px at 560). Built as a parallel route slot, `app/@sheet`, in the root layout: `(.)lists` intercepts a soft navigation to `/lists` and renders `ListsSheet` over whatever page is underneath, while a hard load of `/lists` renders `app/lists/page.tsx` (the sheet over the Library's room). Chosen over a client-only modal because the URL, Back and a reload all work, and over a full page because the room you were in stays. A slot keeps its last state on soft navigation, so `[...catchAll]` empties it and the sheet also renders nothing unless the path is `/lists` (a nav click to `/` isn't caught by the catch-all). `/lists` no longer redirects to `/library` (the redirect was a 308, so a browser that followed it may have cached it until its cache clears). The rows are shaped in `core/domain/lists-view.ts` (pure, tested): dates from today on first, then the rest by last touched, so a missed date doesn't top the list. `PATCH /api/intentions/[id]` gains `move` (to one of the user's lists) and `drop`; `updateIntention` and `dropIntention` take a `via`, and the context block says *they moved / let go "X" in Lists* for page actions. The context and persona now say *on Today or in Lists* where they said *in the Library* (a one-time change to the cached persona prefix). Replaces the redirect in *Lists is the Library* and *the list panel comes off* below, for the list view only; the Library stays its room. No migration; logged in `ef-burden-log.md`.
- **2026-09-13 · Idle database connections are kept for thirty minutes, not five.** Coming back to the app after a short break reopened the pool (~0.5s before the first query, and the warm-up again). Measured first: connections held idle 6 and 12 minutes through the Supabase transaction pooler answered in ~70ms with no reconnect, so the pooler doesn't drop them first. `idle_timeout` 300 → 1800 (`db/client.ts`). Chanté: yes. Replaces the five minutes from *Today's path is cut before it's needed*. No migration; nothing for the user to keep.
- **2026-09-13 · A smoother first load: the pool opens whole, and the rooms' paintings start with the HTML.** Chanté asked what would make the first load feel smoother, and chose three: warm the database connections, load the paintings sooner with a fade, trim the fonts. **The pool** (`db/client.ts → warm`) opens all 10 connections the moment it is made: the driver opens a new connection for any query that finds none free (it doesn't wait for a busy one), so Today's ~9 parallel reads each paid ~0.5s to connect after a cold start; warmed, they took ~90ms instead of ~510ms. Warming only 6 measured no better than none. **The paintings** are a preloaded `img` in each scene (`RoomScene`) instead of a CSS background, which the browser found only after the stylesheet (~0.4s after the HTML on a first visit); they fade in over the room's colour when they arrive and show at once when already loaded (an inline script marks them before hydration). **The fonts stay as they are:** both are variable fonts, so the ten declared weights are four files (one per family and style), preloaded and done within ~25ms of the HTML; trimming the weights would have saved nothing. Replaces the painting as a CSS background and a pool that opened on demand. No migration; nothing for the user to keep.
- **2026-09-13 · Fewer waits before a page renders, and pages re-read on Back and on coming back to the tab.** Chanté chose cutting server work over loading screens, on the condition that things reload when anything changes (the Library's signs, later, must change when their sections do). No cookie or token copy of the user was needed after all: `requireVisit()` finds the user and records the visit in one query (`visit()`, `UPDATE … FROM` a copy of the row, which returns the previous visit), where `requireUser` + `recordVisit` were two; Home loads its conversation and its messages alongside everything else instead of one after the other. Nothing is cached, so nothing new can go stale. Two existing ways of seeing a stale page are closed by `FreshOnReturn` in the layout: Back/Forward (Next restores the page as it was left) and a tab left open a minute or more are re-read with `router.refresh()`. The rule is now a key convention (`architecture.md` → *Fresh on every visit*). **Today renders whole when its path is ready** (Chanté: "the UI elements in Today take a second to load"): the three plan parts streamed in under Suspense even when the plan was already primed, so the page showed its header and the dots first and the card ~0.3s of reads plus React's 300ms reveal throttle later. Now the page checks `planIsReady` (the snapshot it would load anyway, cached for the request) and renders without Suspense when the path is cut; only a path still to be generated streams behind the dots. Replaces `recordVisit`, and Suspense on every Today load. No migration; nothing for the user to keep.
- **2026-09-13 · The database pool never pipelines, and the functions run beside the database.** Chanté found page loads slow. Measured: a query sent when every connection of a warm pool is busy gets pipelined onto a busy connection by `postgres`, and the Supabase transaction pooler never answers it; the snapshot's 7 parallel queries on a pool of 5 were enough. `src/db/client.ts` now sets `max_pipeline: 0` (overflow waits for a free connection) and `max: 10`. And the Vercel functions move from `iad1` (Virginia) to `cle1` (Cleveland), the region of the Supabase database (us-east-2), in `vercel.json` (Chanté: "go ahead and move the vercel app"). Replaces the pool of 5 and the default region. No migration; nothing for the user to keep.
- **2026-09-13 · No fade behind Home's composer.** Chanté asked what the shadow on the rug was: it was the composer dock's fade, re-lit for Home as a dark tint. The dock spans the chat column, which stops at the companion lane and runs wider than the ~70% pill, so on the painting the fade was a dark box with a hard right edge. On Home the dock now has no background (`.shell:has(.home-scene) .composer-dock { background: none }`); the pill's blurred glass and the text shadow already hold lines scrolling behind it. The ivory pages keep their paper fade. Replaces the dark gradient from *Home, set in the room*. No migration; nothing for the user to keep.
- **2026-09-13 · On a phone the nav is a bar along the bottom, with the names on it.** Chanté, on her phone: move the nav to the bottom. Below 768px the shell becomes one column over two rows (page, then nav), and the rail's painted body is turned a quarter turn into a 62px bar, its corners chamfered in CSS since the painted star and moon ends are too long for a phone's width. With no hover on a phone, the names sit under the icons rather than on a parchment the compass star opens: one tap fewer to read a place, and it keeps the rule that an icon's name is always reachable. The phone's open/close state (`data-open`, the outside-tap and Escape listeners) is gone from `Sidebar`; pin and lock stay desktop-only. Replaces the phone trial in *The nav is a green rail…* below (a 48px rail, the parchment tapped open). No migration; nothing for the user to keep.
- **2026-09-13 · No nav before you sign in.** Chanté, on her phone: the rail showed on the sign-in page, and it's only needed once you enter the space. `Sidebar` renders nothing on the public pages and the shell drops the rail column there (`.shell:not(:has(> .nav))`). Decided by the path, not Clerk's session (`<SignedIn>` renders only after Clerk loads, so the nav would pop in on every page); every other page is protected by the proxy, so a signed-out visitor only ever sees the public ones. The list of public pages moves from `proxy.ts` to `src/lib/public-paths.ts` so the proxy and the nav can't disagree. No migration; nothing for the user to keep.
- **2026-09-13 · The nav's parchment is Chanté's painting too, narrower, with the mockup's marks.** Chanté painted the sheet (`art/ui/nav-parchment.png`: aged paper, a browned rim, an inset rule, corner flourishes) and asked for it as the parchment's background, about 15% narrower, and for the dividers to look more like the mockup. The sheet is cut like the rail, into a head and a foot holding the flourishes and a stretching body, by one script for both pieces (`scripts/cut-nav-art.py`, which replaces `cut-nav-rail.py`); it is keyed by saturation, since its paper is as light as the checkerboard, and resized to 384px wide, twice its size on screen. The three pieces are layered in one element's background (caps over the body), so one drop shadow covers the sheet. It is 192px wide (was 226px) and stands 4px clear of the rail instead of tucking under it, as in the mockup; the wordmark drops to 30px to fit. The marks are new ornaments, `Sparkle` (a slender four-pointed star) and `Flourish` (a sparkle between hairlines fading toward their ends), short over the wordmark and full width under the tagline, replacing the diamond and the 52px-hairline divider there. The active icon's tile is centred on the icon: the icon cell starts at the page edge and the rail 10px in, so a tile centred on the cell sat 5px left. **The moon locks the sheet away** (Chanté's ask, the same day): a click on the painted moon keeps hover from opening it until the moon is clicked again or the nav is pinned. The nav now has one mode, `pinned | hover | locked`, in the same cookie (`coherence_nav`; the old `folded` reads as `hover`), rather than a second cookie that could contradict the first; keyboard focus still opens a locked sheet, since the names are how a keyboard user sees where focus is. The rail's height became a token (`--rail-h`) so the moon's button sits on the painted moon at any height. **Then the rail's halves** (Chanté's ask, the same day): a click on the top half of the rail pins the sheet out and on the bottom half locks it in, the same half again returning to hover; the parchment still pins. Judged by the click's position against the rail's midpoint, not by the element hit, because the icon list is laid over the rail and its gaps would otherwise fall to it. Replaces the CSS gradient sheet of the entry below. No migration; the lock is logged in `ef-burden-log.md`.
- **2026-09-13 · The nav's rail is Chanté's painting.** Chanté painted the rail (`art/ui/nav-rail.png`: olive green, brass rules, chamfered ends, the compass star and the moon) and asked for it in place of the drawn one, keeping its colour and texture, not full height, as in the mockup. The PNG has its checkerboard painted in, so `scripts/cut-nav-rail.py` keys the rail off it by darkness (solid between each row's dark extents, a soft edge just outside, edge pixels recoloured green) and cuts a head, a body and a foot into `public/` (17 KB together): the ends keep their proportions and the body stretches, so the rail fits any window height without squashing the star or the moon. Three backgrounds rather than one `border-image`, whose behaviour with no border style is less certain across browsers. The rail floats 10px in from the corner and is `min(100dvh − 20px, 780px)` tall. The painted star is the pin button; the drawn compass star, moon, sparks and brass line (`CompassStar`, `MoonIcon`, `.nav-spark`) are gone, and the icons take the painting's brass. Replaces the CSS gradient rail of the entry below. No migration; nothing for the user to keep.
- **2026-09-13 · The nav is a green rail with a parchment that opens from it.** Chanté's ask, with `art/mockups/rail-nav.png`: the 272px sidebar is replaced by a 64px green rail, always there, with an icon per place and a brass star on its rule beside the open one; the wordmark, the names and the aside move onto a parchment sheet that slides out on hover (or keyboard focus) and floats over the page, and a click on it, or on the rail's compass star, pins it open. **Pinned floats, like hover**: a first pass docked a pinned sheet (the shell's column widened to rail + panel), and on review Chanté didn't want the chat box shifting right, so the page never moves for the nav; on a narrow window a pinned sheet can cover a page's left edge. **A row's link is only as wide as its name** (at least 128px): with full-width rows most of the lower parchment was links, so a click there navigated instead of pinning; and the click that pins is the whole nav's, rail included, not only the sheet's (a click low on the rail did nothing). The pin is a cookie (`coherence_nav`) read by the root layout rather than `localStorage`, so a pinned nav renders on the server and doesn't jump in after hydration; reading it makes the layout dynamic, which every authed page already was. **One link per row**, spanning the rail icon and the name, so screen readers and the tab order see five links, not ten; the rows carry `aria-label` because a hidden name drops out of the accessible name. Everything is drawn in code (SVG icons and marks, CSS rail and sheet) — no raster assets. On phones, a trial: the rail stays at 52px and the compass star opens the sheet over the page (Chanté: "tap to open for now and we'll revisit"). Each page's own sidebar treatment goes (Home's wood panel, the paper plate at .84 on Today and the Library); the parchment tokens are shared by the nav's sheet and Home's greeting scroll. `design-system.md` now allows icons that carry meaning when their name is always a hover, focus or tap away (Chanté: icons with expandable names). A click that unpins folds the sheet at once, even under the pointer (`data-resting` holds hover off until the pointer leaves): on review Chanté couldn't close it, because the hover under her pointer kept it open. Replaces the sidebar of plain names with a diamond on the open chapter. No migration; the icons and the pin are logged in `ef-burden-log.md`.
- **2026-09-13 · Today is three layers on the garden, not one panel.** Chanté, looking at the garden: the list UI works but could feel nicer. The one tall panel read as a list pasted on a painting: six sections parted by hairlines under small-caps kickers, the greeting and Right now's title at the same size, the capacity question between them, Lumi's portrait on the panel while she stood in the room, and the closing line on a separate green plate. Now there are three layers, each quieter than the last (`product/today-garden.md` §6, §35–37): **Lumi's words on the painting** (running head, greeting, day line, and the once-a-day capacity question with its answers as words, not chips — the clock's parchment treatment over a feathered shadow; no portrait); **the one Right now card** (no kicker, title 34px, *Start with Lumi*, then *Not this* · *Break it down* · *Done* as quiet words — the tick circle in front of the title is gone, so the card invites starting); and **a faint slip** (paper at .80) with After that, Later and the closing line as its last word. Lumi's position is unchanged (her movement comes with the animation work). Replaces the one paper panel and the green slip of *Today, set in the garden* (below). Same content, same order, same plan; no migration, nothing for the user to keep.
- **2026-09-13 · Reconcile the living documentation canon.** Documentation only. Complete Spaces/Garden and five dedicated space/design documents now share explicit terminology and approved dispositions. Product direction remains distinct from as-built architecture, schema, features and Today behaviour; no runtime, schema or V1 milestones changed. Earlier source text is archived, and logs retain their historical rationale. Source hashes, section mapping and gaps: [reconciliation record](living/reconciliation.md). The automatic session-outcome interpretation is flagged as a gap, not changed here.
- **2026-09-13 · Lumi's shadow is drawn by the page, per room (hands-free branch).** Chanté: her shadow makes her look superimposed, and she will walk around rooms. The cut had baked a cream shadow into every cell, made for the paper; on a painted floor it read as a pale smudge. Now the hands-free cut leaves her feet on transparency (`lumi_cut.Sheet(shadow=False)`) and the companion draws a contact shadow (a backdrop filter darkening the floor under a soft oval, so it takes the floor's colour), a cast shadow (her current frame's silhouette laid on the floor away from the room's light) and the room's light as a tint over her, each room setting its values in CSS variables. Chosen over drawing shadows in the sheets (they would be wrong in every room but one, and wrong again when she walks) and over a blend mode (the companion is its own layer, so `mix-blend-mode` can't reach the painting). A cast direction per room is a fixed light for now; a Lumi who walks past a lamp would need it computed from her position. Nothing for the user to keep; no migration.
- **2026-09-13 · Lumi is hands-free by default — settled.** Chanté: "Lumi will be hands free often." The hands-free Lumi of the entry below is no longer a proposal: her usual state is holding nothing, and the lantern is one of the things she picks up (`art-direction.md` bet 8, settled). The hands-free body (`public/lumi-free.webp`) is the one that stays; the lantern body (`public/lumi-idle.webp`) and its cut stayed served beside it until 2026-09-18 (retired; the entry above). Poses and paths (bet 7) and pieces (bet 2) are still open. `PROJECT-CANON.md` → Lumi and `living/decisions.md` follow. No code change; nothing for the user to keep; no migration.
- **2026-09-13 · Lumi is hands-free, and her movements are poses joined by paths (proposal, `ux/lumi-hands-free`).** Chanté's direction: Lumi mostly holds nothing and picks things up in her environment; "document your thoughts and keep them updated". The plan is `art-direction.md` §4a (Claude's, kept current): a **hub** drawing (her rest pose without the lantern, `art/lumi/lumi-free-rest.png`) every sheet is generated from; **paths**, short in-betweened sheets from the hub to a pose, which `LUMI_LOOP_CELLS` can play backwards (the pick-up is also the set-down); **pieces** for small, frequent life, under test as a separate prototype. Built: a hands-free model sheet; the wave, a hands-together idle variation and a book pick-up, each passing the gates on its first or second run; a spec-driven cut (`scripts/cut-lumi-free.py`, one entry per loop) that holds every loop to one rest drawing and eases the cloak where a loop leaves it; `public/lumi-free.webp` served as the companion's body (176px cells, was 160). The pick-up plays only from the dev cue strip (`ACTIONS`): nothing in the app gives her a reason to pick something up yet. Chosen over redrawing the lantern loops one by one: a hub keeps every sheet one drawing, and a path played both ways halves the sheets. The lantern body (`public/lumi-idle.webp`) and its cut stay until Chanté decides; the avatar heads are still the lantern sheet's (the lantern doesn't show in them). `PROJECT-CANON.md` → Lumi still says she carries a lantern — raised with Chanté, not changed. Nothing for the user to keep; no migration.
- **2026-09-13 · The Library's painting is `scenery/library/background.png` again; its repaints are archived.** Chanté's ask: the painting in use (`background-smaller2.png`) is renamed `background.png`, the one-name-per-place convention of `art/scenery/`, and the repaints before it move to `art/archived/scene_mockups/` (`library-background-long-table.png`, `library-background-small.png`). Nothing changes on the page (`public/library-room.webp` is the same image). The spatial-map manifest keeps drawing over the archived long-table painting it was measured on until it is redrawn. No migration; nothing for the user to keep.
- **2026-09-13 · The Library is served from `background-smaller2.png`.** Chanté painted the reading circle smaller again (a low round table, two small armchairs, a stool and cushions on a wider rug, open floor around it). `public/library-room.webp` is regenerated from it. Lumi's spot (`--lumi-x: 1190; --lumi-y: 890`) still stands on open floor right of the circle and now covers only floor and the rug's corner, so it stays. `background-small.png` and `background.png` stay in `scenery/library/`. Replaces the served painting in the entry below. No migration; nothing for the user to keep.
- **2026-09-13 · The Library is served from `background-small.png`, and Lumi stands beside the reading circle.** Chanté painted the room again with furniture at Lumi's scale (a small round table with armchairs, stools and cushions instead of the long table), which also leaves the centre bookcase unobstructed — two of the concerns in the spatial-map proposal. `public/library-room.webp` is regenerated from it. The old spot (700, 900) now falls on the round pouf, so Lumi's spot moves to the open floor right of the circle (`--lumi-x: 1190; --lumi-y: 890`), where everything she covers is further back and she stays clear of the sidebar and the crop at every desktop window. `background.png` stays in `scenery/library/` as the room the spatial map was measured on. Replaces the spot in the entry below. No migration; nothing for the user to keep.
- **2026-09-13 · Lumi runs on OpenAI (`gpt-6-astra`); Anthropic stays alongside.** Chanté's ask, with `OPENAI_API_KEY` set locally and on Vercel. `src/core/ai/model.ts` builds `openai("gpt-6-astra")` through `@ai-sdk/openai` for every call (chat, the day plan, mail leads, reflection), and `LUMI_MODEL=anthropic:claude-opus-5` runs the Anthropic implementation instead, so the voice eval can run both, as the model strategy asks. Effort comes from `effortOptions()`, which carries both providers' keys, instead of inline `anthropic` options in plan, leads and reflect. OpenAI options: `store: false`, so conversations, beliefs and mail gists aren't kept as stored responses, with `reasoning.encrypted_content` so a tool loop carries reasoning between steps; `strictJsonSchema: false`, because the provider defaults to strict, which refuses the optional fields in tool and output schemas; `promptCacheKey`. `gpt-6-astra` because it is OpenAI's flagship, the place Opus held; `gpt-5.6-sol` and `gpt-5.6-terra` are the cheaper steps if cost or latency says so. Checked live: a two-step tool loop, a structured plan with optional fields, cache reads from the second call, and the nine voice scenarios (`voice-eval-log.md` run 4, not graded). The persona prefix stays byte-stable (OpenAI caches it on its own). Replaces `claude-opus-5` as Lumi's model; the blind comparison is still open question 21. No migration; nothing for the user to keep.
- **2026-09-13 · A folder per place in `art/scenery/`; the Library's signs are blank.** Chanté reorganised `art/`: each painted place has its own folder holding `background.png` and what else belongs to it (`scenery/home/`, `scenery/today/`, `scenery/library/` with her Library IA and the empty book template), and the mockups gathered in `mockups/`. The Library is served from her repaint with every sign and banner blank (`public/library-room.webp` regenerated), which settles the words-in-the-painting question `art-direction.md` §1 left open; the painting with words is kept as `mockups/library-background.png`. The table moved in the repaint, so Lumi's spot moves with it, to the rug in front of the table (`--lumi-x: 700; --lumi-y: 900`). Served paths (`public/*-room.webp`) unchanged. Replaces the flat `scenery/*-background.png` layout and the Library painting of the entry below. No migration; nothing for the user to keep.
- **2026-09-13 · The Library is only its room, for now.** Chanté's ask, after seeing the list panel on the painting: the panel comes off. `/library` is the painted reading room with Lumi standing in it and nothing on it. The lists are unchanged underneath — Lumi still files into them, Today still draws its path from them, and the context block still reads them — but no page shows them, so seeing everything, ticking an item off in place and *Add something* are gone from the app; all of it happens in conversation (the companion bubble works on the Library as anywhere). The panel's CSS is deleted rather than hidden; what the Library shows next is open (`art/mockups/book-mockup.png` and `book_layout-mockup.png` show a thread as an open book). Replaces the panel of the entry below.
- **2026-09-13 · Lists is the Library, set in its reading room.** Chanté's ask, with `art/scenery/library-background.png`: the nav item and page are renamed **Library** (`/library`; `/lists` redirects permanently, so old links and bookmarks land) and the painting is served behind it as `public/library-room.webp`. The page takes Today's shape — the lists on one paper panel on the right, the room open, Lumi standing on the rug by the reading table — and shares Today's sidebar, top bar, veil and panel CSS rather than copying them. It is the first design name from `spaces.md` §3 to reach the nav; *Today* stays *Today*. Lumi calls the page the Library too: the context block's recent changes and the persona line say *on Today or in the Library*, a one-time change to the cached persona prefix. The lists inside it (School · Work · Personal · Later), `intentions.list` and `users.preferences.lists` keep their names — the Library holds lists. The painting's signs are left as scenery and open (`art-direction.md` §1). Replaces the *Lists* name of 2026-09-12 (`today.md`). No migration; nothing for the user to keep.
- **2026-09-13 · Spaces as the organising model (draft).** Chanté's `docs/spaces.md`: Coherence's major functions are organised as spaces, each a cognitive mode rather than a feature — Home (arrive, converse), Today as the Garden (tend), Lists as the Library (organise, retrieve), Focus as the Study (attend) — with Lumi and shared state carrying context between them. A new feature is placed by the mode it belongs to, not given a space of its own (§4). User-facing names stay plain; Garden, Library and Study are design vocabulary (§3). The environments may grow with the relationship, never with output, and never decay (§25–27). Nothing ships differently yet: the nav is still Home · Today · Lists · Insights · Settings, there is no Study page, and §30 lists what pulls against the existing docs (dragging vs the EF-burden log, completions in the world vs `art-direction.md` bet 3, the garden's vocabulary, emergent Library sections). Replaces nothing; `design-philosophy.md` §2 and `art-direction.md` point to it. *Moved the same day into the canon as `docs/product/spaces.md`, without its §30: the product decision is an addendum to `living/decisions.md` → *Four places*, and the tensions §30 listed are notes on `living/open-questions.md` 2, 3, 5, 7, 14 and 15; the dragging tension was already decided (*Direct manipulation*).*
- **2026-09-13 · Lumi waves when you arrive, and her body is the wave sheet's drawing.** Chanté approved `art/lumi/lumi-wave.png` at 120 ms a frame ("the wave looks natural"). It plays as a **reaction, not an idle variation**: once, at the next rest frame, when a tab of the app becomes visible after thirty minutes or more with none visible in this browser (and on a first visit), then back to breathing — Lumi moves in answer (`art-direction.md` §5), warmth through behaviour, and once so her presence never becomes an interruption. Arrival is measured in the browser (`localStorage["lumi-last-seen"]`, refreshed each minute while a tab is visible and when it hides) rather than from the server's `app.opened` gap: it also catches a tab left open overnight, needs no prop threaded through every page, and is an animation cue, not domain state; thirty minutes matches `SITTING_GAP_MS`, so the wave and the greeting's new sitting mostly agree. The **breath is now synthesised from the wave sheet's first cell** instead of the lantern sheet's rest cell, so breath and wave are one drawing and hand over without a swap (the retired foot showed a hand-over between two drawings reads as a head turn); the avatar heads stay from the lantern sheet. The wave has **no blink rows** (`LUMI_LOOP_EYES`; a blink mid-wave shows the open cell): with them the body sheet was 917 KB, without 525 KB, and with the hold below 461 KB (244 KB before the wave). **The wave is held**: every cell keeps the first cell's hood, eyes, lantern and boots, and takes only the arm and the cloak it lifts from its own drawing, after aligning on those held parts. The first cut placed cells by the hood's right edge and settled them on the head alone, and Chanté saw her jump ("she jumps around a little bit"): five cells had dropped 2–4px, feet and all, and the rest drifted a pixel. Chosen over aligning alone because the generator's redraw still moved the boots, lantern and eyes by a pixel or two, which a 150px figure at 120ms shows. A second pass holds each phase ("still jittering a bit, mostly up and down"): measured on alpha, the hem had dipped ~2px before lifting and shifted 1–2px through the waves, so the rest cells are now the rest cell exactly, the waves keep one cell with only the hand swinging, and the lower cloak moves one way through the rise and the fall. Replaces "`VARIATIONS` empty; breath and blink only". Nothing for the user to keep; no migration.
- **2026-09-13 · Claude generates Lumi's sheets, and the prompt is iterated like the pipeline.** Chanté had been the courier between Claude and ChatGPT, and bad sheets were the ledger's most expensive line (`animation-pipeline.md`). Now `scripts/gen-lumi-sheet.py` calls OpenAI's image API (`gpt-image-2.5-*`, `OPENAI_API_KEY` in `.env.local`) with a prompt file from `art/prompts/`, measures every candidate on arrival and logs the run in that file; Claude changes one thing per version until a candidate passes the gates, and Chanté sees and approves the first sheet that passes, before it is cut. Chosen over driving chatgpt.com in the Chrome extension: the API gives several candidates a call, runs unattended, and doesn't depend on the Chrome tab group other sessions share. Costs money a run (tokens logged per run; the script stops at four images, and the doc says to stop and ask after six runs or ~$10 on one sheet). Stdlib only (no `openai` or `requests` package); it loads `/etc/ssl/cert.pem` because python.org's macOS build has no CA certificates. Replaces "Chanté generates in ChatGPT; give her the prompt ready to paste". Dev tooling only: nothing ships, no migration.

- **2026-09-12 · Today, set in the garden.** Chanté's ask: bring Today into the new art direction with `art/scenery/today-background.png` (a greenhouse in late light) behind it, laid out like `art/mockups/today-mockup.png` — breathing room, the room visible, the character in it. Today follows Home's shape — one fixed layer (`.today-scene`, `public/today-room.webp`) — but not its light: the painting is daylight, so the tokens stay ivory. A first pass stacked the page's parts as plates down a centred column and covered the room; the page is now **one paper panel on the right** (greeting, day line, capacity, Right now, After that, Later, parted by hairlines) with the closing line on a small green slip at the foot, as the mockup does, and **Lumi stands in the room** on wide windows — a spot in the painting's pixels mapped through the cover fit, so she keeps her flagstone at any size — instead of in the corner. Right now stays the one dominant thing by size and place in the panel rather than by a card of its own. She stands; she does not walk: her sheet is one pose with a breath, and sliding that drawing across the floor would glide — walking needs an in-betweened walk cycle (`animation-pipeline.md`). Not taken from the mockup: the All · School · Work · Personal tabs (horizon-switching is Lists' job, `today.md`), a plant per category, *In Season* with *3/5 steps* (a count of undone things; its corner is left open), *Plant something new* (Chat is capture) and the quote's copy (warmth comes from Lumi — the slip carries her closing line). `design-philosophy.md` §2 moves from "one painted surface" to painted places; `today.md` principle 10 notes the painting sits behind the page, not on it. Content and order unchanged; no migration, nothing for the user to keep.
- **2026-09-12 · The Coherence rename lands in the app and the docs.** Follows the entry below. In the app: the sidebar wordmark, the tab titles (*Today · Coherence*; Home is just *Coherence*), the package name, the message and tool types (`CoherenceUIMessage`, `CoherenceMessageMetadata`, `CoherenceTools`), the local voice engine's audio worklet (`coherence-pcm`), the timezone cookie (`coherence_tz`) and the voice-engine override (`localStorage["coherence.voice"]`). Renaming the cookie costs each existing browser one quiet refresh on its first visit after the deploy (`TimezoneCapture`), and the server keeps the stored timezone meanwhile, so a second cookie name wasn't worth reading. The commit guard asks for `COHERENCE_ALLOW_MAIN=1` and still accepts `LUMEN_ALLOW_MAIN`, so sessions started on the old brief aren't blocked. `CLAUDE.md`, `README.md` and the doc titles say Coherence. **Unchanged on purpose:** the persona prompt, which never named the product (the cached prefix is byte-identical); historical entries in this log, `qa-log.md` and `branching.md`; and paths that belong to the folder (`~/Projects/lumen`, `../lumen-<branch>` worktrees). **Outside the repo, not renamed:** the folder, the GitHub repo `chanteblais/Lumen`, the Vercel project `lumen` and its domain, the Clerk application *Lumen* (its name shows on the sign-in card), the Google OAuth consent screen. What each would involve is in `pre-prod.md` → Rename outside the repo. No migration.
- **2026-09-12 · Coherence, the medallion Lumi, she, and isometric rooms.** Chanté's answers to the questions in `art-direction.md` §9. **The product is Coherence**, the name the scene mockups carry; the app, the repo and most docs still say Lumen until the rename lands. **The canon costume is the medallions:** the sun-embroidered hood, the ribbon with brass medallions, boots and the lantern, as in `art/lumi/lumi.png` and the cut source; the fringed scarf in `art/lumi/lumi-ref.png` is off-model, and that sheet is kept for its behaviours. **Lumi is she**; `product.md` and `design-philosophy.md`, which said he, now say she. **Rooms are isometric**, like the served Home room; the eye-level mockups are not the direction, and a Lumi who walks in a room is drawn at its angle with four diagonal facings. Replaces Lumen as the product's name going forward, the brass-fastener description of Lumi in `product.md`, and bet 5 in `art-direction.md`. No code change, no migration.
- **2026-09-12 · Home, set in the room.** Chanté's ask: rename Chat to Home, and bring the page to the painted mockup (`art/background-mockup.png`, a lamplit cabin at night) with `art/home-background.png` as the background. The nav's first item is **Home** (it is where you land and where Lumi is; "Chat" named the mechanism), route and page unchanged. The page renders one fixed layer (`.home-scene`, `public/home-room.webp`) behind the shell and the palette is re-mapped under `.shell:has(.home-scene)` — parchment on dark glass, the lamp's gold for brass — so every class keeps its shape and takes the room's light; the greeting hangs as a parchment scroll (ink on paper, wooden rods). Chosen over a second set of components or a theme switch: one layer and one token scope, no state, and every other page stays ivory. `design-philosophy.md` §2 bends for it: the painting is the place the book is read in, not imagery inside it. Replaces "Chat stays the homepage" wording only in name; no migration, nothing for the user to keep.
- **2026-09-12 · Lumi is the lantern character; a pose sheet gives one cell, not a loop.** Chanté's new sheet, `art/lumi-lantern-idle.png` (hood with the sun, ribbon and medallions, a lantern in her hand; sixteen cells titled as an idle animation), replaces the earlier character everywhere she is drawn — the corner companion and the six avatar heads. Measured, the sixteen cells are separate drawings (head IoU 0.83–0.96 per step; the gate is 0.975), so cutting them as a loop would have boiled her like the first foot sheet did; instead the body is the rest cell alone and its idle life is synthesised from it — a breath of ≤ 2px at the hood top with the feet held, and lids drawn down over her own eyes for the blink rows — while the other cells become the avatar's expressions. The earlier character's loops (slow-idle breath and sway, the playful foot) are retired rather than played beside her, since a variation of another character is a swap, not a movement; `VARIATIONS` is empty until an in-betweened sheet of her exists (the prompt is in `art/README.md`). The lantern's light on the ground is kept as a translucent warm glow, so she lights the paper a little. The body cell is 160×208 (was 144). Replaces the three-loop sheet and its row map.
- **2026-09-12 · Design pass: quieter marks, the same opening everywhere.** The boxed grey `.pill` (list · estimate) read as a SaaS tag against the paper; it is now a marginal note — tracked small caps in the mute ink, no box, a middle dot between two — on Today's card, After that, and Lists rows. Every page but Chat now opens with the same running head (name kicker + `Divider`; Lists had a bare short rule), and Today and Insights set Lumi's portrait as a `.medallion` the way the Chat greeting does. Today's loading skeleton (grey bars) became the card with Lumi's three slow dots. Two quiet micro-interactions: a message said this visit settles onto the page (`.chat-now`, 6px rise + fade), and the complete circle's tick draws itself as the circle fills. Display lines `text-wrap: balance`, message text `text-wrap: pretty`. Old-style figures re-checked: the display face (Cormorant) *does* have them, the body face still doesn't — set on `body`, effective wherever a number lands in a display line; labels keep lining figures. Tab titles read as running heads (*Today · Lumen*). Nothing new for the user to keep; no ef-burden row.
- **2026-09-12 · Mail comes in, as a look, not an inbox — and Insights comes back for it.** Chanté's ask: Lumi looks through recent email, and an Insights tab asks "do any of these still need to get done?" with what she inferred. `product.md` had email integration out of V1; it is in now, framed so it adds no system to manage: Lumi reads Gmail (read-only, through the Google connection Clerk already holds — `getUserOauthAccessToken`, no token of our own) only when Insights is opened after ≥ 30 minutes or when asked in chat; one model call proposes *leads* (`core/ai/leads.ts`), `clampLeads` guards them, a new `leads` table (migration `0002`) holds a line per thing — never the mail; the page asks one question with two answers (*Still needs doing* → an intention, *Let it go*). Considered and rejected: our own Google OAuth + token table (Clerk does it); a sync job and a mail cache (a second inbox); three answers (done / not a thing / not mine — the distinction was for us, not for them); a refresh button (a thing to remember to press; the page looks on open); an unread or "needs reply" count (a bill). Insights leaves the After-V1 list ("only if it can be framed without scores") — it is framed as a question, and shows no number anywhere. Calendar stays out. Clerk is now imported in three lib files (`auth-mail.tsx` added for the connect chip).
- **2026-09-12 · An ask in chat reshapes Today, with Lumi's pick pinned.** Chanté said "I need an easy task"; Lumi answered with one, and Today kept its old Right now. The reserved `asked` re-cut is now wired through a tool, `reshape_today {ask, right_now?, first_step?}`, that writes nothing and only raises `onPlanChange({ reason: "asked", ask })`; the turn's `after()` re-cuts with `PlanInputs.ask` (the user's words + Lumi's pick), the planner rule "the ask outranks the default order", and `clampPlan(…, pin)` forcing the pick into Right now. Chosen over re-cutting inside the tool (a second model call the reply would wait on) and over parsing the ask from the message (never parse prose for state): Lumi decides *that* it's an ask and *what* she picked, code makes Today agree. The pin overrides a decline earlier today because the ask is the user's later word; without a pick, the planner chooses within the ask. In one turn an `asked` re-cut replaces a `capacity`/`declined` one (both still reach the planner from the snapshot). The ask rides on `plan.generated {ask}`, not on `day_plans` — no migration; asks are a learning signal, not state.
- **2026-09-12 · The glance is drawn, and a loop is an order over cells.** The head turn Chanté liked before the kick was an accident — the crossfade between the breath sheet's hood and the foot sheet's — and the fix that held the breath head removed it ("oh, you got rid of the head turn?"). It is now drawn: `art/lumi-idle-foot.png` (24 frames, three rows) has rest, a glance toward the kicking foot, and the kick with the head held turned; the turn back was not drawn (row 3 is rest frames) and the kick row is ~3.5% smaller, so the cut scales each row to the breath rest height and the loop replays the glance in reverse. Hence `LUMI_LOOP_CELLS`: a loop is the order its row's cells play in, and a cell may play more than once (fifteen cells, twenty-one frames); `LUMI_LOOP_FRAMES` is derived. Holds are per phase — the kick keeps the last glance frame's head — and the blink eyes are composited aligned on the eyes, not the face, since the glance moves them. Replaces the `FOOT_ORDER` frame list in the cut script and "every foot frame keeps the breath rest frame's head".
- **2026-09-12 · A variation is drawn as in-betweens of one drawing, not as a set of poses.** The first playful-foot sheet (eight frames, each a fresh drawing) never read as motion even once its frames were anchored: the hood tip, ribbon and folds changed every frame so the figure boiled under the crossfade, and the foot covered most of its travel in one 200ms step — Chanté: "the transition isn't smooth and it doesn't look like real motion." It is replaced by `art/lumi-idle.png`, sixteen frames generated from one base drawing (only the foot and dirt move; the head silhouette overlaps frame 1 by ≥98% in every frame) on a flat ground, cut as one kick out and back (frames 1–7 + 10–16, dropping the mid-loop return to rest) at 120ms a frame. Rule for any new sheet: one drawing, only the moving part changes, twelve or more eased in-betweens, a flat contrasting background, frame 1 = the breath rest pose. A contrasting ground needs its own matte (the cream sheets' tolerance read the grey-blue shadow and anti-aliased edge as figure, giving her a cold shadow and a grey rim): edges are de-matted against the known ground, the shadow is found by its blue cast and redrawn warm. The sprite grid widens to the longest loop (14 columns). Even so the generator redrew the hood rim and its sun a little per frame, so the cut settles each frame on the head to a quarter pixel and then holds the breath rest frame's head on every foot frame (an animation hold, blended in under the chin above the ribbon) — one head drawing through the loop and on both sides of the hand-over, since the foot sheet's hood is not quite the slow-idle one and the crossfade between them read as a small head turn; the fade lasts 80% of the faster loop's frame time rather than a fixed 260ms, so a 120ms frame is fully in before the next arrives and a hand-over is as crisp on the way back as on the way out. Chanté: "foot is a little blurry around the head near the end" · "smooth when she turns her head before the kick, a bit blurry when she turns her head after."
- **2026-09-12 · Idle frames are anchored on what holds still, measured — not on a detected feature.** The playful-foot frames were centred on the dark face; the face reads differently frame to frame (hood tilt, eye area) and sits right of the hood's centre, so Lumi hopped ~14px at every hand-over from breathing and wandered ~5px through the kick — Chanté: "the centre isn't consistent through the movement and the character jumps around." Now every loop is centred on the figure and the foot frames are then settled sideways by best overlap of the head silhouette (frame 0 onto the breath rest frame, the rest onto frame 0); the scale is chosen so hood top and chin match the breath loop. Feet stay on the baseline; the small vertical head movement drawn into the kick is kept. Replaces "centred on the face".
- **2026-09-12 · The context block carries what just changed, by whoever changed it.** Chanté ticked an item off by mistake, asked Lumi to add back "the item I just deleted", and Lumi asked what it was: the tick went through `/api/intentions`, wrote its event, and never touched the transcript. Now every chat turn loads the last 36h of `intention.*` events joined to their intentions (`core/domain/activity.ts`) into a *Recent changes* section, phrased by actor (`payload.via: "app" | "chat"` on `.completed` / `.reopened`; other types are chat-only today), with ids and current status; *Recently done* gains ids; the persona says act on it rather than ask; and `reopen_intention` exists so the fix is un-ticking, not a duplicate. Considered and rejected: pushing a hidden "user ticked X" message into the conversation (forks the transcript — decision 11 keeps one transcript) and reading the events into `loadSnapshot` (every page would pay the query; only chat reads it). Events stay the one source; nothing is parsed from prose.
- **2026-09-12 · M5: a session's end is a fact, not a request.** Focus Together lands as planned (two tools, the bar above the composer, deterministic check-ins, reflection in `after()`), with three calls made while building: (1) **Done and End close the session in code** before Lumi sees the message — the same path as *Not this* — so her one line is about something that already happened, there is no "did you finish?" question, and the EF check (three asks up front, none during) holds; `end_focus_session` stays for when they say it in prose. (2) **Sessions start only through Lumi.** No start control anywhere; *Start with Lumi* and "Body double" still hand off into conversation and she opens the session once what · first step · how long are settled, taking what the context already knows. (3) **The bar and check-ins live on Chat only.** On Today and Lists the companion is enough, and a second timer surface would bring back the idle-timer pressure Today's spec cut. Abandoned sessions (open past twice their planned length) are closed by `resolveSession` — the one write inside `loadSnapshot`, run by any page or turn — and offered back by the greeting until they've said anything since ("Pick it back up, or let it go?" — the greeting variant from the plan, over a silent close, because a session left open is the moment a companion should notice). Reflection guardrails learned from the first live run: evidence Lumi already recorded during the session is not recorded twice; a strategy gets evidence only from a session that used it (approach or first step); a session that ended within a couple of minutes without finishing skips the model step. The persona's "sessions aren't wired yet" line is gone (one cache break).
- **2026-09-12 · Lumi's sheets live in `art/`.** The folder that held the design mockups now holds only her animation sheets, so it is committed as `art/` (README inside) — the cut script reads `lumi-slow-idle` and `lumi-playful-foot` from it, and `lumi-stretch` waits there for a cut. Earlier entries that say `mockups/…` mean this folder; the mockups themselves were never checked in.
- **2026-09-12 · Lumi in the corner is tappable.** Tap her and a speech bubble opens with one text box; Enter sends through `/api/chat` (a second `useChat` client, same server-held transcript), her reply and ledger show in the bubble, and the page refreshes so Today/Lists reflect the write. Reverses "never with controls" from the 2026-09-11 companion entry: the ask ("add take out compost while I'm on Today") is exactly the kind of moment a companion is for, and the alternative — Chat → type → back to Today — is three steps of executive function for one thought. Still no state of her own; on Chat she only focuses the composer. Considered and rejected: navigating to Chat with the text prefilled (leaves the page, then needs a send).
- **2026-09-12 · Idle variations are a list, breath the default.** `mockups/lumi-playful-foot.png` (top row, eight frames) joins the sway as a second variation on the breath loop: every 20–45 s one pass of one of them, never the same one twice running, then back to breathing. Loops carry their own frame count and timing (`LUMI_LOOP_FRAMES`, `FRAME_MS`) so a new variation is a row in the sheet and an entry in a list. The sheet's alternate row (squinting eyes) is not used — eye state comes from the blink rows. The playful-foot art is drawn larger than the slow-idle sheet and is scaled to match at the cut; a small change of drawing at the hand-over is accepted, softened by the frame fade.
- **2026-09-12 · Coming back opens on the greeting, with the old conversation one scroll up.** Chanté, testing re-entry: "it refreshes when you come back, but you can scroll up to see the conversation — it's still there, but the chat feels fresh." The greeting card moves from the top of the page to the end of whatever was there when the page opened; the page opens scrolled to it, every time (two earlier cuts — "only after six hours", then "only after thirty minutes" — both failed the same test: navigating back to Chat in the evening still opened on the old conversation). What you say next goes below the card. The sitting window (`SITTING_GAP_MS`, the quick-starts and compact-card rule) drops from six hours to **thirty minutes** — the gap that already writes `app.opened`. The date rules between messages keep six hours. One transcript, unchanged data; only where the page opens. Replaces "greeting card always first, page opens at the newest message".
- **2026-09-12 · M4: the path is re-cut only on purpose, and the sitting carries the gap.** Three re-cut triggers (`capacity`, `declined`, `reentry`), collected during a chat turn and applied once in `after()`; `clampPlan` never returns a declined-today intention as Right now. *Not this* asks its six answers in place on the Today card (deterministic question, `core/declines.ts`) and hands off to Chat as one visible message with the reason — chosen over answering in Chat because the resistance happens on the card, and over re-cutting silently because the reason is what Lumi should answer. Capacity is asked on Today once a day (skippable, `capacity.asked {skipped}`) and re-cut is skipped when the answer matches what the plan assumed. The visit gap now comes from the newest `app.opened` event (`currentSitting`) rather than the previous request: the route was passing "first time here" on every turn (`recordVisit` returns the same value it compared against) and the greeting lost the gap the moment Today was opened first. Every page calls `recordVisit`. `PlanReason` gains `reentry` (text column, no migration).
- **2026-09-12 · Voice works everywhere without a vendor.** Brave exposes `SpeechRecognition` but ships neither Google's speech-service keys nor the on-device speech component, so recognition can never succeed there; Firefox has no API at all. Rather than a paid transcription API (new vendor, key, audio leaving the device; Claude itself takes no audio), the fallback is Whisper in the browser: `@huggingface/transformers` running `whisper-base.en` on WebGPU in a worker, behind the same `useVoiceInput()` surface (`components/chat/voice/`). Costs a one-time ~90 MB model download per browser; buys privacy and zero marginal cost. Engine choice is automatic and invisible.
- **2026-09-11 · Initial architecture, domain model and V1 plan proposed.** See `architecture.md` §1–4, `domain.md`, `v1-plan.md`. Not yet scaffolded.
- **2026-09-11 · Understanding layer promoted to first-class.** Beliefs carry confidence + evidence + supersession; model proposes ops, code applies with guardrails; interventions link to outcomes via `approach` on sessions; reflection triggers on session end and lazily on a new day. Tone is learned, not set. Memory tools move from M6 to M3. Replaces the "lightweight now, richer later" stance in the first proposal.
- **2026-09-11 · M0 scaffolded.** Next 16 + Tailwind 4 + Vitest; fonts Cormorant Garamond (display) / EB Garamond (body); `src/core` lint-guarded; `npm run check` = typegen+tsc, eslint, vitest. Greeting is already the deterministic `core/ai/greeting.ts`. Lumi avatar is a brass monogram until the hooded figure exists as an SVG.
- **2026-09-11 · Clerk wired in (M1 start).** `@clerk/nextjs` 7 via `clerk init`; `src/proxy.ts` is protected-first (only `/sign-in`, `/sign-up` public); `src/lib/auth.ts → requireUser()` returns the Clerk id until the `users` table lands, then the internal id. Auth UI (provider, sign-in/up links, account button) is wrapped in `src/lib/auth-ui.tsx` so the provider swap stays a two-file change. Greeting tolerates an unknown first name.
- **2026-09-11 · M1 (database half) built on `feat/m1-db` from a worktree**, because the Clerk half was being built in a parallel session in the main checkout. Drizzle over `postgres` (pooler, `prepare:false`); domain functions take `Db` as first arg; `ensureUser` is the Clerk→internal-id boundary. Migration `0000_initial_schema.sql` generated, not yet applied.
- **2026-09-11 · M1 joined.** `feat/m1-db` rebased onto the Clerk commit; `requireUser()` now returns the internal `users` row (`ensureUser`), timezone via cookie, `recordVisit()` for the greeting gap. Migration still pending `DATABASE_URL`.
- **2026-09-11 · Lumi avatar is the hooded figure.** Eight expressions cropped from the character sheet (`mockups/rali.png`) into `public/rali-sprites.png`; `LumiAvatar` takes `expression` and positions the sprite in the forest circle. Replaces the brass monogram placeholder. Not an SVG: the illustration's paper texture is part of the character, so it stays a raster sprite until a drawn vector exists.
- **2026-09-12 · M2 conversation.** AI SDK v7: `instructions` array (persona with `cacheControl`, then the context block) + `convertToModelMessages` (async now). Client sends only the newest message (`prepareSendMessagesRequest`); server owns the transcript. Quick-start chips send their label verbatim rather than a hidden canned prompt (the user sees what they sent). `createRouteMatcher` replaced with a hand-rolled public-prefix check. `scripts/voice-eval.mjs` runs the nine scenarios against the persona without the database.
- **2026-09-11 · Lumi keeps you company in the corner.** `LumiCompanion` in the root layout: a fixed, non-interactive bust in the bottom-right that blinks. Softens `design-philosophy.md` §"never a full-body illustration in the working UI": a bust at the screen's edge, never in the content column, never with controls. Blink frames are derived from `neutral`, so expressions stay one sprite row.
- **2026-09-11 · The character is Lumi.** Renamed in everything that names the character: persona prompt ("You are Lumi"), avatar/companion components and sprites, composer placeholder, greeting kicker, settings copy. The product, wordmark and page title stay **Lumi** pending a decision. New sheet `mockups/lumi.png` replaces `rali.png`; the corner companion is now her full standing figure, the chat avatar her head.
- **2026-09-12 · Chat layout is app-like, not document-like.** Viewport-height shell; transcript scrolls in its own region; composer docked. Quick starts follow the *sitting* rule (`isInSitting`, 6h). Voice input pulled forward from M7 to M2 via `useVoiceInput` (Web Speech API); Add file / Tools removed. Built on `feat/m2-voice-layout` from a worktree because another session was renaming the companion in the main checkout.
- **2026-09-11 · Companion idle life comes from the animation sheet.** `mockups/lumi-idle.png` replaces the cross-faded poses from `lumi.png` for the corner companion: an eight-frame idle loop with the sheet's blink frames composited onto every idle frame, micro variations as rare moments. Frames fade in over each other (never a translucent midpoint). `public/lumi-idle.webp` replaces `lumi-body.png`; the head avatar is unchanged.
- **2026-09-12 · Companion idle is the subtle sheet.** `mockups/lumi-slow-idle.png` (breath · blink · sway, one pose, 1–2px movements) replaces `lumi-idle.png` for the corner companion. Micro variations dropped: distinct drawings read as image swaps, not movement. Sway plays as an occasional single pass, per the sheet's usage notes.
- **2026-09-12 · The idle sheet is cut by script, matted from outside.** `scripts/cut-lumi-idle.py` regenerates `public/lumi-idle.webp` from `mockups/lumi-slow-idle.png`. The earlier hand cut keyed the paper colour and ate the cream hood (it looked clipped); the script flood-fills paper only from outside the figure, keeps the foot shadow, and centres each frame on the figure rather than trusting the sheet's uneven spacing.
- **2026-09-12 · Today is a path, not a pile (`today.md`).** Lists is in as the user's broader structure; Today curates one dominant task + a short path from a persisted, model-proposed `DayPlan` with code guardrails. *Not this* with reasons is a first-class signal into beliefs. Capacity prompt once a day. Cut from the mockup: calendar timeline, timer widget, quick capture, carousel, tabs, counts. M3–M5 re-cut. Open: product rename to "Lumen", V1 nav, Today as landing page.
- **2026-09-12 · The product is Lumen; the character is Lumi.** Rename pass: wordmark, `<title>`, package name, `LumenUIMessage`, `.msg-lumi`, cookie `lumen_tz`, belief source `lumi_inferred`, docs. Directory + GitHub repo still `rali` until no session is active. Chat stays the homepage (someone *with* you through your tasks; Today/Lists are updated through the conversation). V1 nav: Chat · Today · Lists · Settings.
- **2026-09-12 · Graphics pass: the book gets its engraving.** Personality via printer's ornaments, not illustration: `components/ui/Ornament.tsx` (diamond · hedera · asterism; `Divider` under every kicker, `Tailpiece` on empty pages), crossed corners on the sidebar plate, an inset hairline frame on the greeting card (`.plate`), a brass medallion ring on Lumi's avatar, the thick-thin Oxford rule under the running head, chapters numbered in roman numerals (removed the same day: Chanté preferred bare names), grain laid over the whole shell plus a faint foxing vignette and a gutter shadow, one brass focus ring, brass selection, paper-toned scrollbars, and an SVG favicon of Lumi. `design-philosophy.md` §2 amended: ornaments are typography, not decorative imagery. Rationed to one flourish per surface. True small caps and old-style figures were checked and skipped: the Google-served EB Garamond has neither feature.
- **2026-09-12 · The chat column keeps a lane for the companion.** With the composer docked, the send button sat under Lumi at every desktop width (the column's right edge was 48px from the viewport; her footprint is the outer ~132px), worst once the column centred above ~1450px. `.chat-page` now pads its right by `--companion-lane` (112px) and grows its max-width by the same, so the column ends ≥160px from the edge; under 768px she is hidden and the lane is 0. She stays fixed, non-interactive and outside the content column — the lane is layout, not a control.
- **2026-09-12 · Session conventions carried over from Glåüm.** `CLAUDE.md` gained the standing tasks every session follows there, adapted: branch before the first edit, parallel-session and port etiquette (3005 is the review port; 3000–3004 are other projects), a review server left running with a checklist, migrations printed verbatim, docs folded into the same commit, the EF-burden log as the per-iteration ledger, append-only logs. `docs/branching.md` adds the PR path (merge commits only, first PR #1), the directory-rename stop (learned in the `rali` → `lumen` move), and a dev-servers-and-ports section. `.claude/launch.json` (port 3005) is the one `.claude/` file that is versioned, as in Glåüm.
- **2026-09-12 · M3 built.** Tools are the only write path (ten tools incl. beliefs + capacity); ledger lines from tool parts; `day_plans` persisted per local day, generated once via structured output + `clampPlan` guardrails, streamed under Suspense; closing an intention anywhere advances the path in code. Lists minimum: grouped view, tick, one Add → chat prefill. Today/Lists → Chat handoffs are visible messages with metadata. Library/Insights pages removed; nav is Chat · Today · Lists · Settings.
- **2026-09-12 · Today's path is cut before it's needed.** `core/ai/today-plan.ts → ensureTodaysPlan()` is the one reader/generator of the day plan; `primeTodaysPlan()` runs in `after()` on the home page and after every chat turn so the model call is off the page. One code-derived refresh added — `reason: first_items` re-cuts a plan that was empty once intentions exist — because priming on open makes "open → brain-dump → Today" the normal order; a plan with a Right now is never regenerated by it. Same pass: `ensureUser` takes the display name lazily so Clerk's profile API is called only on row creation (was every request), and the pool keeps idle connections for five minutes instead of twenty seconds. Measured on Chanté's machine: ~200ms Clerk, ~70ms per warm query, ~500ms per new connection, seconds for generation.
- **2026-09-12 · GitHub repo renamed to `chanteblais/Lumen`** (Chanté). Local remotes updated; the directory stays `~/Projects/lumen`. Closes the "still `rali`" note above. Same pass: stale `rali_tz` / `rali_inferred` names in the docs corrected to `lumen_tz` / `lumi_inferred` (the code had been renamed on 2026-09-12); the detached `lumen-main` view fast-forwarded; merged branch `fix/lumi-hood-matte` deleted.
- **2026-09-12 · The greeting asks its question once, then continues.** "What are we working with today?" is now only the *first* landing — while there is no conversation to pick up. Every later open continues from the last thing said, keyed on *when* it was in the user's calendar (`localDayDiff`): same day → "Welcome back" / "Where did we end up?" (within five minutes, "Still here."); yesterday → "Picking up from yesterday. What's first today?"; a few days → "It's been a few days. Where did we leave things?"; a week or more, by visit or by conversation → the existing re-entry offer. Still deterministic, still never a count of anything undone; naming the last intention was considered and rejected (the most recently touched thing is often the last one filed from a brain dump, not where we were).
- **2026-09-12 · Docs are audited whole-branch before every merge and push.** The per-commit docs sweep stayed, but it only sees one commit at a time, and a branch's later commits kept quietly outdating the doc its first commit had updated. New standing step in `CLAUDE.md` and `docs/branching.md` (its own section, with a changed-file → doc map): after `npm run check` and the click-through and before any `--no-ff` merge, PR merge or push of `main`, diff the branch (or `origin/main..main`) and read the passage in every doc those files are described by; anything stale is fixed on the branch first, never after; the final summary names what was audited. Applies in miniature to `LUMEN_ALLOW_MAIN=1` tweaks. Replaces the single "docs ride along — verify before pushing" bullet, which only fired at push time and only asked whether docs were *touched*, not whether they were *true*.
- **2026-09-13 · Lumi's movement in rooms is a rig from drawn facings, not sprite sheets.** Chanté approved the pieces test ("looks great") and asked for a walk test; over six review rounds it became `art/prototypes/lumi-walk/` — nine generated isometric drawings (sixteen facings by mirroring), each cut into a body and boots, legs drawn every frame, a row-by-row warp for bob, sway and lean, paths on an eight-direction ground grid, furniture cut from the painting and laid back over her, and turns that morph between neighbouring drawings. Her verdicts: "she looks awesome", then, on the morph, "That's looking a lot better" — merged as a prototype, nothing served yet. What made it work is written where the next session needs it: measure the drawing, not its label (`facings.py`: the first diagonals were turned 29°, not 45°); audit facing against movement every frame (`capture.py --audit`); a foot piece carries the boot alone; pace and stride on the ground, not the page; halve the angle between drawings, then morph. Sheets stay for signature gestures. Still open: the room map, the app playing it, the base idle on the rig (`art-direction.md` §4a, bets 2 and 4).
