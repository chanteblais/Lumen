# EF-Burden Log

*2026-09-13 reconciliation: historical rows remain evidence. Approved optional correction and continuity-based growth supersede incompatible earlier proposals; see [current decisions](living/decisions.md). This documentation change introduces no user control or maintenance burden.*

A **running ledger of every place Lumi asks the user to maintain, decide, categorise, rate, or tidy something** — the raw material for the one question: *does this reduce the user's executive-function burden, or accidentally create more of it?* Mirrors Glåüm's generalizability log: captured from reality as we build, not guessed.

> **Why this exists:** productivity systems accrete maintenance. The only defence is to notice each increment as it appears. Some burden is fine (typing a message). The point is that every increment is *seen* and *chosen*.

## Maintenance protocol — for Claude (automatic, every iteration)

During **any** work on this codebase, whenever you write, propose, or read code or copy that:
- asks the user to **set** something (a status, priority, category, tag, due date, setting, tone),
- asks the user to **keep** something current (a list to groom, an inbox to clear, a session to close),
- asks the user to **rate** or **confirm** something Lumi could infer from behaviour,
- shows the user a **count** of undone things,
- or requires the user to **remember** to do something in the app for the app to work,

→ **add a row to the table below before you finish the task.** Rules:
- **Log it, then decide.** Options: `accepted` (a deliberate, minimal ask — say why), `derived` (replaced by inference from events/behaviour), `removed`, `open` (needs a decision).
- **Prefer derived over asked.** If behaviour can tell us, don't ask.
- Keep entries terse. One row. Link `file:line` where useful.

## Categories (for the "Type" column)
`set` · `maintain` · `rate` · `count` · `remember` · `choose` (a menu of options where one default would do)

---

## Ledger

| Date | Area / `file` | Type | The ask | Alternative | Status |
|---|---|---|---|---|---|
| 2026-09-13 | The Library, `core/ai/consolidate.ts` + the library tools in `core/ai/tools.ts` | maintain | An archive of threads and notes that grows on its own — the risk is a second brain the user feels they ought to curate | Nothing to file, tag, name, review or tidy: consolidation files between visits, a thread appears only for a sustained subject, resting is derived, and corrections are words ("forget that", "that changed"). No page, no counts | derived |
| 2026-09-13 | What Lumi knows, `app/settings/page.tsx` + `components/settings/MemoryItem.tsx` | maintain | A list of everything Lumi holds, with Correct and Forget on each — a store the user could feel they ought to curate | Never prompted, linked to from elsewhere or counted; learning still needs nothing from the user; the same corrections work in words in chat, where a wrong belief shows up. Forget asks once only because it can't be undone | accepted (optional; inspection and correction are the canon's ask — open question 12, AI & IA §51) |
| 2026-09-13 | Sensitive details, `core/domain/memory-rules.ts` | remember | For a health or other sensitive detail to be kept between visits, the user has to ask ("remember…", "keep that in mind") | Lumi still hears it in the conversation; only durable storage waits for the ask. Storing it quietly would save the ask and cost privacy | removed 2026-09-13 (Chanté: not worrying about privacy yet — personal details are kept like anything else they say; the ask is gone) |
| 2026-09-13 | Library plaques, `art/scenery/library/spatial-map/README.md` question 5 (proposal, not built) | set | Who names a Collection or Thread Group shown on a plaque, and when — if the user had to, it would be a taxonomy to design and keep | Proposed: Lumi names a Collection from what it holds once the context is sustained, and the user corrects by saying so; nobody is asked to name a slot. Waits on Chanté's answer (open question 3) | open |
| 2026-09-13 | Nav on a phone — the bottom bar, `components/shell/Sidebar.tsx` | remember | Was: on a phone, the places were five icons and reading a name took a tap on the compass star | The bar shows each name under its icon; nothing to open, pin or lock on a phone | removed (the tap to read a name is gone on phones; desktop's row below stands) |
| 2026-09-13 | Home — the greeting scroll rolls up, `components/chat/Greeting.tsx` | choose | A tap on the bottom rod rolls the scroll up to its title line; a second tap unrolls it | Never asked and never kept: it opens unrolled every visit, and a rolled scroll left as it is costs nothing. Not persisted — remembering it would make it a setting to undo | accepted |
| 2026-09-13 | Nav — the rail's icons, `components/shell/Sidebar.tsx` | remember | With the parchment folded, the places are five icons; the user learns which is which | The name is always a hover, a keyboard focus or a tap away, and pinning keeps the names out for good; the first item is still where you land | accepted (Chanté's call: icons with expandable names over plain names) |
| 2026-09-13 | Nav — the moon's lock, `components/shell/Sidebar.tsx` | set | Click the bottom half of the rail (the moon's) to keep the names from opening on hover; remembered per browser | Hover is the default; the lock is only for someone the sliding sheet bothers, and the same half or a pin undoes it | accepted (optional, one click, undone the same way; with the pin it is a three-way choice the user never has to make) |
| 2026-09-13 | Nav — the pin, `components/shell/Sidebar.tsx` | set | Click the top half of the rail (the star's) or the parchment to keep the names out; remembered per browser | Hover, focus or a tap shows the names without it; folded is the default and nothing needs the pin | accepted (optional, one click, undone the same way) |
| 2026-09-13 | Today — beyond the path, `components/today/PlanSection.tsx` | count | Correction to the 2026-09-12 *"Other items (2)"* row: its alternative, the wordless collapse ("A few more, when you get there"), was never built | Nothing past After that (≤ 3) is shown; the closing slip, *Everything else can wait*, is the only sign there's more. No count either way | removed (as built; whether a collapse is wanted is Chanté's call — `today.md` → Anatomy) |
| 2026-09-13 | Library dragging, `docs/product/spaces.md` §14 (not built) | set | Drag up and down for priority, between sections for context, to Later to stop foregrounding | Reopens the 2026-09-11 turn-down of drag-to-prioritise, this time as an optional correction, not upkeep. It holds only if Lumi orders and files everything without it, a drag is an event she derives from (no priority field to keep current), and nothing goes stale or looks wrong when the user never drags | accepted (decided: [`living/decisions.md`](living/decisions.md) → *Direct manipulation*; optional correction, never required; the design is open question 20) |
| 2026-09-12 | Rewards: earning and spending coherence (contested, not built; `docs/living/decisions.md`) | count, choose | A coherence balance to see, and plants or things for Lumi to choose to spend it on | Conflicts with Product Vision §14 ("inhabited, not gamified", growth not points) and Experience Principles §14 (points and currencies to watch for), which would need neither the balance nor the choice. If kept: only ever positive, earned for showing up as well as doing, spending never required. `docs/living/open-questions.md` 14, 6–7 | open |
| 2026-09-12 | Focus session setup, `core/ai/persona.ts` → Focus Together | set | Three things before a session: what, first step, how long | Lumi takes any of the three the context already holds (the intention's next step, its estimate, a proven strategy) and asks only for what's missing; there is no form, no start button, no timer picker — `minutes` defaults to their usual | accepted (M5) |
| 2026-09-12 | Check-in card, `components/focus/SessionBar.tsx` | rate | *Still with it?* every `check_in_minutes` — Yep · Stuck · Got distracted · Done | One tap; Yep costs nothing further (event only, no reply); typing anything else dismisses it; nothing repeats or escalates if ignored. Considered deriving "still with it" from typing activity — rejected: the user is working elsewhere, so activity in the app means the opposite | accepted (M5) |
| 2026-09-12 | End on the session bar, `SessionBar.tsx` | decide | Stop the session early | No "did you finish?" follow-up: End is stopped early, Done is completed, decided by which was tapped; Lumi says one line either way | accepted (M5) |
| 2026-09-12 | Abandoned-session greeting, `core/ai/greeting.ts` | decide | "Pick it back up, or let it go?" on the next visit after a session was left open | Considered a silent close (nothing asked) — kept the line: it is the companion noticing, not a chore, and it needs no answer (say anything and it's gone). Never shows how long it was open or how many | accepted (M5) |
| 2026-09-12 | Session defaults, `users.preferences.session_minutes` / `check_in_minutes` | set | Session length and check-in interval | Defaults only (45 / 15); nothing to set anywhere yet. Lumi can hear "make it twenty" per session. Whether Settings ever exposes these is M6's call — prefer learning a `preference` belief from what they ask for | open (M6) |
| 2026-09-12 | Insights → *Still needs doing* / *Let it go*, `components/insights/LeadActions.tsx` | choose | Two answers per thing Lumi noticed in the mail | The question is the page's whole point: Lumi did the reading and the inferring; the user's part is a yes or a no per card, nothing to type, nothing to sort. A third answer ("already done" vs "not a thing") was dropped — the distinction was for us. Answered cards disappear; nothing waits to be cleared | accepted |
| 2026-09-12 | Insights → *Connect Google* / *Allow mail*, `lib/auth-mail.tsx` | set | One-time OAuth connection (and a re-authorise if the scope is missing) | Unavoidable for mail; done once, from the page that needs it, with what it's for and what is kept stated in two lines. No settings screen, no toggles. Disconnect is Clerk's own panel — not a control we add | accepted |
| 2026-09-12 | Looking through the mail, `core/email/scan.ts` | remember | A "check my mail" button the user would have to remember to press | Looks on open when the last look is older than 30 minutes; a fresh look is a label, not a control. In chat, Lumi looks when asked and never unasked | derived |
| 2026-09-12 | Insights → "Last looked 20 minutes ago", `core/insights.ts` | count | Could have been "read 14 messages, found 3 things" | Time since the look only; no message count, no lead count, no unread count anywhere (page, nav, context block) | accepted |
| 2026-09-12 | Greeting's opening question, `core/ai/greeting.ts` | decide | "What are we working with today?" on every page open — re-declaring the day's work each time you came back | Asked once, while there is nothing to pick up; afterwards the line continues from when we last spoke (same day · yesterday · a few days), derived from the transcript's timestamps — never from what is undone | derived |
| 2026-09-12 | Capacity prompt built, `components/today/CapacityPrompt.tsx` | rate | Three answers + Skip above the Right now card | Shown only when nothing has reported capacity today (chat counts), not yet skipped today, and there is a path to shape; Skip is remembered for the day (`capacity.asked`); *Normal-ish* on an unstated plan changes nothing, so answering never costs a re-cut for no reason. Not shown on an empty day | accepted |
| 2026-09-12 | *Not this* → "Keep it", `components/today/RightNowActions.tsx` | choose | A seventh control beside the six reasons | It is an escape, not a reason: closes the chips without recording anything. Without it, opening the question commits you to answering | accepted |
| 2026-09-12 | Coming-back pass, `core/ai/persona.ts` → Coming back | maintain | Going through stale intentions after a long gap | Lumi does the going-through: names them by shape, drops in one go on one answer, ends with one step. The user's part is a yes and a sentence. Never a count, never one-by-one confirmation | derived |
| 2026-09-11 | Quick-start chips, `components/chat/QuickStarts.tsx` | choose | Four starting points on the landing page | Kept: they are prompts, not modes; nothing happens if ignored. Watch that they never grow beyond four | accepted |
| 2026-09-11 | Composer tool links "Add file / Tools" | choose | Two affordances with no V1 behaviour | Hidden 2026-09-12 (review finding: dead buttons on the main screen). Return only when they do something | removed |
| 2026-09-12 | Voice input, `components/chat/useVoiceInput.ts` | choose | A second way to put words in the box | Kept: transcription lands in the composer for reading, never auto-sends; one tap on, one tap off. The lowest-effort input for the lowest-capacity moment | accepted |
| 2026-09-12 | Voice error lines, `components/chat/useVoiceInput.ts` | confirm | "Is the mic on? Tap Voice to try again" / "Tap Voice to listen here again" after the browser ends a session | The alternative was a button that silently switched off, which the user had to notice and diagnose. One line that names the cause and the single next tap is less burden than a mystery; a plain end restarts without asking at all | accepted |
| 2026-09-11 | `mockups/rali-list.png` — Lists board | maintain, count, set | Per-column counts, due-date on every row, five "Add item", drag-to-prioritise, category columns | **Revised 2026-09-12** by the Today brief: Lists is in as "the pile" so Today can be "the path". The interaction-model objections stand: no per-column counts, no due date unless set, one Add, reorder later. See `today.md`. **Revised again 2026-09-12** by Experience Principles §6, §16: dragging comes back as *optional correction that teaches Lumi* (vertical = relative priority, horizontal = list, Later = stop foregrounding), never as sorting the system depends on. Counts and extra Adds stay out. `docs/living/decisions.md` → Direct manipulation | accepted (scoped) |
| 2026-09-12 | Today mockup — "Other items (2)" | count | A count of items beyond the path | Wordless collapse: "A few more, when you get there" | removed |
| 2026-09-12 | Today mockup — "1 of 3" carousel on Right now | choose | Browse alternative current tasks | *Not this* is the only way to change the current task; a carousel invites scanning | removed |
| 2026-09-12 | Today mockup — quick capture with Task/Note/Idea | set, choose | A second input with a type picker | Chat is capture; Lumi files. Revisit only if chat-capture proves slow in daily use | removed (V1) |
| 2026-09-12 | Today mockup — focus timer widget (25 min ±) | set | A timer to configure on the page | Timer appears only inside a started session (M5); defaults from beliefs/preferences | removed from Today |
| 2026-09-12 | Today mockup — "Your Day" calendar timeline | maintain | Calendar events to keep current | Post-V1 with real calendar integration; until then Later shows fixed-time intentions only | deferred |
| 2026-09-12 | Capacity prompt on Today | rate | "How much have we got today?" three answers + skip | Kept: asked at most once a day, skippable, and it materially changes the plan — the one self-report that earns its tap | accepted |
| 2026-09-12 | *Not this* quick answers | rate | Six reasons for declining the current task | Kept: it's the opposite of pressure — the user corrects Lumi in one tap, and the reason is the best learning signal we have | accepted |
| 2026-09-12 | Lists membership (`intentions.list`) | set | Which list a thing belongs to | Lumi infers on capture; the user only ever corrects. Never a required field | accepted (derived by default) |

| 2026-09-11 | Tone preference | set | A tone picker in Settings | Learned as a `preference` belief; visible/correctable in "What Lumi knows" | derived |
| 2026-09-11 | Focus check-ins | rate | "Still with it?" Yep / Stuck / Distracted / Done at intervals | Kept: it's the body-double contract the user agreed to; "Yep" costs one tap and no reply | accepted |
| 2026-09-12 | Quick-start chip text, `core/ai/persona.ts` | choose | The chip sends its label as the user's message | Considered a hidden canned prompt per chip; rejected — the transcript would show words the user didn't say. The persona interprets the four labels instead | accepted |
| 2026-09-12 | "Another way in" button, `components/chat/QuickStarts.tsx` | choose | Sends "I don't know where to start." | Kept: one tap for the person with the least capacity to type | accepted |
| 2026-09-12 | Lists "Add something" chip, `app/lists/page.tsx` | choose | One affordance to add, which opens Chat prefilled | Kept: one, and it routes through the conversation so Lumi files it | accepted |
| 2026-09-12 | Today "First" line (first step) | — | Shows Lumi's chosen first physical action | Not an ask — but if it ever becomes editable it becomes maintenance. Keep read-only | accepted |
| 2026-09-12 | `reshape_today` tool, `core/ai/tools.ts` + persona | — | Nothing asked of the user: "I need an easy task" in their own words re-cuts Today around it. Considered a "Replan" button or an "easy / quick" filter on Today; rejected — a control to remember and a state to reset. The words they already typed are the input | Derived from the conversation; the ask is kept on the event, never shown back or kept current | derived |
