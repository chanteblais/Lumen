# Design System

*[Visual Language](design/visual-language.md) and [Motion & Interaction](design/motion-and-interaction.md) now hold shared design direction. This file retains current tokens, classes and component behaviour; examples in the new documents do not silently change shipped values.*

Antique book × modern editorial interface. Tokens live in `src/app/globals.css` (`:root` custom properties, mapped into Tailwind via `@theme inline`). Tailwind is for layout; the named classes below carry the look.

---

## Color Palette

| Name | Hex | Tailwind class | Usage |
|---|---|---|---|
| Paper | `#efeae2` | `bg-paper` | Page ground |
| Paper deep | `#e6e0d5` | `bg-paper-deep` | Chips, icon buttons |
| Card | `#f8f5ef` | `bg-card` | Composer, Today's cards, the companion bubble (the greeting sits on bare paper) |
| Ink | `#1b1a17` | `text-ink` | Primary text |
| Ink soft | `#3f3c36` | `text-ink-soft` | Secondary text, labels |
| Ink mute | `#8a8378` | `text-ink-mute` | Placeholders, nav numerals, quiet labels |
| Brass | `#9c7e4e` | `text-brass` | Accent — sparingly (one flourish per surface) |
| Brass soft | `#c9b58c` | `text-brass-soft` | Accent on dark fills |
| Forest | `#2a342e` | `bg-forest` | Avatar circle, send button — the only dark fills |
| Glow | `#f1d8a0` | `text-glow` | Lumi's eyes (reserved) |
| Foxing | `rgba(122,96,54,.11)` | — | The page-edge vignette only |
| Rule | `rgba(27,26,23,.16)` | — | Fine rules, borders |
| Rule strong | `rgba(27,26,23,.34)` | — | Short rules, composer border |

**Paper grain:** `body::before` — a fixed, non-interactive SVG `feTurbulence` layer at 28% opacity, multiply blend, laid *over* the shell so cards and the nav's parchment are paper too. Keep it barely perceptible; if you notice it, it's too strong.

**Foxing:** `body::after` — a fixed radial vignette in `--foxing`, multiply blend: the page browns very slightly toward its edges. Same rule: if you notice it, it's too strong.

**Gutter:** `.main` carries a soft inset shadow on its left edge where the page meets the spine. Off on phones.

**Dark mode:** none in V1. The book is ivory — with two exceptions, below.

**Home: the room** (2026-09-12). Home is not set on ivory: it is set *in* a painted room (`art/scenery/home/background.png` → `public/home-room.webp`, 1536×1024, cover-fit, fixed) — an evening study by lamplight, the same book read by lantern light. The page renders one layer, `.home-scene`, behind the shell (all three rooms render it through `RoomScene`: since 2026-09-13 the painting is a preloaded `img.scene-painting` inside the layer rather than its CSS background, so a first visit starts it with the HTML, ~0.4s sooner than a background found after the stylesheet; the layer shows the room's own colour — `#362e29` here, `#6b5236` the garden, `#231912` the reading room — under a 24×16 copy of the painting stretched soft (a ~220-byte webp data URI in the stylesheet, since 2026-09-13, so the first paint already has the room's colours: the flat colour made the fade read as a pop), and the painting fades in over it in 450ms, coming into focus, or shows at once when it is already loaded, set by an inline script in the layout before hydration; no fade under reduced motion, and shown after 3s regardless); its `::after` dims the painting toward the spine (darkest along the left edge) and the floor (behind the composer) so the type sits in the light — at half the strength it first had (2026-09-13, Chanté: the room read much darker than the painting, ~43% darker on average; now ~24%), since the greeting has its own scroll and the composer its glass. **The stove's fire moves** (2026-09-13, Chanté's asks: first the light flickering, then the flames moving). `RoomScene` adds `HomeFire` to Home's layer, two siblings after the painting, each placed in painting px through the cover fit (`--x`/`--y` from `fire.ts`, `--px` as Today places Lumi), so they stay on the stove at any window. **The flames** (`.scene-flame`, art-direction §4 tier 1: motion from one drawing): a canvas over the stove's glass (crop 598,192 · 74×68 painting px, buffered at 3×) where `fire.ts` warps the painting's own flame pixels each frame — sideways waves that travel up the flame and sway the tips most, a lick running upward, and the whole fire stretched taller or lower from the log bed with `fireLevel(t)` (four incommensurate periods, 0.29–3.7 s, so it never visibly repeats). What may move is an ellipse inside the glass, above the log bed, weighted toward the flames, with the dark logs held (they bent with the flames on the first render); everywhere else the canvas is transparent, so the painting shows through with no seam. ~30 frames a second from `requestAnimationFrame` (waits in a hidden tab), under 2 ms a frame headless; the painting is read once from the same preloaded file. **The light** (`.scene-fire`, tier 0): two dark amber radial lights, `.scene-fire-glow` on the glass (120×96 painting px, stretching from its base with the flames) and `.scene-fire-spill` on the hearth, wall and floor (460×320), colour-dodged over the painting, so what the fire lights brightens and the dark iron stays dark (screen was tried first and lifted the stove into a grey haze). Dodge only brightens: at its lowest it's the painting as painted. The light follows the flames: `HomeFire` sets `--fire` (the level) and `--fire-slow` (lagging it, for the floor) each frame. Both sit under the veil, fade in with the painting (the same `data-shown` / 3s fallback) and are removed under reduced motion, followed live. Everything else is the same tokens re-mapped under `.shell:has(.home-scene)` (both `--x` and Tailwind's `--color-x`, which resolve on `:root` and would otherwise keep the ivory values):

| Token | By lantern light | Usage on Home |
|---|---|---|
| Paper | `#1a120c` | The fleuron's vein |
| Paper deep | `rgba(241,230,207,.08)` | Chips, icon buttons, your lines' tint |
| Card | `rgba(26,18,12,.72)` + `backdrop-filter: blur` | Lumi's plates, the composer — dark glass. Write `backdrop-filter` unprefixed and alone: the CSS pipeline adds `-webkit-` for Safari, and a hand-written `-webkit-` line after it makes lightningcss drop the unprefixed one (no blur in Chrome; `npm run check:css` catches it) |
| Ink / soft / mute | `#f1e6cf` / `#d6c8aa` / `#a89a80` | Parchment type |
| Brass / soft | `#d9ac5f` / `#f0d9a6` | The lamp's gold: ornaments, the send button, focus ring, thinking dots |
| Rule / strong | parchment at .16 / .40 | Every hairline |
| Shadow | two black layers (.25 / .35) | Plates and the scroll |

The nav is the same on every page — the painted rail and its parchment (Components → Nav) — so the dark room does not re-light it; the grain stays at half strength (it reads as the paper of the painting); the foxing and the gutter shadow are off. Text set straight on the painting (the kicker, the clock, date rules) carries a small dark text-shadow. **The greeting is the exception inside the exception:** it hangs on the wall as a parchment scroll — `.opening` on Home takes the ivory tokens back (ink `#2b2216` on lamp-warm parchment at ~.73 opacity, so the room reads faintly through it; brass `#8b6a38`), 14px corners, a warm inset glow and a faint lamplight halo, and two dark wooden rods (`::before` on top, the `.opening-rod` button below) 9px past its edges — so the scroll sits 9px in from each side of the column (`margin-inline: 9px`), which keeps the rods inside `.chat-scroll`: past it, the scroll area overflowed sideways and a finger dragged the greeting left and right on a phone (2026-09-13); it is set tighter than the bare-paper opening (padding 20/22px, headpiece 20px, quick starts 20px above — 2026-09-13, ~18% shorter); the quick-start chips inside it are ink on paper again. No backdrop blur: the room should survive through it, not smear. Today and the Library have paintings of their own (below); Insights and Settings stay ivory. The room is not a theme, it is where Home is.

**Today: the garden** (2026-09-12). Today is set in a painted greenhouse (`art/scenery/today/background.png` → `public/today-room.webp`, 1536×1024, cover-fit, fixed) and laid out after the mockup (`art/mockups/today-mockup.png`): the page sits on the right, and the room is left open — Lumi stands in it. **Three layers, each quieter than the last** (2026-09-13, Chanté's ask — the one tall panel read as a list pasted on a painting): Lumi's words set straight on the painting, **the one Right now card**, and a faint slip with the rest of the path. Distance from now is less weight (`product/today-garden.md` §6, §35–37). It follows Home's shape (one layer, `.today-scene`, behind the shell; everything scoped to it) but not its light: Home is lamplit and re-maps the palette; Today is daylight, so **the tokens stay ivory**. The veil (`::after`, `rgba(44,30,16,…)`) is heaviest along the left and the top, clear over the room.

| Piece | In the garden |
|---|---|
| `.today-page` | Fills `.main` as a flex column, gap 16px. Below 1100px a centred 620px column; from 1100px its three layers align right at `clamp(400px, 31vw, 470px)` |
| `.today-voice` | Lumi's words, on the painting: the running head (kicker + divider), the greeting (30px) and her day line (`.today-dayline`, 19px), then once a day the capacity question (`.today-capacity`, 18px) with its answers as `.tool-link` words parted by middle dots (*Skip* in the mute ink) — no chips. The clock's treatment: parchment tokens (`--ink #f7f1e5`) and a small dark text-shadow, over a feathered shadow (`::before`, `rgba(30,20,10,.5)` blurred 26px) so it reads on any part of the room. The shadow reaches 22px past the words' sides, and 20px on phones (2026-09-13): a phone's page has only 20px of padding, so at 22px the shadow spilled 2px past `.main` and the whole page dragged sideways under a finger. No portrait — Lumi is standing in the room |
| `.today-now` | The one card, the only full paper on the page: card at .97 over a 20px blur, 10px radius, a hairline at .10, shadow .08 / .24. No kicker inside. The title (34px), the list · estimate note, *First* and the first step, then three `.tool-link` words — *Not this* · *Break it down* · *Done* — and no primary button (*Start with Lumi* removed 2026-09-13). Everything they do happens on the card: *Not this* opens its chips in place, then *Finding something that fits…* (`.today-now-working`, italic display 20px, mute) while the path is re-cut, and the new Right now carries Lumi's line under its title (`.today-now-note`, italic display 18px, ink-soft); *Break it down* shows the thinking dots, then its steps as quiet paper rows (`.today-steps` › `.today-step`: a hairline, 8px radius, 17px ink, paper-deep on hover) under a 20px display prompt, with *Smaller still* · *Keep it as it is* beneath; a failure is one italic mute line (`.today-trouble`, 15px). No circle in front of the title: the card invites starting |
| `.today-rest` | After that and Later (`.today-part`, mute kickers, 22px circles, 16px titles, no rules between rows) and the closing line (`.today-closing`, italic display 17px, mute) as its last word, parted by hairlines. Paper at .80 over a 14px blur, a lighter shadow — the garden shows through. Absent when there is nothing after Right now and nothing can wait |
| Lumi | From 1100px (landscape) she leaves the corner and stands on the flagstones behind the raised bed. The spot is in the painting's own pixels (`--lumi-x: 630; --lumi-y: 346`) mapped through the cover fit (`--px: max(100vw/1536, 100vh/1024)`), so she keeps her stone at any window size. Choosing a spot: she is always drawn on top, so her feet must sit lower on screen than anything she overlaps. She stands high enough that there is rarely 300px above her, so the bubble usually opens beside her, growing down (Lumi companion) |
| Top bar | On the painting: parchment type with a small dark text-shadow; the double rule shortens to 240px under the clock |
| Grain / foxing / gutter | Grain at .14; foxing and the gutter shadow off |

Not carried over from the mockup: the filter tabs, the plant per category, the *In Season* panel with step counts (its corner is left open), the *Plant something new* button and the quote's copy — `decisions.md` 2026-09-12 · Today, set in the garden.

**Library: the reading room** (2026-09-13). The Library (the Lists page until then) is set in a painted reading room (`art/scenery/library/background.png` → `public/library-room.webp`, 1536×1024 at quality 84, fixed, fitted to its walls: see *The fit* below): since 2026-09-13 an isometric cutaway like Home's and Today's rooms, its tiled floor running to the canvas edges under a dark ground (`#332218` above the walls, which is also the scene's own colour, so the fade-in has no seam). Since the same day's re-render its reading circle has full-size armchairs; the isometric painting before it is archived as `art/archived/scene_mockups/library-background-iso.png`, and the frontal one as `library-background-frontal.png`. **The room is the page, with what the Library holds lettered onto it** (2026-09-13, `feat/library-sections`):
- **The fit.** The painting is scaled so the room's walls (the corner posts' outer edges, painting x 128–1412) meet the window's left and right edges (Chanté, 2026-09-13): `--library-px` is `max(100vw / 1284, 100vh / 1024)`, and the painting is centred on x 770 (`--scene-cx`) and y 512, so a window narrower than 1284:1024 fills its height instead. The floor past the walls and a little of the top and bottom are cropped. The 24×16 placeholder is placed the same way, and next/image's inline `fill` box is overridden (`!important`), as is the base `img { max-width: 100% }`, which would otherwise hold the painting to the window's width and leave the right wall short of the edge. Today keeps the plain cover fit.
- **The stage.** `.library-stage` is a fixed box the size of the painting under that fit (`--library-px`), so everything on it is placed in painting coordinates and its type scales with the room. The numbers are in `components/library/room-slots.ts`, measured on the served painting (the spatial map's manifest is still v3, measured on the archived one).
- **Plaques.**
  - `.library-case` is one link per bookcase face. Side walls are `skewY` along the wall.
  - `.library-case-name` is the section's name as gilt lettering on the painted plaque: Cormorant 500, `#dcb46f`, a dark text-shadow, never under 12px, cut at a word past twice the manifest's characters per line.
  - `.library-case-glow` is warm lamplight over the plaque on hover or focus. There is no chip and no chrome at rest.
- **The table.** `.library-slip` is a small italic paper slip tilted −7° on the reading circle's table.
- **Below 768px.** `.library-plain` / `.library-plain-head` are a parchment list, visually hidden above that width.
- **The sheet.** `.library-veil` / `.library-sheet` share the Lists sheet's frame (its selectors name both). Inside it:
  - `.library-crumbs`, the trail, with brass `›`
  - `.library-head` / `.library-kicker` / `.library-title` / `.library-line`
  - **the section view:** `.library-shelf-plaque` (a dark plaque with a gilt hairline and a sparkle each side) over `.library-spines` on a wooden board, and `.library-spine` (116×180px, 18px type, `data-tone` 0–3: green, oxblood, navy, olive, chosen from the id and meaningless)
  - **the book:** `.library-spread`, two `.library-leaf` pages with a gutter that stack below 900px, each `.library-part` headed by a sparkle and `.library-part-title`, and notes carrying `.library-source` (*your words* / *Lumi's reading*), plus `.library-change*`, `.library-visit*` and `.library-revised`
  - **the table page:** `.library-papers` / `.library-paper` (`.is-section`)
- **Only in debug mode.** The plaques and the slip show only in debug mode until the Library is integrated properly (Chanté, 2026-09-13).
- **The debug parchment.** `.debug-library` (`components/library/LibraryDebug.tsx`) is a `<details>` fixed at the top right of a room (top 112px, right 24px, up to 320px wide), on the parchment tokens:
  - the frame: `#e7d3ae` with a lamp-warm centre, a `#b8945c` hairline, an inset light band and a soft drop shadow;
  - the heading: a brass `Debug` kicker and *Categories and threads* at 20px display;
  - categories: 18px display at 600; shelves: 12px tracked caps in the mute ink; threads: brass middots;
  - `.debug-library-who`: 11px italic;
  - an italic foot line over a hairline.

  The body scrolls within `100dvh − 420px`, which keeps it clear of Lumi. Hidden while a sheet is open and below 768px. A list panel on the right was tried first and taken off the same day (Chanté's ask). Everything that isn't the page itself is shared with the garden — the CSS names both scenes (`:has(.today-scene, .library-scene)`): the veil, the parchment top bar with its 240px rule, grain at .14, and Lumi's spot mapping.

| Piece | In the reading room |
|---|---|
| `.library-page` | Fills `.main` (`flex: 1 0 auto`); holds the scene and the hidden heading, nothing else |
| Lumi | From 1100px (landscape) she stands on the open floor to the right of the reading circle, below the stair bookcase — `--lumi-x: 1085; --lumi-y: 755` in the painting's pixels (the isometric room's open floor in front of the alcove's plant, left of the stair bay), mapped through the Library's fit (`--px` is `--library-px`, centred on `--scene-cx`). She covers only floor, the plant and the stair bay's cabinet, all further back than her feet, and stays clear of the nav and the crop at every desktop window. Below that, her corner |

The painting's signs and banners are **blank** (repainted 2026-09-13; the first painting, with words on them, is `art/mockups/library-background.png`). The boards are the Library's architectural collection slots in Chanté's IA (`art/scenery/library/library-spatial-information-architecture.md`): fixed places whose names would come from the user's life. Nothing writes on them yet.

---

## Typography

| Font | Weights | Source | Variable / class |
|---|---|---|---|
| Cormorant Garamond | 400 / 500 / 600 + italics | Google Fonts via `next/font` | `--font-cormorant` → `.font-display` |
| EB Garamond | 400 / 500 + italics | Google Fonts via `next/font` | `--font-garamond` → `.font-body` (default on `body`) |

- **Display** (Cormorant): the wordmark (30px, centred at the head of the nav's parchment), the greeting's title line (38px) and second line (26px, `--ink-soft`), the composer input (24px), italic asides ("Progress lives here.").
- **Body** (EB Garamond, 17px/1.45): everything else, including nav names (18px) and chips (16px).
- **Labels** (`.label`): EB Garamond 11px, uppercase, `letter-spacing: 0.24em`, `--ink-soft`, lining tabular figures. `.label-mute` for the quieter variant. Every kicker, date, and tagline in the interface is a `.label`.
- **Marginal notes** (`.pill`): the list and the estimate beside a title (*Personal · ~5 min*), set as tracked small caps in the mute ink like a source noted in the margin — no box, no fill (the boxed grey pill read as a SaaS tag, 2026-09-12). Two in a row are parted by a middle dot (`.pill + .pill::before`).
- **Figures:** `body` asks for old-style proportional figures (`font-variant-numeric`). The display face honours it, so a number in any display-set line sits on the baseline the way a book sets it; the Google-served EB Garamond has no such feature and shows lining figures regardless (`decisions.md`, 2026-09-12). Labels and marginal notes ask for lining figures explicitly, so caps and numerals stand at one height.
- **Line breaks:** display lines (`.opening-title`, `.opening-line`, `h1`, `h2`, `.session-goal`, `.session-question`) are `text-wrap: balance`; running text in messages is `text-wrap: pretty`, so no word is stranded on its last line.

Heading defaults: none imposed. Headings are display-font lines set per surface; there is no `h1` style to fight.

**Running heads:** every page but Chat opens with the same running head — its name as a `.label` kicker with a `Divider` beneath — and Insights then sets Lumi's 48px avatar beside the display-serif opening line, the way the greeting does on Chat. Today sets its opening line on the painting without the portrait (2026-09-13): Lumi is standing in the room. The browser tab reads the same way (`layout.tsx` title template): *Today · Coherence*, *Library · Coherence*; Home is just *Coherence*.

---

## Spacing & Layout

- **Shell:** CSS grid `calc(var(--rail-x) + var(--rail-w)) minmax(0,1fr)` — the rail is 64px wide, 10px in from the corner; the nav's parchment floats over the page and never takes a column, **viewport-height** (`.shell { height: 100dvh }`); main `.main` is a scrolling column (`overflow-y: auto`, padded `36px 48px 0`). The chat page overrides this with its own scroll region so the composer stays fixed.
- **Content width:** `max-w-[1080px]`, left-aligned within main.
- **Mobile breakpoint:** `767px` (CSS). The shell is one column over two rows, `minmax(0,1fr) auto`: the page on top, full width, and the nav as a bar along the bottom (62px, 6px in from the sides and the floor), so the page and the docked composer stop above it (Components → Nav → Phones). **Tap targets on phones are at least 44pt** (Apple's minimum; 2026-09-13, UX review 1): what is drawn smaller keeps its look and gets a transparent `::after` reaching past it — `.circle` (−11px all round), `.tool-link` (−12px above and below, −6px at the sides), `.lists-due-btn` (−7px/−4px), `.lists-more` (−5px), `.lists-close` and `.lists-add` (−3px) — and the Lists tabs, whose scroller would clip a pseudo-element, are 44px tall. Lists rows take 12px of left padding on phones (6px elsewhere), so the ticks' hit area isn't clipped by the list's scrolling edge. Measured in Safari on the iOS Simulator by hit-testing outward from each control's centre.
- **Radii:** cards `6px` (a plate, not a bubble); user notes `8px` with a `2px` tail corner; chips, icon buttons, send `999px`; the composer `38px` — half its height at rest, so one line is a full pill and a long message keeps rounded corners rather than ends that curve over its first and last lines.
- **Shadow:** one token, `--shadow` (two soft layers). Nothing else casts.

---

## Reusable CSS Classes (`globals.css`)

| Class | What it does |
|---|---|
| `.label` / `.label-mute` | Tracked small caps (see Typography) |
| `.rule` / `.rule-short` | 1px full rule / 34px short rule |
| `.rule-double` | Thick-thin "Oxford" rule under the running head (top bar) |
| `.divider` / `.tailpiece` | Ornament layouts: hairline · diamond · hairline under every kicker; hairline · hedera · hairline closing a page with nothing more to say (see Ornaments) |
| `.opening` | The greeting as a chapter opening on bare paper: `.opening-head` (hairline · asterism · hairline across the measure), `.opening-body` (portrait + text), `.opening-title` (38px display), `.opening-line` (26px display, ink-soft). `.is-compact` within a sitting: same break, smaller (26/20px, 44px portrait). On Home only: `.opening-rod` (the bottom rod, a button), `.opening-roll` / `.opening-sheet` (what folds away), `.opening-rolled` (the title line left between the rods) and `.is-rolled` |
| `.card` | Card surface: `--card` bg, rule border, 6px radius, `--shadow` |
| `.chip` | Pill button on paper-deep; hover darkens, active nudges 1px |
| `.icon-btn` | 44px round icon button on paper-deep |
| `.chat-page` / `.chat-scroll` / `.composer-dock` | Chat layout: flex column filling `.main`; the transcript scrolls; the dock sits below with a paper fade above it — except on Home (2026-09-13), where the dock has no background: the fade was a dark box on the painting, with a hard edge where the column stops at the companion lane, and the pill's glass already holds the lines scrolling behind it. `.chat-scroll` is a size container and its `::after` is one view's height of nothing (`100cqb`, 2026-09-13): the greeting can sit at the top of the view on a fresh open and hold still while it rolls up, and the whole conversation can be scrolled just out of sight (on Home, to see the room). Replaces the measured `room` padding `MessageList` used to add |
| `.today-scene` / `.today-page` / `.today-voice` / `.today-now` / `.today-rest` | Today only: the painted greenhouse behind the shell, the page column, Lumi's words on the painting (`.today-capacity` inside), the one Right now card and the faint slip with the rest (`.today-part`, `.today-closing` inside) (see Color Palette → Today: the garden) |
| `.home-scene` | Home only: the painted room, fixed behind the shell, with its dimming `::after`; its presence (`.shell:has(.home-scene)`) re-lights every token on the page (see Color Palette → Home: the room) |
| `.composer` | The pill input container: the paperclip (`.composer-attach`, a 44px `.icon-btn`, 2026-09-13 — it shares a file; the inert `+` it replaces had been removed earlier that day), the textarea, then send, 14px in from either end. The `textarea` inside is display-font, auto-grows to 160px, then scrolls, a line scrolled part-way out fading over its 8px padding (a mask) rather than cut. Radius 38px, not 999px (2026-09-13): a pill at rest, a rounded rectangle once it grows. `.is-listening` = brass border + soft ring while voice input is on, and while a file is dragged over the pill. From 768px it is set to a measure — `clamp(min(100%, 460px), 70%, 100%)` of the chat column, centred — and `.composer-foot` (the Voice / tagline line) keeps the same measure; phones keep the full width |
| `.send` | 54px forest circle (52px on phones; 64px until 2026-09-13); disabled at 45% opacity |
| `.tool-link` | Text+icon quiet button (Voice under the composer, and quiet text actions elsewhere; Add file became the composer's paperclip, 2026-09-13) |
| `.nav-item` | One row of the nav, one link per place: `.nav-icon` on the rail, `.nav-mark` (the star on the rail's rule) and `.name` on the parchment; `[aria-current="page"]` lights the icon's tile, shows the star and tints the name |
| `.pill` | Marginal note beside a title (see Typography): tracked small caps, mute ink, no box; `·` between two |
| `.row` / `.row-quiet` | A list row: hairline beneath, 12px (8px quiet) of air; last row has no rule |
| `.circle` | The complete/reopen circle: hairline ring, brass on hover, forest fill when `.is-done` — and the tick draws itself in one stroke (220ms, the path carries `pathLength="1"`) |
| `.chat-now` | The part of the transcript said this visit: each `.msg` in it rises 6px and fades in over 260ms as it lands (`msg-in`); earlier messages are simply there |
| `.thinking-dots` | Lumi's three slow brass dots — in the chat while she thinks, and in Today's Right now card while the path is being cut (no grey skeleton bars). In a chat client, render `<ThinkingDots />` (`components/chat/chat-client.tsx`; `label={null}` when the parent already says "Lumi is thinking"). Under reduced motion they hold still at 0.6 |

---

## Component Patterns

### Greeting (`components/chat/Greeting.tsx`)
A chapter opening, not a card (2026-09-12: the boxed `.card.plate` read as a widget dropped onto the page). Set on the paper itself the way a chapter begins in a book: a headpiece across the measure (hairline · asterism · hairline, `.opening-head`), Lumi's portrait in the margin (her head, nothing behind it), the first line as the title (38px display), the second beneath it in the softer ink (26px), then the quick-start chips. Lines come from `core/ai/greeting.ts` — never hardcode copy in the component. It is the chapter mark of the transcript (`MessageList`): everything from before this page open above it, this visit below; the page opens scrolled to it. No rule or box between the earlier messages and the greeting — the headpiece and the white space are the break. Within a sitting (`compact`) it is a running head: the same headpiece, a 44px portrait, 26/20px lines, no chips. On a phone the full opening takes the 44px portrait too (2026-09-13, UX review 1): the 64px one narrowed the title to two lines and the scroll filled most of the first screen. On Home the conversation's top edge fades over 18px (a mask on `.chat-scroll`), so paper passes out of view under the clock bar rather than being cut; its `scroll-padding-top` is 26px, which keeps the greeting's top rod just below the fade when the page opens on it. It matches Insights' opener (her portrait beside a display-serif greeting on bare paper); Today's opener, set on its painting, drops the portrait. On Home (2026-09-12) the same opening hangs on the room's wall as a parchment scroll — ink on paper inside it, a wooden rod past its top edge (the bottom rod was taken off 2026-09-13, Chanté preferred it without: the parchment's own edge ends the scroll); since 2026-09-13 rounder (14px), a little translucent and warmer, and set tighter — because it is the one light thing in a lamplit room, the way the tagline hangs on a scroll in the mockup; the look is only the tokens under `.shell:has(.home-scene) .opening`. **It rolls up** (2026-09-13, Chanté's ask): the bottom edge is a real button (`.opening-rod`, `aria-expanded`, a 28px-tall hit area where the 10px rod was; nothing drawn since the rod came off, so it's a quiet affordance: tap just under the scroll) — tap it and the sheet (`.opening-roll` › `.opening-sheet`, rows `1fr → 0fr` over 420ms while it fades, then `inert`) folds up until only the title line remains between the rods (`.opening-rolled`, 22px display, ink-soft, centred); tap again to unroll. The top rod stays where it hangs while it rolls: `.chat-scroll::after` always leaves a view's height below the conversation, so the page never runs short and doesn't slide. Rolled lasts for this page open only — never stored. Under reduced motion it changes without transitions. Away from Home there are no rods, so the rod button and the rolled line are not shown.

### Lumi sprites (`components/chat/LumiSprite.tsx`)
The single source of Lumi's drawings.

**The body is the hands-free Lumi** (2026-09-13, settled the same day — Chanté: "Lumi will be hands free often"; `docs/art-direction.md` §4a, bet 8): `public/lumi-free.webp`, cut by `scripts/cut-lumi-free.py` (helpers in `scripts/lumi_cut.py`), a 27×6 grid of 176×208 cells (wider than 160 so a prop fits beside her; her hood's centre sits at 45% of the cell). Rows 0–2 the nine-cell **breath** with open, half-shut and shut eyes; row 3 the 24-cell **wave** (`art/lumi/lumi-free-wave.png`); row 4 **hands**, 16 cells, her empty hands meeting in front and parting (`lumi-free-idle.png`); row 5 **pickup**, three cells fading a stack of books in and 24 of her taking the top book and holding it to her chest (`lumi-free-pickup.png`), played forward, held, and backwards as the set-down (`LUMI_LOOP_CELLS`). The last three have their eyes open only. The cut is one spec per loop (`LOOPS`: sheet, cells, the region the moving part may be in, rest cells, phase holds, monotone lower cloak, ease, prop), and **every loop is held to one drawing**, the wave sheet's first cell: each cell is scaled to `FIGURE_H`, placed by the hood, aligned on what holds in that loop (the right half for the wave, the head and boots otherwise) and keeps that drawing's pixels except where its moving part differs; the two cells next to a rest cell ease from the rest drawing into the loop's own, anything dark kept out of the blend (each sheet draws the cloak a little differently, and it swapped shape at the join); a prop is kept in the rest cells and fades in over three cells; and **her boots below the hem are held** in every cell of every loop, the breath included, except where a cell's own cloak hangs over them (`hold_feet`) — with no baked shadow under them Chanté saw her feet jitter: the loops took the boots' upper rows from each sheet's drawing (0.25–0.38px shifts at the start of the wave and the hands) and the regions stopped 8px above the ground, which also kept a stub of a resting hand. The moving regions now run to the ground. The matte cuts her shadow away with the ground (`Sheet(shadow=False)`): the page draws it (Lumi companion → her shadow and the room's light). It also cuts out ground enclosed between the boots (it showed as a grey-blue patch), and ruled lines a generator draws between cells are painted over before segmentation. Held parts on the cut cells, measured on alpha by `scripts/preview-lumi-loop.py`: hood and boots move 0.00px through the wave and the hands, ≤ 0.5px through the pick-up (the stack beside her boots). The sheet is 899 KB (the lantern body was 461 KB); the pick-up's rows are most of it.

**The heads and the lantern body are retired** (2026-09-18). `public/lumi-heads.webp` (the six expressions cut from `art/lumi/lumi-lantern-idle.png`, unshown since the avatar stopped using them on 2026-09-13), `public/lumi-idle.webp` (the lantern body, unplayed since the hands-free Lumi) and the cut that wrote them, `scripts/cut-lumi-idle.py`, are gone — `animation-pipeline.md` backlog #2's second option. They were kept in case the avatar took expressions again, but they are a different hood from the avatar's drawing now (below), so they could not be used beside it; an expression, when one is wanted, is a drawn eye shape over the avatar's face (`art-direction.md` bet 1). The source sheets stay in `art/lumi/` (the lantern sheet is still the style reference every prompt attaches), and what its cut taught — per-row scaling, the quarter-pixel settle, per-phase head holds, the warm shadow and the lantern's glow — lives on in `scripts/lumi_cut.py`, `art/README.md` and git history. `LumiSprite` draws one cell of the one sheet at a height via `background-position`; `idleCell` addresses them. A new state is a new cell, never a new component. Adding a sheet — the prompt, `scripts/measure-lumi-sheet.py`, `scripts/preview-lumi-loop.py` and what the cut corrects — is written up in `art/README.md`; the process in `docs/animation-pipeline.md`.

### Lumi avatar (`components/chat/LumiAvatar.tsx`)
**Just her hooded head, nothing behind it** (2026-09-18, Chanté's ask, as `art/mockups/chat-mockup.png` draws it). Her drawing `art/lumi/lumi-avatar-head.png` (2026-09-15: the hood, the ribbon and its three medallions, in the costume the Lumi in the corner wears, on transparency) is cut by `scripts/cut-lumi-avatar.py` into `public/lumi-avatar.webp`: the whole drawing trimmed to its own extent, fitted to 98% of a 272px square and centred, transparent around it (27 KB). The component draws it as a background image (`bg-contain`) in a square span with no circle, ring or ground, so the paper or the painted room shows around her; the cream hood reads on both. It replaces Chanté's painted medallion (2026-09-13 to 09-18: `art/lumi/avatar.png`, a forest disc with a brass rim, cut to its circle), and before that the lantern sheet's heads in a forest circle. Props: `size` (default 68): 68px beside the greeting (44px within a sitting), 48px on Insights' opener, 36px beside each of her lines. It is one drawing, so there are no expressions. Never larger than the text it accompanies; never decorative on its own.

### Lumi companion (`components/shell/LumiCompanion.tsx`)
Lumi, full figure, standing a little in from the bottom-right corner of every page (`.companion`, fixed, 150px tall, ~127px wide (the hands-free cell; the lantern one was ~115, and the hood stays 57px in from the figure's right edge, so her spots in the rooms didn't move) at `right: 56px; bottom: 40px` — 28px/2px read as tucked into the corner). On Today, from 1100px, she stands in the painted room instead (Color Palette → Today: the garden). In a room her size is the room's, not the corner's (2026-09-13): `--lumi-h`, a height in the painting's own pixels times the room's fit, so she keeps her size against the furniture at any window: Today 160, the Library 134, each her 150px at 1440×900. `LumiCompanion` sizes the figure and its sprites from it (`var(--lumi-h, 150px)`), and the feet offsets that place her and the dev cue strip's gap scale with it. She is fixed to the viewport, outside the content column: on desktop `.chat-page` keeps a lane clear on its right (`--companion-lane`, 140px on top of `.main`'s 48px padding, added to the column's max-width) so the docked send button never sits under her at any width, whether the column fills `.main` or is centred inside it. Hidden under 768px, where there is no room for a lane, and the lane drops to 0. Idle life: the breath loop at ~320ms/frame (one breath ≈ 3s), every frame fading in **on top of** the previous so she never turns translucent (two frames mounted, keyed by loop and frame, the fade a mount animation — so a hand-over between loops fades like any other step; the fade lasts 80% of the frame time of the faster of the two loops involved, capped at 260ms, via `--fade` on the figure — a fixed 260ms on 120ms frames left each frame a third of the way in when it was replaced and snapped to full, a soft doubling of the hood; and a hand-over fades at the variation's pace both ways, since the way back at the breath's 260ms read as a slow turn of the head); every 20–45 s one pass of a variation, then back to breathing — her empty hands coming together in front of her and parting (`hands`, 16 frames at 200ms ≈ 3 s, fade 160ms; the hands-free Lumi, 2026-09-13; with more than one variation, never the same one twice running); **a wave when you arrive** (`REACTIONS`, 2026-09-13) — the page opened, or its tab shown again, after 30 min or more with no tab of the app visible in this browser (`localStorage["lumi-last-seen"]`, refreshed each minute while a tab is visible and when it hides; `ARRIVAL_GAP_MS` matches `SITTING_GAP_MS`), or a first visit in the browser: 900ms later it is queued, plays once from the next rest frame at 120ms a frame (24 frames ≈ 3 s, fade 96ms) and hands back to breathing without scheduling a variation; a blink every 2.5–6.5 s (half · shut · half, ~0.3s, occasionally doubled) by switching the eye row. Every loop is one drawing, handed over at the rest cell — the micro variations from `lumi-idle.png` were separate drawings, read as image swaps and were dropped. All of it stops under `prefers-reduced-motion`, followed live (turn it on mid-visit and she settles at rest with her eyes open; off, and she starts breathing again). While nobody can see her — a hidden tab, or under the Lists sheet at `/lists` — the frames and blinks wait and carry on from the same frame when she's seen again. The loop lives in `LumiFigure` (same file), so its frames re-render only her figure, never the bubble; its timers leave their set as they fire. **Her shadow and the room's light on her** (2026-09-13, hands-free branch) are drawn by the page, not baked into the sprite — the cream shadow the cut used to paint read as a pale smudge on a painted floor and made her look pasted on. Inside `.companion-figure`: `.companion-shadow`, a soft oval under her feet (45% across the mirrored cell, 96.2% down) that darkens whatever is behind it with a backdrop filter (`--lumi-contact`, brightness) under a radial mask, so it is the floor's own colour — a blend mode can't reach the painting, since the companion is its own layer; `.companion-cast`, her current frame as a mask (`LumiSprite mask`) filled with the room's shade (`--lumi-shade`), flipped under her feet, squashed to `--lumi-cast-length` and leaned by `--lumi-cast-lean` away from the room's light, blurred 1.5px at `--lumi-cast-a`; and `.companion-light`, her silhouette filled with the room's light (`--lumi-light`) multiplied over her at `--lumi-light-a`. She doesn't move on hover (a 3px lift, with the shadows held on the ground under it, read as her hovering off the ground and was removed, 2026-09-13). On the paper only the contact shadow shows; Today and the Library set a cast and a light (low sun from the back left, long shadows to the front right; light from the left windows, shadows reaching right); Home sets a lamplit tint and no cast, since she stands in the corner there, not in the room. She is also the one control in the corner: `.companion-btn` wraps the figure (the pointer cursor and the global brass focus ring; no movement on hover), and `.companion-bubble` is the speech bubble that opens above her (or beside her where there isn't room — below) — a 330px `--card` plate with a `--rule-strong` hairline, a 12px rotated-square tail over her head, a display-serif textarea (21px, one line at rest — the bubble rests ~58px tall, down from ~76px — growing with what you write to 168px, then scrolling on a thin `--brass-soft` scrollbar) with its own `.companion-send` (2026-09-13; it had carried the chat's `.send`, which came later in the stylesheet and won at 54px): a 34px forest circle with a parchment arrow and a brass-soft hairline ring set 2px off the plate, brass on hover; with nothing to send, a `--paper-deep` disc with a brass hairline and a brass arrow rather than a 45%-opacity fade; a 44px hit area, and above it the exchange (`.companion-said`: your line as a right-aligned `--paper-deep` bubble like the chat's `.msg-user`, 15px, sliding up 8px over 220ms on each send; `.companion-reply` at 17px with the chat `.ledger` beneath) under a hairline. While a turn streams, `.companion-send` is a stop (2026-09-13): the same forest medallion with a parchment rounded square, `aria-label="Stop Lumi"` (Home's `.send` swaps the same way). The bubble never runs off the window: on open (and on resize) the component measures the room above her figure (less the 14px gap and 24px clear at the top); with 300px or more it opens above as before, otherwise beside her (`data-side="right"`, or `"left"` when 330px won't fit to her right) — absolutely placed level with her top, the tail moved to its side at 34px down, pointing at her hood — and grows down. It is a flex column capped at `--bubble-max` (560px, or the room it opens into); `.companion-exchange` takes the squeeze and scrolls on the same thin `--brass-soft` scrollbar, pinned to the newest line unless you've scrolled up. Fades in over 160ms (off under reduced-motion). Its text can be selected and copied (`user-select: text`, 2026-09-13 — `.companion` sets `none` so a tap on her never highlights anything, and the bubble had inherited it). Stacks in the `.companion` flex column so she stays put when it opens, at `z-index: 2` so it sits over her figure — the button comes later in the DOM, and without it her cast and contact shadows painted across the bubble's tail and edge where it opens beside her (Chanté, 2026-09-13). Testing note: Chrome freezes animations in a hidden tab (the Claude-in-Chrome automation window is usually occluded), and a capture of a frozen tab shows her mid-fade, ghostly — that is the capture, not the page. Dev only (`NODE_ENV === "development"`): `.companion-debug`, a row of tiny `.companion-debug-btn` chips fixed just above her head with a `.companion-debug-fold` handle at the right end that folds them away toward it (`.is-folded`, remembered in `localStorage`), cues a variation, a reaction or an action (played at the next rest frame, the same hand-over as a scheduled one — `hands`, `wave` and `pickup` today; the pick-up is an `ACTIONS` loop, played only from here because nothing in the app asks her to pick something up yet: 62 frames at 140ms ≈ 9 s, the stack fading in, the book lifted, held, set down, the stack fading out) or a blink (now) via a `lumi:cue` window event — for checking the loops without waiting; hidden with her under 768px, and while the bubble is open (it sits where the bubble opens).

### Lead card (`components/insights/LeadsSection.tsx`)
Insights opens the way Today does — the running head (kicker + divider), then Lumi's 48px portrait beside a display-serif line (30/36px) — and then a column of `.card`s (gap 20px), one per thing she noticed: the title in display serif (26/28px), her *why* line in body 17px `--ink-soft`, a `.label.label-mute` meta line (sender · subject · when · date), and the two answers — a `.chip` (*Still needs doing*) and a `.tool-link` (*Let it go*), the same weights as *Not this*'s chips and *Keep it*. Answered, the row's controls become one ledger-style line (`.ledger-mark` ✦ + *On your list.* / *Let go.*). The page's one flourish is the divider under the kicker; the tailpiece appears only when there is nothing. No counts, no badges, no unread dot on the nav item.

### What Lumi knows (`components/settings/MemoryItem.tsx`)
Settings opens with the running head (kicker + divider), then a display-serif title (30/36px) and one body line (17px `--ink-soft`). Each kind is a `.label` kicker over a list of `.row`s: the belief in display serif (22px), then one line holding its provenance (`.label.label-mute`) and two `.tool-link`s, *Correct* and *Forget*. Correcting swaps the sentence for a textarea in place — display serif 21px on `--card`, 6px radius, a hairline of `--ink-mute` at 40% that turns brass on focus — with a `.chip` *Save* and a `.tool-link` *Cancel*. Forget asks once on the same line (*Forget this for good?* · `.chip` Forget · `.tool-link` Keep); done, the row becomes one ledger-style line (`.ledger-mark` ✦ *Forgotten.*). A refused correction is one 15px `--ink-soft` line under the row. No cards, counts or badges; the tailpiece closes the page.

### Ornaments (`components/ui/Ornament.tsx`)
Printer's marks, the engraved vocabulary an old book decorates with. They are not icons and never stand in for one (the nav's places have line icons — Icons, below). Inline SVG in brass (`.ornament`), hairline weight, `aria-hidden`; they decorate, never carry meaning. Three glyphs: **Diamond** ✦ (the smallest mark: dividers, visit rules), **Fleuron** ❦ (the hedera, an ivy leaf with a curled stem: tailpieces), **Asterism** ⁂ (a pause in the text: the greeting's headpiece, where this visit begins after whatever was there). Two layouts: `Divider` (replaces the short rule under every kicker) and `Tailpiece` (closes an empty page). The nav's parchment has its own marks, after the mockup: **Sparkle**, a slender four-pointed star with its upright arms longer than its cross, and `Flourish`, a sparkle between hairlines that fade toward their ends. **One flourish per surface** — the greeting has its headpiece, a page has its tailpiece, a sitting break has its diamonds. If a surface already has one, it does not get another. The nav's parchment is the exception, drawn after the mockup: a short flourish over the wordmark, one under the tagline and a sparkle at the foot.

### Quick starts (`components/chat/QuickStarts.tsx`)
Three chips + a round "another way in" button (*Body double* went with sessions, 2026-09-13). They are starting points, not modes; from M2 they send a canned first message.

### Session bar — removed 2026-09-13
The ruled strip above Home's composer while a focus session ran, and its check-in plate, were deleted with focus sessions (`components/focus/SessionBar.tsx` and its `.session-*` CSS). If sessions come back, git history has both.

### Composer (`components/chat/Composer.tsx`)
Paperclip · auto-growing textarea · send. A ~76px pill at rest (2026-09-13, Chanté's ask: shorter): 10px vertical padding, the 44px paperclip (`.icon-btn.composer-attach`, which shares a file), a 54px send. A long message grows it to a 160px textarea, then scrolls; the corners stay 38px round, so no line runs under a curved end. Files waiting to go sit above the pill as `.shared-file` chips in `.composer-files`, on the same measure: paper-deep, a rule hairline, 8px radius, 15px ink-soft; a 26px thumbnail (`.shared-file-mini`) or a brass page glyph, the name truncated, a mute × (`.shared-file-remove`). In a user note, `.msg-user-said` stacks what was shared above the words: a photo from this page open as itself (`.shared-file-photo`, at most 260px, with the note's radii and tail corner), anything else as a chip. On Home the chips take the user note's tint on glass. A line about a file Lumi can't take sits under the pill where voice errors do. On a wide page (from 768px, 2026-09-13) the pill is ~70% of the chat column (never under 460px), centred on the column under the greeting rather than running edge to edge; phones keep the full width. Below it, on the same measure, the Voice toggle (only where supported; `.tool-link.is-listening` breathes in brass) and the closing label ("You don't have to do it alone."). Enter sends, Shift+Enter newlines. Voice is `useVoiceInput` (`components/chat/useVoiceInput.ts`): Web Speech API, continuous + interim results, transcript appended to the typed text.

### Nav: the rail and its parchment (`components/shell/Sidebar.tsx`)
After `art/mockups/rail-nav.png` (2026-09-13). Two pieces, the same on every signed-in page, painted or ivory; the sign-in and sign-up pages have no nav and no rail column (`isPublicPath`, `src/lib/public-paths.ts`, the same list the proxy lets through):

- **The rail** (`.nav-rail`) is **Chanté's painting** (`art/ui/nav-rail.png`): mottled olive green (about `rgb(37,38,17)`) with a brass rule inset along both sides, chamfered ends, the **compass star** and a spark at its head, a spark and a **crescent moon** at its foot. `scripts/cut-nav-art.py` keys it off the checkerboard painted into the PNG and cuts three pieces into `public/`: `nav-rail-head.webp` and `nav-rail-foot.webp` keep their proportions, and `nav-rail-body.webp`, the plain length between, stretches to the rail's height (the numbers in the CSS calc()s are the source's pixels: 118 wide, a 267px head, a 236px foot). It floats 10px in from the top-left corner, 64px wide, `min(100dvh − 20px, 780px)` tall (at least 560px), with a soft drop shadow, and stops short of the floor like the mockup. The painted star is the **pin button** (`.nav-toggle`, transparent over it; a warm radial glow on hover, full while pinned). Between the ends, one line icon per place (`NavIcons.tsx`: house, sun, open book | bulleted list, sprig | gear) in the painted brass (`#d4ad6c`; `#f3d292` on hover and for the open place). The open place's icon sits on a lit tile (42px, 8px radius, centred on the icon past the rail's inset) and **a brass star sits on the rail's painted right-hand rule beside it** (`.nav-mark`) — the diamond that marked the open chapter, moved onto the rail.
- **The parchment** (`.nav-panel`) is **Chanté's painting** too (`art/ui/nav-parchment.png`): warm aged paper (about `rgb(248,224,185)`) with a browned rim, rounded corners, a thin brown rule inset along its edges and small flourishes with sparkles at the corners. `scripts/cut-nav-art.py` keys it off its painted checkerboard by saturation and cuts a head and a foot (the corner flourishes, 67px each at the pieces' 384px width) and a body that stretches, all three layered in one background on `.nav-panel::before` with a soft drop shadow. The sheet is **192px wide, 4px clear of the rail**, from 20px below the rail's top, on the parchment tokens it shares with Home's greeting scroll. Top to bottom: a short `Flourish` (72px), the wordmark (30px display), *A quieter way forward.* (italic display 17px), a `Flourish` across the full measure, the names (18px body, each beside its rail icon: one row, one link, only as wide as its name tile, at least 168px, so the parchment beside it still pins; **the rows fall in three groups, each set apart by a break** — the rooms (Home, Today, Library); the tools (Lists, Insights; Lists alone while mail is switched off, 2026-09-13), **32px further down** (`.nav-tools`, 2026-09-13, Chanté: "separate the rooms from the tools"); and Settings, a utility, **64px further down** again (`.nav-utility`, 2026-09-13, Chanté's ask) — rail icon and name moving together. A soft divider marks each break: a brass hairline on the rail (up to 30px), fading at both ends, centred on the icons and halfway down the gap. On a short window the breaks give way first, so no row slides under the moon's button: the rail's room past its rows (`--nav-spare`, none at a rail of 638px or less) goes to the tools' break first, then to Settings'; each hairline narrows with its gap and is gone when it is. Below a 638px rail the rows themselves shorten, from 50px down to 38px, keeping the last icon clear of the moon, and *Progress lives here.* with a sparkle. The open place's name has a brass tint (`rgba(139,106,56,.16)`), a hovered one a faint ink tint.

**How it opens.** Folded, the parchment and names are hidden and let the pointer through, and each row is only as wide as the rail. It is **shown** while the pointer is over the nav (`@media (hover: hover)`), while keyboard focus is in it (`:has(:focus-visible)`) or when pinned (`data-pinned`, from 768px): the three ways in set the same custom properties (`--nav-o`, `--nav-v`, `--nav-x`, `--nav-pe`, `--nav-reach`). It fades and slides 8px in over 160–200ms; folding waits 180ms first, so a pointer that slips off and back doesn't flicker it. **A click on the top half of the rail (the compass star's half) or anywhere on the parchment that isn't a link pins it out**, still floating: the page never moves for it (a first pass docked it beside the page, and Chanté didn't want the chat box shifting right), so on a narrow window a pinned sheet can cover the left edge of a page. Clicking again folds it — at once, even with the pointer still over it: `data-resting` holds hover off until the pointer leaves the nav (without it the sheet stayed under the pointer and looked stuck open). **A click on the bottom half of the rail (the moon's half) locks it in** (the moon is `.nav-lock`, a button with `aria-pressed` and a cooler glow while locked, softer than the pinned star's — 80%, where a hover is 50%): hover no longer opens it (`data-locked`) until that half is clicked again or the nav is pinned. Which half is judged by where the click lands, since the gaps between the icons belong to the list laid over the rail; keyboard focus still opens it, so tabbing still shows where you are going. The mode is a cookie (`coherence_nav=pinned|hover|locked`, a year; an old `folded` reads as `hover`) the layout reads, so the nav is right from the first paint. The rail's height is one token, `--rail-h`, so the moon's button follows the painted moon at any window height. The compass star glows while pinned. Transitions are off under reduced motion.

**Phones** (<768px, 2026-09-13, Chanté's ask). The rail lies down as a **bar along the bottom** of the shell (its own grid row, 62px tall, 6px in from the sides and the floor), so nothing has to be opened to see where a place is. The bar is the rail's painted body (`nav-rail-body.webp`) on `.nav-rail::before`, turned a quarter turn so its brass rules run along the top and bottom, as long as the viewport less the insets, its corners chamfered 9px by a `clip-path` in place of the painted ends (the star and moon heads are too long for a phone's width). The six places share it equally (Settings takes no gap and no divider here): each icon (28px cell, its lit tile 36×34px) with **its name under it** (13px body, the painted brass, `#f3d292` for the open place, no tint), and the open place's brass star on the bar's top rule above it (`.nav-mark`). No parchment, no compass star or moon buttons, no pin or lock (`.nav-toggle`, `.nav-lock`, the sheet, `.nav-head` and `.nav-foot` are `display: none`; `Sidebar`'s click handler does nothing below 768px), so the cookie's mode only matters from 768px.

### Lists sheet (`components/lists/ListsSheet.tsx`)
After `art/mockups/lists-mockup.png` (2026-09-13). A **veil** (`.lists-veil`, fixed, `rgba(24,16,8,.38)`, z-index 20: over the page, under the nav's 30, so the parchment with the names still slides out over it) holds the **sheet** (`.lists-sheet`): up to 1180×840px, centred in the room right of the rail (28px clear), on the parchment tokens it shares with the nav's panel and Home's scroll, warmed for it (ink `#33281b`, brass `#9a7440`, rules in brass at 24% and 44%). Painted in CSS after the mockup (2026-09-13, Chanté's ask: "the colours, accenting, and bordering more like this mockup"): aged paper (`#e7d3ae`, lighter toward the middle, browning toward the edges), a 1px brass border (`#b8945c`, 4px radius) with a pale paper band and a brass hairline inside it and a lighter band and a darker line outside, along its top **the greeting scroll's rod from Home** (2026-09-13, Chanté's ask: a 12px bar of dark wood, `#8d6538` → `#55391c`, 6px radius, 12px past each side and 8px above the edge, with the scroll's small drop shadow; on `::after`, full width and square on a phone), **brass corner brackets** at the foot (24px, 3px, on `::before`), a `Sparkle` in each foot corner, and one deep drop shadow. It fades in and rises 8px (180–220ms; none under reduced motion). While it's open the companion is hidden (`body:has(.lists-sheet) .companion`).
- **Head** — `SprigIcon` at 58px in olive (`#5f5a34`), *Lists* (display 50px) over the italic tagline (display 21px), then on the right a pill search box (280px, a tan border `#c8ac7c` on near-white paper with a faint inset shadow, brass on focus, italic placeholder) and **Add task** (a dark olive pill, `#5b5631` → `#423f1f` with a darker rim and a lit top edge, gold text `#f2e3b8`, display 19px). × sits in the top-right corner (38px round, a paper-deep tint on hover). A brass rule under the head, fading at both ends.
- **Tabs** (`.lists-tab`) — a `Sparkle` leads the row; 6px-radius buttons in display 18px on a pale paper gradient with a tan border (`#cdb488`, brass on hover), each with its glyph in dark ink (`ListGlyphs.tsx`: grid, mortarboard, briefcase, leaf, hourglass, a bookmark for any other list, check, and two calendars for the quick views; drawn at 1.8 stroke here). The pressed one is the Add button's olive with gold text and glyph. They scroll sideways when they don't fit.
- **Side column** (230px) — framed by two brass rules, one along its top fading in from the left and one down its right edge fading out at the foot, with a `Sparkle` where they meet. The same views as rows of body 17px with glyphs, the default lists' in their own colours and softly filled (School `#6a6391`, Work `#7b5634`, Personal `#6d7a3f`, Completed `#7f9a57`; others dark ink); pressed, a tan tint (`rgba(176,140,88,.28)`). *Quick views* as a display 20px head over a rule, and a `Tailpiece` at the foot where the mockup had a quote.
- **Rows** (`.lists-row`) — a grid: the 24px `CompleteCircle`, title (18px) with the next action under it (15px mute), the list tag (15px on one of four tints with a matching ink, `data-tint` 0–3 in the order of the user's lists: lavender grey, peach, olive, tan; neutral for Unsorted), the date (16px ink-soft; *Today* has a brass `Sparkle` after it, as in the mockup — an accent, never an alarm; since 2026-09-13 the date is a button, `.lists-due-btn`, and an undated row's empty place shows an italic mute *Add date* on row hover or focus, always faintly where there's no hover — tapped, it becomes a 100px field with a brass underline, `.lists-due-input`, dashed while what was typed can't be read, with a 13px italic hint beneath, `.lists-due-hint`), the ⋯ button (34px round). Circles in tan (`#bea47c`); a brass hairline between rows. A done row's title is mute. The ⋯ opens a small paper menu (`#f6eedc`, 8px radius, a shadow): a tracked small-caps *Move to*, the other lists, a rule, *Let it go* — a plain disclosure of buttons, not an ARIA menu (Tab walks them; Escape folds it back to the ⋯). A move, let-go or undo that didn't land writes one line under the title, `.lists-row-hint` (14px italic mute; empty and heightless otherwise, and a mounted `role="status"`). The sheet is a modal for the keyboard (`components/ui/useDialogFocus.ts`): focus moves into it, Tab and Shift+Tab wrap inside it, and closing hands focus back to where it was.
- **Add line** (`.lists-add-line`) — one pill input with two `.chip`s (Add · Done); Lumi's reply and `Ledger` under it, or her three thinking dots.
- **Narrower** (<1100px) the tools wrap under the title. **Phones** (<768px) the sheet fills the page above the nav bar with square corners; the sprig and the side column go, the quick views join the tabs (`.lists-tab-quick`), rows drop the tag. The tabs are 44px tall there, and while tabs run past the right edge the strip fades out over its last 36px (`ListsTabs` sets `data-more` from the scroll position; a mask on `.lists-tabs[data-more]`), so *Completed* and the quick views don't hide unannounced (2026-09-13, UX review 1).
- No counts, no red, no badge anywhere on it; a date that has gone by looks like any other date.

### Clock (`components/ui/Clock.tsx`)
Viewer-local date + time as a `.label`, top right. Renders empty on the server and fills on mount (no hydration mismatch); `.clock` fades it in over 320ms when it is set (`.is-set`), rather than popping in. The account button beside it (`AuthControls`) sits in `.auth-slot`, which holds its 28px place while Clerk loads, so the clock doesn't slide 60px left when the button arrives; the button fades in (`arrive`, 320ms). Neither moves under reduced motion.

### Icons
Inline SVG, `stroke="currentColor"`, 1.4–1.6 stroke, round caps. No icon font, no icon library. **An icon may carry meaning when its name is always a hover, a focus or a tap away** — the nav's places are icons on the rail with their names on the parchment (2026-09-13, Chanté's call; it replaces "plain names, no icons" in the nav). An icon with no way to see its name is still out, and every icon-only control has an `aria-label`.

### Favicon (`app/favicon.ico`, `app/icon.png`, `app/apple-icon.png`)
**Chanté's compass star** (`art/scenery/library/icon.png`, 2026-09-13): the gold four-pointed star inside interlaced rings, the same mark as the star at the head of the nav rail. `scripts/make-favicon.py` keys it off its white ground and writes three files Next links on its own: `favicon.ico` (16, 32, 48) and `icon.png` (32) for tabs, with the strokes thickened and pulled toward the mid gold so the hairlines survive 16px on light and dark tabs; and `apple-icon.png` (180), the untouched mark on the rail's olive green, opaque because iOS fills transparency with black. Replaces the SVG of Lumi's head in the forest circle (2026-09-12). The proxy's matcher already skips `.png` and `.ico`, so the icons are served without sign-in.

---

## Accessibility

- Every icon-only button has an `aria-label`.
- Every animation stops under `prefers-reduced-motion` — the companion (which follows the setting live, not only at load), messages landing, the tick drawing, the thinking dots (held at a steady 0.6) and the listening mic.
- Nav uses `aria-current="page"`; its rows carry `aria-label` (the names are hidden while the parchment is folded), keyboard focus in the nav opens the parchment, and the compass star is a button with `aria-expanded`.
- Focus: one ring for the whole book — `:focus-visible` is a 1px brass outline offset 3px. The composer textarea suppresses it (the pill's `.is-listening`/border carries state).
- Selection is brass-soft on ink. Scrollbars are thin and translucent brass, never the browser grey.
- Contrast: ink on paper ≈ 14:1; ink-mute on paper ≈ 3.4:1 — mute is for non-essential text only.
